# Adarian MVP — 过期/无用文件审查

> 审查日期：2026-06-03
> 审查范围：`adarian mvp/` 项目根（不含 WorkflowBase/ — 为新迁移底座，已另做审查）
> 总项目大小：554 MB（含 .venv 406MB）

---

## 一、🟢 可直接删除

| 项目 | 大小 | 说明 |
|------|------|------|
| `profiling/output/_to_be_deleted/` | 20 KB | 名字已经说了 |
| `_deprecated/` | 空 | 空目录 |
| `.chromadb/` | 0 B | 空目录 |
| `docs/papers/` | 空 | 空目录 |
| 全局 `__pycache__/` | 合计 ~几 MB | gitignored，随时可清 |
| 全局 `.DS_Store` | 若干 | gitignored |

---

## 二、🟡 大概率过期（≤1 周后无用）

### 2.1 `.claude/worktrees/` — 6 份 Agent 工作副本

| 项目 | 值 |
|------|-----|
| 大小 | **27 MB** |
| 内容 | 6 份完整的项目快照（audit/docs/src/profiling 全部拷贝） |
| 来源 | Claude Code Agent Team 每次启动创建的缓存工作区 |
| 判断 | 旧版本 Agent Team 遗留产物。Git 记录在，工作副本可用 repo clone 重建 |

### 2.2 `outputs/runs/` — 49 个旧测试运行目录

| 项目 | 值 |
|------|-----|
| 大小 | **4.8 MB** |
| 内容 | test1~test8 的 49 次运行记录（v1.1~v1.2 迭代时期的 smoke/benchmark） |
| — | `outputs/outputs_test2/`、`outputs/outputs_test5/`、`outputs/test7/` 等 |

### 2.3 `outputs/` 根目录杂项

| 目录 | 大小 | 说明 |
|------|------|------|
| `outputs/outputs_test2/` | 旧测试产出 | 可能已无参考价值 |
| `outputs/outputs_test2_v1.1.6/` | 同上 | v1.1.6 时代的测试 |
| `outputs/outputs_test3/` | 同上 | v1.1 时代 |
| `outputs/outputs_test5/` | 同上 | v1.1 时代 |
| `outputs/run_test3_20260424_163044/` | 同上 | 4 月 24 日的单次运行 |

### 2.4 `audit/` — 4.1 MB 历史审计证据

包含 phase1/phase4 大版本改造的完整审计记录、DS Team 审查报告、产品侧审查。

**注意**：其中部分内容在 `.claude/worktrees/*/audit/` 下还有 **6 份副本**，实际磁盘占用 ~28MB。

### 2.5 `profiling/output/` — 性能测试存档

| 项目 | 值 |
|------|-----|
| 大小 | 696 KB |
| 内容 | `runs_archive/` 含 10+ 旧 profiling 运行记录；`_to_be_deleted/` 可直接删 |
| 判断 | Profiling 工具链（`profiling/` 下源码）本身可以保留，输出存档可清理 |

---

## 三、🔵 已被替代/过时

### 3.1 `docs/debug/` — 旧调试文档

```
docs/debug/
├── 2026-03-30_Enhanced-SEIR-Stance-模型设计文档.md
└── 2026-03-30_问题诊断报告.md
```

两份文档同时存在于 `docs/_archive/debug/`，`debug/` 是冗余副本。

### 3.2 `docs/audit/` — 工作流审计

```
docs/audit/
├── project/       — last_good_commit 调查、phase3 诊断
└── workflow/      — control_plane_retirement, git_freeze, workflow_audit 等
```

根目录 `audit/` 已包含更完整的审计记录，`docs/audit/` 是早期版本子集。

### 3.3 `docs/skills/workflow_core.md` — v3.0 工作流

你自己说的"过时的东西，过时的定义"。v4.0 在 `docs/skills/workflow_v4.0/` 下。

### 3.4 `deliverables/workflow_review/` — 4 份审查报告

```
Adarian项目推进计划与二师兄DAG工作流深度审查报告_v0.1.md
agent_b_report.md / agent_c_report.md / agent_d_report.md
```

2026-05 期间的 workflow 对比审查。现在已搬 v4.0，这些报告不再需要。

### 3.5 `learning/` — 50+ 份未落地架构设计

```
learning/项目推进计划/
├── Agent工厂架构/
├── 流水线分层架构/   — Docker容器启动、事件流回传、Skill自动绑定...
└── 平台解耦方案/
```

大量概念设计文档（第0层~第9层架构、Docker容器、Redis-SSE桥接等），从未落地实现。
如果以后要用可以放回，但当前没有维持的必要。

### 3.6 `profiling/` 本身可能也需要评估

`profiling/` 下有完整的 benchmark 框架（chain_benchmark, profile_runner 等），如果不再做 model profiling 可以整体归档。

---

## 四、⚪ 需你判断的

| 项目 | 大小 | 问题 |
|------|------|------|
| `seeds/` | 10 个测试种子文件 | test1~test8 + example_event — 是否仍用于冒烟测试？ |
| `scripts/probes/` | 3 个 probe 脚本 | p1a/p1g prompt probe — 已不用？ |
| `docs/_archive/` | 30+ 文件 | 已归档，但很多在 `.claude/worktrees/` 下有 6 份副本 |
| `src/schemas/_legacy.py` | 1 文件 | 遗留 schema，可能未引用 |
| `tests/` | 14 个测试 | phase4 相关的测试 — 如果不再开发 v1.2 功能，可归档 |

---

## 五、各区域磁盘占用总览

```
554 MB  总项目
├── 406 MB  .venv/                    ← Python 虚拟环境
├──  27 MB  .claude/                  ← 其中 worktrees 6 份副本
├──   5 MB  outputs/                  ← 旧测试运行记录
├──   4 MB  audit/                    ← 历史审计证据
├──   1 MB  profiling/output/         ← profiling 存档
├── 696 KB  profiling/output/         ← 同上
├──  .5 MB  docs/                     ← 含大量归档/过时文档
└──   ~2 MB  seeds/ scripts/ etc.     ← 零散文件
```

---

> **建议清理顺序**：
> 1. `profiling/output/_to_be_deleted/` + `_deprecated/` — 零成本
> 2. `.claude/worktrees/` — 节省 27MB
> 3. `outputs/runs/` — 节省 5MB
> 4. `docs/debug/` + `deliverables/` + `learning/` — 清理逻辑混乱
> 5. `audit/` — 大块，但需确认历史引用
