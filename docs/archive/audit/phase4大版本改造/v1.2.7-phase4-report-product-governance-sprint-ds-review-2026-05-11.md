# DS Agent Team Review Report

## 1. Metadata

```text
review_target:
- v1.2.7 iteration document
- roadmap v0.2.1
- prompt externalization amendment (embedded)
- PRD v0.1

review_type: v1.2.7_iteration_readiness_review
team_mode_used: true
review_date: 2026-05-11
timezone: Asia/Tokyo
ds_verdict: CONDITIONAL_GO
report_path: audit/phase4大版本改造/v1.2.7-phase4-report-product-governance-sprint-ds-review-2026-05-11.md
```

### 路径确认

审查对象实际路径：

```text
1. docs/iterations/v1.2.7 - Phase 4 Report Product Governance Sprint - prompt reprosity.md  ✅ 存在
2. audit/phase4大版本改造/Phase4_Report_Product_Governance_Track_Roadmap_v0.2.1.md        ✅ 存在
3. audit/phase4大版本改造/Phase4_Prompt_Externalization_Amendment_v0.1.md                  ❌ 不存在（内容嵌入 Roadmap §4.10 和 iteration doc §4.2）
4. audit/phase4大版本改造/Adarian_Report_Product_Contract_PRD_v0.1.md                     ✅ 存在
5. 当前源码树                                                                              ✅ 可访问
```

注意：`Phase4_Prompt_Externalization_Amendment_v0.1.md` 未作为独立文件存在。Prompt 外置化内容嵌入在 Roadmap v0.2.1 §4.10 和迭代文档 §4.2 "Additional Adoption" 中。Roadmap 头声明 `v0.2.1 revised / prompt externalization amended` 可接受作为替代。

---

## 2. Reviewer Agents

```text
1. Workflow Governance Reviewer:
   verdict: CONDITIONAL_GO
   key_findings:
   - 文档完全满足"文档驱动、审计优先、最小落地"原则，§3.3-3.4 定义 13 项不解决问题和 14 项禁止变化
   - v1.2.6 closeout 明确承认（line 18: "base_version_status：v1.2.6 closed / acceptance_result = pass"）
   - 单一主问题 "Phase 4 报告产品化 R0 闭环" 定义清晰，6 项子任务均收敛于同一目标
   - attempt 拆分合规：串行执行，显式依赖，独立允许/禁止文件列表，无文件重叠
   - 工作树存在 dirty files（src/__init__.py, src/phase3/tick_simulation.py, src/schemas.py 已删除）
     必须在 Codex 开始前清理或提交，因为 v1.2.7 禁止修改 Phase 3
   - 文档状态为 "draft / pending owner approval"，需 Owner 批准后方可作为 Codex 执行依据

2. Phase 4 Architecture Reviewer:
   verdict: CONDITIONAL_GO
   key_findings:
   - v1.2.7 正确避免过早拆分 Phase 4 架构，§5.3 列出 9 个禁止创建的模块
   - report_prompts.py 被合理允许，附带 5 项边界约束
   - report_agent.py 保持为单一入口，不做大规模重构
   - _llm_generated_markdown 模块级全局变量（report_agent.py:342）是隐性耦合通道：
     generate_report_with_llm() 和 save_markdown_report() 通过可变全局状态耦合
   - 存在两条 Markdown 生成路径（LLM 路径 vs fallback 路径），generated_at 等新字段必须在两条路径中一致实现
   - report_prompts.py 与 report_prompt_builder.py 之间的边界需要硬化（仅允许纯数据，禁止函数/方法）

3. Product Report Contract Reviewer:
   verdict: CONDITIONAL_GO
   key_findings:
   - 10 项产品侧报告 contract 规则全部在 v1.2.7 迭代文档中正确落地（PASS × 10）
   - 五章模板（舆情概要/演化分析/风险研判/对策建议/附录）被正确采纳
   - report_type 固定为 "模拟推演型舆情风险研判报告"
   - generated_at 由代码侧生成、不由 LLM 生成的要求明确
   - "指标后台化，判断前台化" 原则正确实施
   - 风险研判结构为 "风险等级 + 主要风险类型 + 风险解释"
   - 对策建议严格限定为舆情防范与回应建议
   - 禁止伪装未实现能力（外部检索/政策知识库/完整 selector）
   - 源码尚未修改——所有改动等待 attempt-01/attempt-02 执行
   - 风险等级 4 级 vs PRD 6 级是有意选择的已知差异

4. Engineering Feasibility Reviewer:
   verdict: GO
   key_findings:
   - v1.2.7 规模合理：6-8 文件、~400-500 行变更，与 DS 预审计估算 23 工时一致
   - attempt-01 scope 干净：schemas/phase4.py + report_agent.py + test_report_product_contract.py
   - attempt-02 scope 干净但共享 report_agent.py（须串行，diff 叠加于 attempt-01 最终状态）
   - closeout-patch scope 适当最小
   - src/schemas/__init__.py 未在 allowed-modify 列表中——新增类型需要导出条目
   - 夹具（report_product_basic.json）标记为"可选"但实际需要——建议升级为推荐
   - 一周可行：7 个日历天 vs 23 工时，保守按 4-5 生产小时/天计算有 5-12 小时余量
   - 无硬验收标准需要降级：20 项硬标准均可测量和客观判定

5. Prompt Governance Reviewer:
   verdict: CONDITIONAL_GO
   key_findings:
   - v1.2.7 在三个位置明确允许创建 src/phase4/report_prompts.py
   - 正确禁止完整 Prompt Registry（"不引入 Prompt Registry"）
   - 正确禁止 docs/prompts/phase4/ 作为本轮硬目标
   - 正确避免扩展为完整 Prompt Governance（report_prompt_builder.py 在禁止列表中）
   - report_prompts.py 计划内容（system prompt/Markdown grounding rules/risk expression rules/simulation wording rules/forbidden phrase rules）一致
   - 边界（无业务逻辑/文件 IO/LLM 调用/指标计算/whitebox 检查）隐式存在于 "承接静态 prompt 文本" 和 "不改变 Phase 4 报告生成主链语义"
   - report_prompts.py 缺失于 §5.2 目标结构图
   - 无专门的 report_prompts.py 内容边界验收标准——可能允许未检测到的范围蔓延
```

---

## 3. Overall Verdict

```text
CONDITIONAL_GO

方向正确，文档结构坚固，但进入 Codex attempt-01 前需要完成 Doc Patch。
所有 5 位 reviewer 均未发现 hard blocker。
4 位给出 CONDITIONAL_GO，1 位（Engineering Feasibility）给出 GO。
累计发现 7 项 MEDIUM 问题和 10+ 项 LOW 问题，无 HIGH 问题。
MEDIUM 问题均可在 Doc Patch 中解决，无需第二轮 DS 审查。
```

---

## 4. Must-Fix Verification Matrix

```text
must_fix_01 (风险类型白名单前移):
  status: fixed
  evidence: §4.2 Must Fix #1 — 完整轻量白名单前移至 v1.2.7（13 种风险类型），定义在 schemas/phase4.py 或 report_agent.py 附近最小常量中，不创建独立 risk_taxonomy.py
  risk: LOW — 白名单 13 项可覆盖常见场景，但"属地治理压力风险"等个别类型可能缺乏明确触发条件
  recommendation: 无。当前采纳正确。

must_fix_02 (风险等级枚举 4 级 vs 6 级):
  status: fixed
  evidence: §4.2 Must Fix #2 — 明确沿用 4 级代码枚举（low/medium/high/critical），新增中文 label 映射（低风险/中风险/高风险/重大风险），PRD 6 级预留为后续产品表达
  risk: LOW — PRD 中"中低/中高"缺失可能导致报告风险粒度不够，但这是有意设计选择
  recommendation: 无。采纳合理。

must_fix_03 (audience_mode 填充策略):
  status: fixed
  evidence: §4.2 Must Fix #3 — 最小 deterministic 规则：默认 generic_government，基于关键词匹配 law_enforcement_facing / regulator_facing / public_management_facing
  risk: LOW — 关键词匹配存在误判可能（如"执法记录仪"不含执法主体），但已列为 known issue（§9.2 item 1）
  recommendation: 无。最小规则适合 R0。

must_fix_04 (禁止伪装表达纳入 Markdown grounding):
  status: fixed
  evidence: §4.2 Must Fix #4 — 明确列出 6 项禁止表达："综合全网信息显示"/"依据相关法规建议"/"系统已识别最具代表性观点"/"全网舆情已经"/"公众普遍认为"/"现实中已经形成"
  risk: LOW — 禁止列表限于硬编码字符串匹配，LLM 可能产生语义等价但字面不同的表达（如"根据全网监测"替代"综合全网信息显示"）。attempt-02 的 test_report_markdown_grounding.py 应测试语义变体。
  recommendation: 在 attempt-02 实施时考虑添加语义变体测试案例。

must_fix_05 (对策建议边界纳入 prompt 约束):
  status: fixed
  evidence: §4.2 Must Fix #5 — 明确边界："系统输出的是舆情风险防范建议，不是法律意见、行政处罚建议或正式决策指令"，禁止 5 项越界行为
  risk: LOW — 边界在 prompt 层面声明但非确定性执行；LLM 在低概率下仍可能越界
  recommendation: 无。当前边界适合 R0。v1.2.10 的 whitebox check 可增加越界检测。

must_fix_06 (五章模板迁移策略):
  status: fixed
  evidence: §4.2 Must Fix #6 — 明确选择迁移到 PRD 五章模板（舆情概要/演化分析/风险研判/对策建议/附录），不新增"核心结论"第一章，不实现双输出模式，whitebox REQUIRED_SECTION_GROUPS 必须同步更新
  risk: LOW — 模板迁移涉及 report_agent.py 中 REPORT_SYSTEM_PROMPT 大幅重写，需谨慎
  recommendation: 无。采纳正确。

must_fix_07 (Day1-Day2 之间 Doc Patch 缓冲):
  status: fixed
  evidence: §4.2 Must Fix #7 — 采纳为流程规则：Owner 审阅迭代文档 → DS/Owner 提出边界修正则先 patch → Codex 不得在未冻结前开始
  risk: LOW — 当前文档状态为 "draft / pending owner approval"，缓冲日正在执行中
  recommendation: 在 Codex 开始前必须完成 Owner 审批和文档冻结。

must_fix_08 (report_schema_0507.md 拐点检测归属冲突):
  status: fixed
  evidence: §4.2 Must Fix #8 — 明确：拐点由代码侧/deterministic logic/whitebox 提供，LLM 只负责解释 code-owned inflection_points，无显著拐点则写"本轮模拟未发现显著拐点"
  risk: MEDIUM — report_schema_0507.md 文件在 git status 中显示为已删除（D audit/report_schema_0507.md），但其内容（§2.4.5）声明拐点"报告生成侧独立判断，不依赖 inflection_points 字段"，与 v1.2.7 的 code-owned 方向直接冲突。如果 SKILL.md 或其他文档引用 report_schema_0507.md 作为权威 schema，则该冲突继续存在。
  recommendation: 确认 report_schema_0507.md 已被正式退役（deleted），且 audit/phase4大版本改造/SKILL.md 中的报告生成逻辑不再引用其拐点独立判断规则。

must_fix_09 (whitebox section headings 对齐五章模板):
  status: fixed
  evidence: §4.2 Must Fix #9 — 明确旧 heading 组（事件概述/舆情态势/风险评估/处置建议）必须更新为（舆情概要/演化分析/风险研判/对策建议/附录），whitebox report_completeness.py 必须同步更新
  risk: MEDIUM — 当前 report_completeness.py 的 REQUIRED_SECTION_GROUPS 仍使用旧 headings（事件概述/舆情态势/风险评估/关键风险点/应对建议），FINAL_SECTION_HEADINGS 使用"综合建议/应对建议"需同步更新为"对策建议"。迭代文档只显式提到 REQUIRED_SECTION_GROUPS 但未提到 FINAL_SECTION_HEADINGS。
  recommendation: 在 attempt-02 实施时同时更新 REQUIRED_SECTION_GROUPS 和 FINAL_SECTION_HEADINGS。
```

---

## 5. Prompt Externalization Review

```text
allow_src_phase4_report_prompts_py: yes

should_be_attempt_01_or_02: attempt-02
  迭代文档正确放置——attempt-01 不包含 report_prompts.py，attempt-02 在 allowed-modify 和 allowed-new 中列出。
  Roadmap §5.5 一致。

allowed_content:
  - system prompt (REPORT_SYSTEM_PROMPT 静态文本)
  - Markdown grounding rules (五章模板结构约束)
  - risk expression rules (风险等级 label 映射、风险类型白名单)
  - simulation wording rules (模拟推演口径约束)
  - forbidden phrase rules (禁止表达清单)

forbidden_content:
  - 业务逻辑（条件判断、路由逻辑、数据转换）
  - 文件 IO（不得自行读写文件）
  - LLM 调用（不得调用 llm_client）
  - 指标计算（不得计算 event_scale/polarization_index 等）
  - whitebox 检查（不得导入或调用 whitebox 模块）

risk:
  MEDIUM — report_prompts.py 与 report_prompt_builder.py 之间的边界在当前迭代文档中是隐式的（"不创建完整 report_prompt_builder.py"），而非显式的（"report_prompts.py 不得包含函数或方法"）。实施者可能将 prompt 组装逻辑放入 report_prompts.py。
  MEDIUM — report_prompts.py 缺失于 §5.2 目标结构图，但在 §6.1 和 §7.3 中列出——文档一致性缺口。
  MEDIUM — 9.1 Hard Acceptance Target 中无专门的 report_prompts.py 内容边界标准。

recommendation:
  1. 在 §5.2 目标结构图中添加 report_prompts.py。
  2. 添加显式约束："report_prompts.py 仅允许字符串常量、列表、字典等纯数据定义，不得包含函数或方法"。
  3. 在 9.1 或 7.3 验收标准中添加一项："report_prompts.py 仅包含静态 prompt 常量和 grounding 规则，无函数/方法/类定义"。
```

---

## 6. File Scope Review

```text
Allowed files are sufficient:
conditional

Potential missing allowed files:
1. src/schemas/__init__.py — 新增 ReportMeta、AudienceMode 等类型需要更新 schemas 包的 __init__.py 导出条目。当前不在 §6.2 allowed-modify 列表中，但也不在 forbidden 列表中。建议显式添加到 allowed-modify。
2. FINAL_SECTION_HEADINGS（report_completeness.py:17-22）— 迭代文档要求更新 REQUIRED_SECTION_GROUPS 但未显式提到 FINAL_SECTION_HEADINGS（用于截断检测），应与五章模板同步更新。

Overly broad allowed files:
1. 无。allowed-modify 列表范围适当。

Forbidden files risk:
1. artifact_check.py 的例外条款（"除非仅为路径兼容的最小修正"）存在歧义——如果 report_completeness.py 改变输出格式，artifact_check.py 可能需要对应感知。建议对本轮完全禁止 artifact_check.py，或明确定义例外边界。
2. 当前工作树中存在 Phase 3 修改（src/phase3/tick_simulation.py modified），而 Phase 3 是 v1.2.7 的禁止区域。必须在 Codex 开始前清理。
```

---

## 7. Attempt Split Review

```text
attempt-01:
  verdict: GO
  comment: Scope 聚焦且干净——schemas/phase4.py + report_agent.py + test_report_product_contract.py。
  新增字段（ReportMeta/AudienceMode/risk expression）和 JSON/metadata wiring 形成闭环。
  不包含 whitebox 或 Markdown grounding，防止范围蔓延。

attempt-02:
  verdict: GO
  comment: Scope 正确——report_agent.py + report_prompts.py + report_completeness.py + test_report_markdown_grounding.py。
  与 attempt-01 共享 report_agent.py 但串行执行规则（§7.5）防止冲突。
  实施时需注意 attempt-02 的 diff 叠加于 attempt-01 最终状态。

closeout-patch:
  verdict: GO
  comment: 最小范围——仅文档同步、DS verify 要求的小修、smoke test、evidence 记录。
  "不得新增功能" 边界清晰。
```

---

## 8. Hard Acceptance Criteria Review

```text
clear: yes
  20 项硬标准均以简明中文列出，附可验证的通过/失败条件。

testable: yes
  19/20 项为二元检查（字段存在/值匹配/短语不出现/文件存在）。唯一主观项 #14（"正文不大面积裸露技术指标"）可通过 test_report_markdown_grounding.py 中针对 event_scale/polarization_index/stance_delta 的明确 forbidden-pattern 断言变为客观。

too strict: no
  所有 20 项均与核心目标对齐。无过度约束项。

too loose: partial
  #9（"primary_risk_types 来自轻量白名单"）——检查"来自白名单"但不检查 LLM 报告正文中是否出现白名单外的风险类型。可在 attempt-02 验收中补充此项。

missing:
  1. report_prompts.py 内容边界（仅静态常量和规则，无函数/方法）
  2. FINAL_SECTION_HEADINGS 对齐（与 §4.2 Must Fix #9 的 REQUIRED_SECTION_GROUPS 对齐并行）
  3. 两条 Markdown 生成路径（LLM 路径和 fallback 路径）均包含 generated_at 的一致性检查
```

---

## 9. Risks

```text
risk_id: R1
risk_level: MEDIUM
description: 工作树 dirty——src/__init__.py、src/phase3/tick_simulation.py 已修改，src/schemas.py 已删除。v1.2.7 禁止修改 Phase 3。如果这些变更混入 v1.2.7 diff，将违反核心约束。
mitigation: Codex 开始前执行 execution pre-condition #4，清理或提交 dirty files。

risk_id: R2
risk_level: MEDIUM
description: _llm_generated_markdown 模块级全局变量（report_agent.py:342）将 generate_report_with_llm() 和 save_markdown_report() 通过可变全局状态耦合。如果 generate() 被调用两次，只有最后一个 markdown 被保留。如果 save() 在 generate() 之前调用，静默回退到非 LLM 路径。添加 generated_at 到 markdown 时可能引发不一致。
mitigation: 在 v1.2.7 中将此明确列为 known issue；考虑最小缓解——让 generate_report_with_llm() 返回包含 Phase4Output 和 markdown 字符串的 dataclass/元组，消除模块级全局变量。约 3 行改动，无架构拆分。

risk_id: R3
risk_level: MEDIUM
description: report_prompts.py 与 report_prompt_builder.py 之间的边界模糊。"prompt 常量"和"prompt 组装逻辑"之间的界限依赖实施纪律。
mitigation: 在迭代文档中添加显式约束——report_prompts.py 仅允许纯数据定义（字符串常量、列表、字典），禁止函数或方法。

risk_id: R4
risk_level: MEDIUM
description: 两条 Markdown 生成路径（LLM 路径和 fallback 路径）——generate_report_with_llm() -> _llm_generated_markdown -> save_markdown_report() vs generate_markdown_report() -> save_markdown_report()。generated_at 和其他元数据注入必须在两条路径中一致实现。
mitigation: 在 attempt-01 验收标准中明确要求两条路径均验证。在 test_report_product_contract.py 中包含两条路径的测试案例。

risk_id: R5
risk_level: LOW
description: report_schema_0507.md 已删除（git status 显示 D），但其拐点独立判断规则仍可能被 audit/phase4大版本改造/SKILL.md 或报告生成 prompt 引用。该文件的删除状态需确认归档/替换策略。
mitigation: 验证 SKILL.md 和活跃 prompt 中不再引用 report_schema_0507.md 的独立拐点判断规则。

risk_id: R6
risk_level: LOW
description: 迭代文档文件名使用 "prompt reprosity"——疑似 "repository" 或 "reproducibility" 的拼写错误。虽然内容的 §4.2 正确描述为 "Prompt 外置化"，但文件名不一致可能引发混淆。
mitigation: 考虑重命名为 "... - prompt 外置修正版.md" 或在 §1 Version Info 中添加文件名说明。

risk_id: R7
risk_level: LOW
description: Phase4_Prompt_Externalization_Amendment_v0.1.md 未作为独立文件存在。审查对象 #3 的内容嵌入在 Roadmap v0.2.1 §4.10 和迭代文档 §4.2 中。虽然没有功能影响，但审查命令中引用了不存在的路径。
mitigation: 如果 Owner 希望有独立的外置化修正文档，创建该文件；否则在 Roadmap 中标注 "embedded" 已足够。
```

---

## 10. Required Corrections Before Codex

```text
1. [MEDIUM] 清理工作树——src/__init__.py、src/phase3/tick_simulation.py 修改需提交或还原。Phase 3 是 v1.2.7 的禁止区域，这些变更不得混入本轮 diff。
2. [MEDIUM] 在 §5.2 目标结构图中添加 src/phase4/report_prompts.py。
3. [MEDIUM] 添加 report_prompts.py 的显式边界约束："仅允许字符串常量、列表、字典等纯数据定义，不得包含函数或方法"。
4. [MEDIUM] 在 9.1 Hard Acceptance Target 或 7.3 through-conditions 中添加验收项：report_prompts.py 不得包含业务逻辑、文件 IO、LLM 调用、指标计算或 whitebox 检查。
5. [LOW] 将 src/schemas/__init__.py 添加到 §6.2 allowed-modify 列表（新增类型需要导出）。
6. [LOW] 在 §7.3 或 §4.2 Must Fix #9 中添加 FINAL_SECTION_HEADINGS 对齐要求（与 REQUIRED_SECTION_GROUPS 并行）。
7. [LOW] 将 _llm_generated_markdown 全局耦合明确列为已知问题（§9.2），并为两条 Markdown 路径推荐最小缓解措施。
```

---

## 11. Final Recommendation

```text
Can enter Codex attempt-01?
conditional

Need doc patch before Codex?
yes — 上述 7 项修正中的 4 项 MEDIUM 项应在 Owner 批准迭代文档时一并处理

Need another DS review after doc patch?
no — 所有 MEDIUM 问题均为文档级边界澄清，不需要第二轮 DS Agent Team

Recommended next action:
1. Owner 审阅本 DS 审查报告
2. Owner 对 v1.2.7 迭代文档执行 Doc Patch（上述 10 项修正中的 7 项）
3. 清理工作树 dirty files（提交或还原 Phase 3 修改、src/__init__.py 修改）
4. Owner 冻结迭代文档
5. 向 Codex 发送 attempt-01 执行

预计 Doc Patch 工作量：~30 分钟（编辑 1 个文件，添加若干行）
预计 attempt-01 开始时间：Doc Patch 完成后立即可
```

---

*DS Agent Team Review completed. 5 reviewers, team_mode_used = true, 0 hard blockers, 7 corrections recommended before Codex.*
