# DS Agent Team Review: v1.2.5.1 Source Tree Governance Completion

**Review date**: 2026-05-07
**Project**: Adarian MVP
**Phase**: Phase 1 Generation Governance — v1.2.5 closeout completion patch
**DS Master**: 汇总与冲突消解

---

## 0. Team Mode

**team_mode_used**: `true`

**reviewers**:

| Reviewer | Status | Key Finding |
|---|---|---|
| Source Cleanup Reviewer | **completed** | 3 isolated files → ARCHIVE, CHANGELOG has 3 false entries |
| Import Migration Reviewer | **completed** | 9 imports correct, 1 export gap, 0 missed consumers |
| Runtime & Test Reviewer | **completed** | Verification gaps found: missing py_compile, test7, probe placement |
| Documentation Sync Reviewer | **completed** | MUST add dev_spec.md, bump __version__, correct CHANGELOG |
| Deletion Risk Reviewer | **completed** | Phase1 shim MEDIUM (1 gap), Phase2/3/4 LOW; serial execution required |

---

## 1. Repository State

```
current_branch:         master
latest_commit:          acf8e7e "chore: close out v1.2.5 whitebox artifact shell"
latest_tags:            iter-v1.2.5-closeout, baseline-test1-e2e-runnable
git_status_summary:     code tree clean; 4 untracked files (audit reports + iteration docs)
rollback_anchor:        iter-v1.2.5-closeout tag (matching HEAD)
```

```
clean_tree:             true (code files only)
closeout_tag_exists:    true
rollback_risk:          LOW
```

---

## 2. Executive Verdict

**DS_verdict**: `CONDITIONAL_GO_AFTER_PLAN_PATCH`

**one_sentence_decision**: 计划方向正确、消费者迁移路径完整、风险可控——但需要 6 处补丁（含新增 dev_spec.md 同步、CHANGELOG 勘误、test_legacy_shim_imports.py 处置、py_compile 补充）后才能交给 Codex 执行；执行时 4 个 attempt 必须严格串行。

**定性修正**（owner 已确认）：

> 本轮不叫 "Legacy Source Cleanup"，而叫 **Source Tree Governance Completion**。
> 它是 v1.2.5 的 closeout completion patch，不是独立新版本。
> v1.2.5 做了上半场（建立 phase package），v1.2.5.1 把下半场补齐（退役旧 flat 文件、迁移消费者、同步文档）。

版本链路：

```
v1.2.5  → Source Tree Governance & Whitebox Artifact Shell
v1.2.5.1 → Source Tree Governance Completion
v1.2.6  → Schema Split Governance
```

---

## 3. Source Cleanup Review

### 3.1 Archive Candidates

| file | recommendation | reason | referenced_by | risk |
|---|---|---|---|---|
| `src/phase0_entity_extraction.py` | **ARCHIVE** | 含 6 个独特 prompt 常量、完整 Phase 0 实现、v1.1.1 架构记录 | 仅 `phase1_persona_engine.py:445` 中的 CLI help 字符串（该文件本身也将归档） | LOW |
| `src/phase1_persona_engine.py` | **ARCHIVE** | 含 `shrink_to_agents()` 收缩算法（未在其他地方复现）、原始 PHASE1 prompt | 无 | LOW |
| `src/agent_quality_analyzer.py` | **ARCHIVE** | 含立场分布统计、diff 相似度检测、逻辑一致性验证（当前 pipeline 无此能力） | 无 | LOW |

**归档路径**: `docs/_archive/legacy/`（需新建目录 + README.md 说明文件来源和状态）

**关键发现**：CHANGELOG 2026-04-08 有三条**与磁盘事实不符的声明**：
- Line 88: 声称 README 已移除旧文件描述 → **虚假**（README 仍引用）
- Line 89: 声称 `src/__init__.py` 已标注文件为历史脚本 → **虚假**（未写入）
- Lines 617-619: 声称三个文件已删除 → **虚假**（文件仍在磁盘）

这构成文档完整性问题。v1.2.5.1 的 CHANGELOG 条目中必须勘误。

### 3.2 Files Not To Touch

| file/path | reason |
|---|---|
| `scripts/probes/` (全部) | 活跃生产 profiling 资产 |
| `profiling/` (全部) | 活跃生产 profiling 资产 |
| `profiling/output/` (全部) | 历史 profiling run 档案，含 `phase1_entity_extraction.py` 元数据字符串——是档案不是代码依赖 |
| `docs/_archive/control_plane/` | 已退役控制面档案，不归本轮处理 |
| `docs/iterations/v1.2.5-*.md` | v1.2.5 closeout 历史记录，不得回改 |
| `.venv/` | 虚拟环境，不归版本管理 |
| `src/phase1/extraction.py` | 活跃主链代码（含两条过时注释，可在 attempt-04 顺手改，不是强制要求） |
| `src/phase1/prompts.py` | 活跃主链代码（含一条过时注释，同上） |

---

## 4. Import Migration Review

### 4.1 Required Export Patch

| file | symbol | required_action | risk_if_missing |
|---|---|---|---|
| `src/phase1/__init__.py` | `_normalize_unescaped_quotes_inside_string_values` | 加入 imports 块和 `__all__` 列表 | **HIGH** — `tests/test_json_parser_quote_tolerance.py` 会 ImportError |

**验证**：除上述 1 个符号外，所有其他消费者符号在对应 `__init__.py` 中已全部导出。Phase2/3/4 的 `__init__.py` 无需修改。

### 4.2 Consumer Migration Table（9 条改动，6 个文件）

| # | consumer_file | old_import | new_import | status | risk |
|---|---|---|---|---|---|
| 1 | `profiling/prompts.py:5` | `from src.phase1_entity_extraction import (GENERATOR_SYSTEM_PROMPT, GENERATOR_USER_PROMPT, VALIDATOR_SYSTEM_PROMPT, VALIDATOR_USER_PROMPT)` | `from src.phase1 import GENERATOR_SYSTEM_PROMPT, GENERATOR_USER_PROMPT, VALIDATOR_SYSTEM_PROMPT, VALIDATOR_USER_PROMPT` | correct | LOW |
| 2 | `scripts/probes/p1a_prompt_probe.py:23` | `from src.phase1_entity_extraction import ANALYZER_SYSTEM_PROMPT, ANALYZER_USER_PROMPT` | `from src.phase1 import ANALYZER_SYSTEM_PROMPT, ANALYZER_USER_PROMPT` | correct | LOW |
| 3 | `scripts/probes/p1g_prompt_probe.py:23` | `from src.phase1_entity_extraction import GENERATOR_SYSTEM_PROMPT, GENERATOR_USER_PROMPT` | `from src.phase1 import GENERATOR_SYSTEM_PROMPT, GENERATOR_USER_PROMPT` | correct | LOW |
| 4 | `tests/test_legacy_shim_imports.py:8` | `from src.phase1_entity_extraction import ANALYZER_SYSTEM_PROMPT` | `from src.phase1 import ANALYZER_SYSTEM_PROMPT` | correct — **但建议删除此文件** | LOW |
| 5 | `tests/test_legacy_shim_imports.py:9` | `from src.phase1_entity_extraction import extract_entities_from_file` | `from src.phase1 import extract_entities_from_file` | correct — **但建议删除此文件** | LOW |
| 6 | `tests/test_legacy_shim_imports.py:10` | `from src.phase2_topology_builder import build_topology_from_extraction` | `from src.phase2 import build_topology_from_extraction` | correct — **但建议删除此文件** | LOW |
| 7 | `tests/test_legacy_shim_imports.py:11` | `from src.phase3_tick_simulation import SimulationEngine` | `from src.phase3 import SimulationEngine` | correct — **但建议删除此文件** | LOW |
| 8 | `tests/test_legacy_shim_imports.py:12` | `from src.phase4_report_agent import generate_report_with_llm` | `from src.phase4 import generate_report_with_llm` | correct — **但建议删除此文件** | LOW |
| 9 | `tests/test_json_parser.py:16` | `from src.phase1_entity_extraction import _normalize_inner_cjk_quotes, _parse_json_candidate` | `from src.phase1 import _normalize_inner_cjk_quotes, _parse_json_candidate` | correct | LOW |
| 10 | `tests/test_json_parser_quote_tolerance.py:16` | `from src.phase1_entity_extraction import (_normalize_unescaped_quotes_inside_string_values, _normalize_inner_cjk_quotes, _parse_json_candidate)` | `from src.phase1 import _normalize_unescaped_quotes_inside_string_values, _normalize_inner_cjk_quotes, _parse_json_candidate` | correct | LOW |

**关于 `test_legacy_shim_imports.py`**：迁移后它与 `test_phase_package_imports.py` 有 **80% 断言重叠**（4/5 条）。仅有的独特价值是验证 `ANALYZER_SYSTEM_PROMPT` 为非空字符串。**建议在 attempt-02 中删除此文件**，将那一条独特断言并入 `test_phase_package_imports.py`。保留两个做同样事情的测试文件没有意义。

### 4.3 Missing Consumers

**None**。全量扫描确认只有这 6 个文件 / 10 条 import 指向旧 shim。`scripts/probes/reduced_schema_chain_probe.py` 已确认定义了自己的本地 prompt 常量，无需迁移。

---

## 5. Shim Deletion Review

| shim_file | current_consumers | safe_to_delete_after_migration | deletion_condition |
|---|---|---|---|
| `src/phase1_entity_extraction.py` | 6 文件 / 9 条 import | **YES** (条件：`_normalize_unescaped_quotes_inside_string_values` 加入 `__init__.py` 导出 + 所有消费者迁移 + rg 扫描确认零引用) | MEDIUM — 关闭 1 个 export gap 后降为 LOW |
| `src/phase2_topology_builder.py` | 1 文件 / 1 条 import | **YES** | LOW — 无 export gap |
| `src/phase3_tick_simulation.py` | 1 文件 / 1 条 import | **YES** | LOW — 无 export gap |
| `src/phase4_report_agent.py` | 1 文件 / 1 条 import | **YES** | LOW — 无 export gap |

**Rollback 安全性**：恢复 4 个 shim 文件只需 `git checkout HEAD~1 -- <files>`，< 30 秒。迁移后的消费者同时兼容新旧 import 路径（都解析到同一组符号），不会因恢复 shim 而产生冲突。

---

## 6. Verification Plan Review

### 必须保留

```bash
# 每个 attempt 都执行
python -m py_compile main.py
python -m pytest tests/ -v
python main.py seeds/test1.txt
```

### 必须新增

```bash
# attempt-02: 补全 src package 编译检查（main.py 使用 lazy import，py_compile 不解析）
python -m py_compile src/phase1/__init__.py
python -m py_compile src/phase2/__init__.py
python -m py_compile src/phase3/__init__.py
python -m py_compile src/phase4/__init__.py

# attempt-02: 迁移后验证所有新路径可解析
python -c "from src.phase1 import ANALYZER_SYSTEM_PROMPT, extract_entities_from_file, _normalize_inner_cjk_quotes, _parse_json_candidate, _normalize_unescaped_quotes_inside_string_values; print('phase1 OK')"
python -c "from src.phase2 import build_topology_from_extraction; print('phase2 OK')"
python -c "from src.phase3 import SimulationEngine; print('phase3 OK')"
python -c "from src.phase4 import generate_report_with_llm; print('phase4 OK')"
python -c "from profiling.prompts import build_generator_prompts, build_validator_prompts; print('profiling OK')"
python -c "from scripts.probes.p1a_prompt_probe import main; print('p1a probe OK')"
python -c "from scripts.probes.p1g_prompt_probe import main; print('p1g probe OK')"

# attempt-03: 删除后再次全量 src package 编译
python -m py_compile src/phase1/__init__.py src/phase1/extraction.py src/phase1/prompts.py
python -m py_compile src/phase2/__init__.py src/phase2/topology_builder.py
python -m py_compile src/phase3/__init__.py src/phase3/tick_simulation.py
python -m py_compile src/phase4/__init__.py src/phase4/report_agent.py
```

### 可选

```bash
# 软回归（不作为 gate，但建议跑）
python main.py seeds/test7.txt
```

### 关于 `test7` 的建议

`test1` 作为硬 gate（必须通过）；`test7` 作为软回归（跑一次看输出，不通过不作为阻塞条件——因为可能需要 LLM API 调用且解析可能出现边缘情况，需要人工判断）。

---

## 7. Documentation Sync Review

### 7.1 计划内文件（必须同步）

| document | required_change | mandatory_or_optional |
|---|---|---|
| `src/__init__.py:8-11` | docstring 中的旧 flat 文件列表改为 package 路径引用 | **mandatory** |
| `src/__init__.py:14` | `__version__` 从 `"1.1.10"` 升至 `"1.2.5.1"` | **mandatory** |
| `README.md:76-80` | 树形结构更新为 package 结构 | **mandatory** |
| `docs/iterations/CHANGELOG.md:617-619` | 勘误：三行"删除"声明加上删除线 + `[实际执行: v1.2.5.1]` 注释 | **mandatory** |
| `docs/iterations/CHANGELOG.md:88-89` | 勘误注释：标注 README/__init__.py 声明与实际不符 | **mandatory** |
| `docs/iterations/TASK_LOG.md` | 记录 v1.2.5.1 任务、attempt、acceptance | **mandatory** |

### 7.2 计划外文件（必须补入）

| document | required_change | mandatory_or_optional |
|---|---|---|
| `docs/dev_spec.md:235,290,329,401` | 模块路径从旧 flat 文件名改为新 package 路径 | **mandatory** — 这是权威技术文档，删除 shim 后路径引用会变成事实错误 |

### 7.3 建议同步（低优先级）

| document | required_change | mandatory_or_optional |
|---|---|---|
| `docs/skills/ds_verify.md:163` | 验证命令示例从 `from src.phase1_entity_extraction import ...` 改为 `from src.phase1 import ...` | optional — 示例性质，但 copy-paste 会失败 |
| `docs/skills/workflow_core.md:418` | 同上 | optional |
| `src/phase1/extraction.py:22` | 注释 `"legacy root-level import path is preserved by src/phase1_entity_extraction.py"` 已过时 | optional — 仅注释，不影响运行 |
| `src/phase1/prompts.py:3` | 注释 `"Moved from src.phase1_entity_extraction without content changes"` 已过时 | optional |
| `docs/_archive/legacy/README.md` | 新建，说明归档文件的来源、版本、状态 | optional but recommended |

### 7.4 不得修改的文件

- `docs/iterations/v1.2.5-*.md`（v1.2.5 closeout 历史记录）
- 所有 `docs/iterations/v1.1.*.md`（历史迭代记录）
- `docs/debug/`（历史诊断记录）
- `docs/superpowers/`（历史设计文档）
- `docs/contracts/`（历史合同文档）
- `profiling/output/`（历史 profiling 档案）

---

## 8. Risk Register

| risk | severity | mitigation |
|---|---|---|
| `_normalize_unescaped_quotes_inside_string_values` 未导出导致 test 失败 | **HIGH** | attempt-02 第一步必须将此符号加入 `src/phase1/__init__.py` |
| attempt-02 和 attempt-03 合并执行导致回滚困难 | **HIGH** | **强制串行**：02 全部验证通过 → commit → 才能进入 03 |
| Phase1 shim 删除后 profiling pipeline 中断 | **MEDIUM** | `profiling/prompts.py` 迁移在 attempt-02 中完成且验证通过 |
| CHANGELOG 虚假声明未修正 | **LOW** | attempt-04 中强制勘误（删除线 + 注释，不是删除原条目） |
| `test_legacy_shim_imports.py` 保留导致双份冗余测试 | **LOW** | attempt-02 中删除，`ANALYZER_SYSTEM_PROMPT` 断言并入 `test_phase_package_imports.py` |
| `dev_spec.md` 未同步导致技术文档路径失实 | **MEDIUM** | 补入 attempt-04 文件列表 |
| 外部手动脚本引用旧 import 路径 | **LOW** | 无 CI/CD 配置文件，无 `importlib` 动态导入。owner 需确认无个人脚本依赖旧路径 |
| `src/__version__` 停留在 `1.1.10` | **LOW** | attempt-04 中升至 `1.2.5.1` |

---

## 9. Required Plan Patches Before Codex

以下 **6 处补丁**必须在交给 Codex 执行前修正计划：

### Patch 1: 版本命名修正
- **当前**：`v1.2.5.1 - Legacy Source Cleanup & Shim Retirement Preparation`
- **改为**：`v1.2.5.1 - Source Tree Governance Completion`
- **理由**：owner 已确认这不是独立清理，而是 v1.2.5 治理闭环补完。

### Patch 2: `test_legacy_shim_imports.py` 处置
- **当前计划**：迁移其 import 行（#4-7）
- **改为**：在 attempt-02 中**删除此文件**，将唯一的独特断言（`ANALYZER_SYSTEM_PROMPT` 字符串验证）作为一行新增并入 `test_phase_package_imports.py`
- **理由**：迁移后与 `test_phase_package_imports.py` 80% 重叠，保留无意义。

### Patch 3: attempt-04 文档清单扩充
- **当前**：5 个文件
- **改为**：8 个文件（新增 `docs/dev_spec.md`、`docs/_archive/legacy/README.md`、`src/__init__.py` 的 `__version__`）
- **理由**：dev_spec.md 是权威技术文档，删除 shim 后路径引用会变成事实错误。`__version__` 停留在 `1.1.10` 已经过时 15+ 个版本。

### Patch 4: CHANGELOG 勘误方式
- **当前计划**：未具体说明
- **改为**：用**删除线 + `[实际执行: v1.2.5.1]` 注释**标注 lines 617-619 的三条虚假声明。**不删除原条目**（保留历史记录）。
- **理由**：三个文件当时确实未被删除。删除条目会破坏历史完整性。

### Patch 5: 验证命令补充
- **当前计划**：缺少 `py_compile` 对 src package 文件的检查
- **改为**：attempt-02 和 attempt-03 中各追加 `python -m py_compile src/phase{1,2,3,4}/__init__.py` 等命令
- **理由**：`main.py` 使用 lazy import（函数内 `from src.phase1 import ...`），`py_compile main.py` 不会解析这些 import。必须直接编译 package 文件才能发现导入错误。

### Patch 6: 过时注释清理
- **当前计划**：未涉及
- **改为**：在 attempt-04 中同步更新 `src/phase1/extraction.py:22` 和 `src/phase1/prompts.py:3` 两条过时注释
- **理由**：删除 shim 后这两条注释会变成事实错误（声称 shim 仍存在）。两行改动，零风险。

---

## 10. Final DS Recommendation

**可以交给 Codex 执行**——前提是上述 6 处补丁先落到计划文档里。计划本身方向正确，风险集中在 Phase1 shim 的一个 export gap 上，关闭后全部降为 LOW。四个 attempt 必须严格串行（01→验证→02→验证→commit→03→验证→04），Codex 不得合并 attempt-02 和 attempt-03 为一次提交。

**执行不应该复杂化**——这不是一次大版本，而是 v1.2.5 的补完。整个 v1.2.5.1 的实质工作量是：mv 3 个文件 + 改 10 行 import + 补 1 个导出 + rm 4 个 shim + 更新 8 个文档文件。总共 ~30 行改动。不要把它设计成一个需要复杂编排的任务。

**Schema Split 继续冻结**——v1.2.5.1 完成后 `src/` 目录进入干净状态（每个 Phase 一个 package，无 flat 残留），这恰好是 v1.2.6 Schema Split 的理想起点。但那是下一个版本的事。本轮不碰 schemas.py、不改 prompt、不改业务逻辑。
