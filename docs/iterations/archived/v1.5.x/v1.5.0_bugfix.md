# v1.5.0-bugfix — 热修复版本

> 基于 v1.5.0c。无新功能。纯修 bug。

---

## 任务日志

| 时间 | Bug | 状态 |
|------|-----|------|
| 2026-06-26 | 01-seed file 模式保存后 textarea 不显示内容 | ✅ 已修复 |

## 修复清单

| # | 现象 | 根因 | 修复 | 状态 |
|---|------|------|------|------|
| 1 | 切到 file 模式 textarea 显示不会被发送的旧文本 | seedText 未清空 | `useLocalTest8()` 清空 + `watch(source)` 自动清 | ✅ |
| 2 | 保存成功后 textarea 不显示种子内容 | 后端不返回文件内容 | `POST /api/seed` 返回 `content`，store 用 `result.content` 回填 seedText | ✅ |
| 3 | 无 save 状态提示 | 已有 toast（3s） | 延长到 4s | ✅ |
| 4 | file 模式启动推演时传空 seed_text | `startRun` 条件分支清空了 seed_text | 始终传 `seedInput.seedText`（textarea 内容） | ✅ |
| 5 | 默认 textarea 有硬编码示例文本 | 初始 seedText 写死了 | 改为 `''`，用户自己输入或点示例 | ✅ |
| 6 | 文件模式没有主动"读取"交互按钮 | 只有保存按钮，保存才触发文件读取 | 新增 `loadFile()` + `读取内容` 按钮 | ✅ |
