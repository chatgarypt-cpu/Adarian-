# 迭代记录：v1.2.3 - Phase 1 Output Contract Freeze

## 📍 版本信息

- **版本号**：v1.2.3
- **迭代标题**：Phase 1 Output Contract Freeze
- **任务 ID**：task-phase1-output-contract-freeze-r0
- **Review ID**：review-phase1-output-contract-freeze-01 / review-phase1-output-contract-freeze-02
- **Attempt ID**：attempt-v1.2.3-r0-doc-remediation-01
- **基于版本 / Commit**：`b13dd57 chore: isolate generated runtime artifacts`
- **迭代日期**：2026-05-01
- **状态**：🟡 closeout ready / pass_with_known_issues
- **任务性质**：documentation-only / contract freeze / R0 / review findings remediation
- **DS 审计结论**：PASS_WITH_FINDINGS
- **R0**：GO
- **R1**：HOLD
- **是否允许 breaking change**：否
- **是否涉及架构调整**：否，本轮只冻结契约，不实现治理层

---

## 🎯 本次迭代目标

Phase 1 后续计划从当前链路：

```text
LLM generation
  → JSON parse
  → validator
  → retry
  → entities_and_relations.json
```

逐步升级为：

```text
Parser
  → Compiler
  → Validator
  → ValidationReport
  → Repair Loop
  → Diff Guard
  → Canonical Phase 1 Output
```

当前不能直接进入 Parser / Compiler / Validator / Repair Loop 实现。本轮唯一目标是冻结 Phase 1 当前对 Phase 2 / Phase 3 / Phase 4 / main.py 的真实输出契约，为后续 R1 Parser + Compiler + Validator Skeleton 提供准入依据。

---

## 🧭 审计前提

根据 `audit/phase1工程改造_审计结论_2026-05-01.md`，当前审计结论为 `GO_WITH_ADJUSTMENTS`。

- 可以进入 R0 文档工作。
- 不允许进入 R1 实现。
- 必须先冻结 Phase 1 当前真实输出契约。

根据 `audit/v1.2.3-phase1-output-contract-freeze_审计结论_2026-05-01.md`，DS 审计结论为 `PASS_WITH_FINDINGS`。

- R0：GO。
- R1：HOLD。
- DS review findings 属于 v1.2.3 内部 review findings，不新开 v1.2.4。
- 本轮已在 v1.2.3 内完成文档 remediation。

repo hygiene 第一阶段已完成：

- pre-hygiene checkpoint：`f9fae60 checkpoint: pre hygiene dirty tree before phase1 r0`
- hygiene commit：`b13dd57 chore: isolate generated runtime artifacts`
- `outputs/` 与 `profiling/output/` 已退出 Git 跟踪
- 运行证据改为 `audit/evidence/` 下轻量证据记录

---

## ⚠️ 当前关键发现

1. 当前 Phase 1 主模块仍是：

```text
src/phase1_entity_extraction.py
```

2. 当前不存在：

```text
src/phase1/orchestrator.py
src/phase1/rules_engine.py
```

3. `src/phase1_entity_extraction.py` 文件头声称：

```text
v1.1.14+ 已迁移到 src/phase1/
```

但当前仓库中 `src/phase1/` 不存在。该问题属于文档漂移证据。本轮只记录，不修复。

4. DS 审计发现 `TASK_LOG.md` / `CHANGELOG.md` 中存在关于 `src/phase1/orchestrator.py`、`src/phase1/rules_engine.py`、`src/phase1/__init__.py` 的历史漂移记录。本轮已追加标注，保留历史，不作为当前源码事实依据。

5. DS 审计发现 contract 需要补强 `EntityExtractionOutput` vs legacy `Archetype` 双路径差异、`OpinionSpreader` @property 保护、`group_distribution_strategy` ghost field、`simulation_card.py` persona getattr 容错边界，以及 `C` 属性当前无明确下游消费的优先级说明。本轮已补入 contract 文档。

---

## 🎯 本轮解决目标

1. 明确当前 `EntityExtractionOutput` 顶层字段。
2. 明确 `event_entities[]` 字段。
3. 明确 `opinion_spreaders[]` 字段。
4. 明确 `relations[]` 字段。
5. 审计 Phase 2 真实字段依赖。
6. 审计 Phase 3 真实字段依赖。
7. 审计 Phase 4 真实字段依赖。
8. 审计 `main.py` 的 Phase 1 输出传递方式。
9. 字段分级为 required / optional / legacy / candidate_intermediate / forbidden。
10. 明确 R1 Parser-Compiler-Validator Skeleton 的准入条件。

---

## 🚫 本轮明确不做

- 不修改任何业务代码
- 不新增 Parser
- 不新增 Compiler
- 不新增 Validator
- 不新增 Repair Loop
- 不新增 YAML 支持
- 不接外部检索
- 不修改 Phase 2
- 不修改 Phase 3
- 不修改 Phase 4
- 不修改 `main.py`
- 不修改 `schemas.py`
- 不创建 `src/phase1/` 目录
- 不顺手修复 `src/phase1_entity_extraction.py` 文件头
- 不更新 `TASK_LOG.md` / `CHANGELOG.md`，除非用户明确授权

---

## 🔄 当前数据流说明

当前主链并不是通过重新读取 `entities_and_relations.json` 串联 Phase 2 / Phase 3 / Phase 4。

当前真实数据流是：

```text
run_phase1()
  → 返回 extraction_output: EntityExtractionOutput

run_phase2(extraction_output)
  → 直接消费内存对象

run_phase3(extraction_output, phase2_output, seed_text)
  → 继续消费内存对象

run_phase4(extraction_output, phase2_output, tick_logs, x_t_sequence)
  → 继续消费内存对象
```

结论：

- `EntityExtractionOutput` 是当前 Phase 1 对下游的 canonical object。
- `entities_and_relations.json` 是运行产物与审计证据，不是主链唯一数据通道。

---

## 📦 本轮产物

本轮新增文档：

- `docs/iterations/v1.2.3-phase1-output-contract-freeze.md`
- `docs/contracts/phase1-output-contract-freeze-v1.2.3.md`

---

## 🔗 Contract Reference

- 正式契约冻结产物：`docs/contracts/phase1-output-contract-freeze-v1.2.3.md`
- 后续 R1 Parser / Compiler / Validator Skeleton 必须以该 contract 为准入依据。

---

## ✅ 验收标准

本轮完成后，必须能够明确回答：

1. Phase 1 当前输出字段全集是什么？
2. Phase 2 实际依赖哪些字段？
3. Phase 3 实际依赖哪些字段？
4. Phase 4 实际依赖哪些字段？
5. `main.py` 如何传递 Phase 1 输出？
6. 哪些字段是 required？
7. 哪些字段是 optional？
8. 哪些字段是 legacy / derived？
9. 哪些字段未来可迁移到 intermediate object？
10. 哪些字段 R1 前禁止改变？
11. 当前是否允许进入 R1？

---

## 🔐 R1 准入条件

只有满足以下条件，才允许进入 R1：Parser + Compiler + Validator Skeleton。

1. `docs/contracts/phase1-output-contract-freeze-v1.2.3.md` 已完成。
2. required / optional / legacy / candidate_intermediate / forbidden 字段已明确。
3. Phase 2 / Phase 3 / Phase 4 / `main.py` 的真实字段依赖已明确。
4. 已明确当前 `src/phase1/` 目录不存在。
5. R1 不得按已拆分架构设计接入点。
6. 已明确 `C` / `stance_score` / `confirmation_bias_level` 是派生属性。
7. R1 不得把派生属性当成 LLM 必填输出字段。
8. persona 字段不得删除或改名。
9. `EntityExtractionOutput` 仍保持对下游的 canonical object 地位。
10. `EntityExtractionOutput` 与 legacy `Archetype` 的双路径差异已明确。
11. `OpinionSpreader.stance_score` / `confirmation_bias_level` @property 保护已明确。
12. `group_distribution_strategy` ghost field 已明确。
13. `simulation_card.py` persona getattr 容错边界已明确。
14. `C` 当前无明确下游消费，保护优先级低于 `stance_score` / `confirmation_bias_level`。
15. `TASK_LOG.md` / `CHANGELOG.md` 中 `src/phase1/` 相关历史漂移记录已标注。
16. 本文档与契约冻结文档经用户或内部审计 Agent 确认。

---

## 🧪 验证方式

本轮是 documentation-only，不运行 E2E，不做代码编译。

最低验证：

1. `git diff` 仅包含 Markdown 文档变更。
2. 不存在 `src/` 代码修改。
3. 不存在 `main.py` 修改。
4. 不存在 `schemas.py` 修改。
5. `TASK_LOG.md` / `CHANGELOG.md` 仅包含用户明确授权的 v1.2.3 日志更新与历史漂移标注。
6. 不存在 `src/phase1/` 目录创建。
7. 未进入 R1。

---

## 📋 Closeout 待填区

- **actual_changed_files**：
  - `docs/contracts/phase1-output-contract-freeze-v1.2.3.md`
  - `docs/iterations/v1.2.3-phase1-output-contract-freeze.md`
  - `docs/iterations/TASK_LOG.md`
  - `docs/iterations/CHANGELOG.md`
- **validation_result**：
  - documentation-only validation
  - `git diff --stat` 已执行，见本轮交付输出
  - `git status --short` 已执行，见本轮交付输出
  - 预期仅包含 Markdown 文档变更
  - 未运行 E2E；未编译业务代码
- **acceptance_result**：pass_with_known_issues
- **carry_over**：
  - DS 建议的 `main.py` 编排函数类型标注未执行；本轮明确禁止修改 `main.py`。
  - `src/phase1_entity_extraction.py` 文件头漂移未修复；本轮明确禁止修改 `src/`。
  - R1 进入前必须确认 contract hardening 已完成，并不得基于 `src/phase1/` 历史漂移记录设计接入点。
- **final_status**：closeout ready / R0 GO / R1 HOLD

---

## 当前结论

- DS 审计结论：PASS_WITH_FINDINGS
- Review findings：已在 v1.2.3 内完成 documentation remediation
- R0：GO
- R1：HOLD
- v1.2.3：pass_with_known_issues，可进入 closeout
- 下一步：用户完成 git 检查并提交
