# 迭代记录：v1.2.9 - Phase 4 Report Agent Decoupling R0

## 1. Version Info

- **版本号**：v1.2.9
- **版本名称**：Phase 4 Report Agent Decoupling R0
- **基于版本**：v1.2.8.1.1 - Inflection Output Guard / Normalizer Prefix Guard / Concurrent Run Dir Hotfix
- **当前阶段**：closed
- **状态**：closed / pass_with_known_issues
- **task_id**：task-v1.2.9-phase4-report-agent-decoupling-r0
- **audit_id**：audit-v1.2.9-01
- **attempt_id**：
  - attempt-v1.2.9-01
  - attempt-v1.2.9-02
  - attempt-v1.2.9-closeout
- **acceptance_id**：accept-v1.2.9-01
- **Gate**：PASS_WITH_KNOWN_ISSUES
- **closeout_status**：closed
- **blocks_next_version**：no
- **Git Commit / Tag**：`待填写`
- **当前基准提交**：`0c4f2c2 fix: guard inflection output and group concurrent runs`

---

## 2. Control Agent Decision

### Gate

- [ ] GO
- [x] CONDITIONAL_GO
- [ ] HOLD
- [ ] FAIL

### 决策理由

```text
v1.2.8.1 已 closeout，状态为 PASS_WITH_KNOWN_ISSUES / CLOSED，不阻塞下一版本。

DS Agent Team 对 Phase 4 Report Stack 的完整审计已确认：
1. src/phase4/report_agent.py 已约 1675 行；
2. 文件内混合风险计算、LLM 叙事生成、Markdown normalizer、标题处理、I/O、artifact 保存等多类职责；
3. DS 建议优先拆分 report_narrative.py / report_title.py / report_normalizer.py；
4. 当前适合做 Phase 4 解耦 R0，为后续 Risk Type Signal Source Patch、Inflection Point Framework、结构化产物治理降低耦合成本。
```

### 执行前条件

```text
1. 必须先完成 DS Agent Team pre-audit。
2. DS pre-audit 不得重新设计版本边界，只审查本迭代文档的可执行性、风险和遗漏。
3. 当前 audit/ dirty tree 不作为 blocker，但 Codex 不得把 audit/ 现有变动混入 v1.2.9 diff。
4. Codex 执行前必须记录：
   - git status --short
   - git log --oneline -5
   - find src/phase4 -maxdepth 1 -type f | sort
5. 若 Codex 发现 src/phase4 真实结构与本文档冲突，必须停止并回传，不得自行扩大 scope。
```

---

## 3. Goal & Boundary

### 3.1 本版本主目标

```text
在不改变 Phase 4 对外 contract、风险算法语义、prompt 语义和报告产物语义的前提下，
将 report_agent.py 中的叙事生成、标题处理、Markdown normalizer 拆分为独立模块，
完成 Phase 4 Report Agent 解耦 R0。
```

### 3.2 本版本要解决的问题

```text
1. report_agent.py 过度膨胀，职责过多，后续维护成本过高。
2. Markdown normalizer 与报告生成主链耦合，导致术语修复、code-owned block 注入、禁止语清理难以单独测试。
3. 标题生成与标题规范化逻辑分散在 report_agent.py 中，不利于后续报告产品化。
4. LLM 叙事生成、prompt context 组装与 artifact 保存混合，不利于后续 prompt governance / report contract 扩展。
5. DS 已确认的两个 normalizer 小 bug 需要在拆分过程中最小修复：
   - “模拟模拟极化指数”重复前缀；
   - code-owned / prompt 指令中文转述泄漏。
```

### 3.3 本版本不解决的问题

```text
1. 不做 Risk Type Signal Source Patch。
2. 不重写 select_primary_risk_types() 的信号来源。
3. 不做 Inflection Point Framework。
4. 不新增 single_run_summary.json。
5. 不新增 parallel_world_synthesis.json。
6. 不新增 batch_synthesis_context.json。
7. 不修 main.py run_dir 并发撞名问题。
8. 不重构风险等级阈值。
9. 不标定模拟极化指数。
10. 不修改 Phase 1 / Phase 2 / Phase 3。
11. 不做 Prompt Registry。
12. 不做外部检索 / RAG / MCP / 真实态势感知接入。
```

### 3.4 禁止变化

本轮明确禁止：

```text
1. 不改 schema 语义。
2. 不改 prompt 语义。
3. 不改风险算法语义。
4. 不改 Phase 1-3 主链。
5. 不改 speaker selector 策略。
6. 不改 RuntimeLogger 职责。
7. 不改 final_report.json 对外字段契约。
8. 不改变 final_report.md 五章结构。
9. 不新增 runtime artifact contract。
10. 不把 DS finding 自动升级为本轮功能。
11. 不把 audit/ dirty tree 纳入本轮 diff。
```

### 3.5 v1.2.9 Closeout Hygiene Cleanup Scope

```text
v1.2.9 不拆分独立 v1.2.9.1 hygiene patch。

Dependency / Import Hygiene 作为 v1.2.9 closeout cleanup 纳入本版本。

本 cleanup 依据：
  DS Agent Team Dependency Hygiene Audit
  audit/phase4大版本改造/DS_Agent_Team_Dependency_Hygiene_Audit_v1.2.9_2026-05-15.md
  audit_id: DS-AGENT-TEAM-HYGIENE-2026-05-15-001

本 cleanup 只允许执行 DS 标记为 SAFE_TO_REMOVE 的 A 类 6 项：
  A1. 移除 report_agent.py 中未使用的 _normalize_report_title_line import。
  A2. 删除 build_entity_distribution()。
  A3. 删除 _trajectory_description()。
  A4. 删除 _stance_summary_lines()。
  A5. 删除 load_tick_logs() 内重复的 TickLog 局部导入。
  A6. 清理 src/phase4/__init__.py 中 4 个无外部消费者的 re-export：
      - build_full_report_context
      - load_tick_logs
      - parse_llm_report_response
      - generate_markdown_report

不得触碰 KEEP_REQUIRED（B 类）项目。
不得触碰 NEEDS_CODEX_VERIFICATION（C 类）项目。
不得触碰 DO_NOT_TOUCH（D 类）项目。
```

---

## 4. Audit Summary & DS Review Scope

### 4.1 DS Pre-Audit Summary

- **是否经过 DS 前置审查**：待执行
- **DS Verdict**：N/A

DS 关键发现来源：

```text
DS Agent Team 会话完整审计导出 — v1.2.8.1 Phase 4 Report Stack
audit_id: audit-v1.2.8.x-phase4-report-stack-snapshot-01
```

已采纳作为 v1.2.9 输入的发现：

```text
1. report_agent.py 职责过载，需要拆分。
2. report_narrative.py / report_title.py / report_normalizer.py 三路拆分方向合理。
3. normalizer 区域存在“模拟模拟极化指数”重复前缀 bug。
4. prompt / code-owned 标签中文转述泄漏可在 normalizer 拆分时同步修复。
```

不采纳进入 v1.2.9 的发现：

```text
1. select_primary_risk_types() 改为 code-owned signal source。
   - 原因：等待产品侧风险分层矩阵，不抢跑。

2. single_run_summary / parallel_world_synthesis / batch_synthesis_context。
   - 原因：属于后续结构化产物治理，不属于解耦 R0。

3. main.py run_dir 并发撞名问题。
   - 状态：已在 v1.2.8.1.1 作为独立 infra hotfix 处理；v1.2.9 不再触碰 main.py。

4. Inflection Point Framework。
   - 原因：L-Level 后续架构版本，不混入本轮。
```

### 4.2 DS Review Scope

DS Agent Team pre-audit 只审查以下内容：

```text
1. Scope Compliance
   - v1.2.9 是否只做 Phase 4 解耦 R0。
   - 是否越界进入风险类型、拐点、结构化产物治理。

2. File Scope Feasibility
   - 允许新增 / 修改文件是否足够。
   - 是否需要补充 __init__.py export。
   - 是否存在遗漏的测试文件。

3. Import Boundary
   - report_agent.py 是否可作为 façade / orchestrator 保持外部 API。
   - 新模块是否只承接拆出的逻辑，不改变语义。

4. Behavior Preservation
   - 风险算法语义是否保持。
   - prompt 语义是否保持。
   - final_report.json / final_report.md 对外契约是否保持。

5. Normalizer Included Fixes
   - “模拟模拟极化指数”是否适合在本轮修复。
   - code-owned / prompt 指令中文泄漏过滤是否适合在本轮修复。

6. Verification Plan
   - targeted tests 是否足够。
   - 是否需要最终 smoke test。
```

### 4.3 DS Must Not Do

DS / 验收 Agent 不负责：

```text
1. 不重新设计版本范围。
2. 不扩大到 report contract / risk taxonomy / inflection framework。
3. 不把建议项自动升级为 blocker。
4. 不要求进入下一版本。
5. 不替 Control Agent 做最终 gate。
6. 不直接修改源码。
```

---

## 5. Target Structure / Artifact Contract

### 5.1 当前结构

```text
src/phase4/
  __init__.py
  report_agent.py
  report_prompts.py
```

### 5.2 目标结构

```text
src/phase4/
  __init__.py
  report_agent.py
  report_prompts.py
  report_narrative.py
  report_title.py
  report_normalizer.py
```

### 5.3 模块职责

#### report_agent.py

```text
定位：Phase 4 façade / orchestrator。

保留：
1. 对外主入口函数。
2. artifact 保存与主链协调。
3. 对 Phase4Output 的最终组装。
4. 与 main.py 现有调用兼容。
5. 风险计算逻辑可暂留，除非 DS pre-audit 明确建议局部抽出但不得改语义。

不得：
1. 不再承载大段 Markdown normalizer pipeline。
2. 不再承载标题处理细节。
3. 不再承载 LLM 叙事生成细节。
```

#### report_narrative.py

```text
定位：LLM 叙事生成与 prompt context 组装模块。

承接：
1. generate_report_with_llm() 或等价叙事生成函数。
2. LLM 调用编排。
3. 报告 prompt 输入组装相关辅助函数。
4. 与 report_prompts.py 的 prompt 常量引用。

禁止：
1. 不改变 prompt 文本语义。
2. 不让 LLM 决定 risk_level。
3. 不让 LLM 重新计算指标。
4. 不新增外部检索。
```

#### report_title.py

```text
定位：标题生成与标题规范化模块。

承接：
1. 标题生成函数。
2. 标题清洗 / 规范化。
3. 标题 locale / 中文表达适配。
4. 标题 fallback 逻辑。

禁止：
1. 不改变报告正文结构。
2. 不改变 report_type / audience_mode 逻辑。
```

#### report_normalizer.py

```text
定位：Markdown normalizer pipeline。

承接：
1. _normalize_saved_markdown()。
2. _replace_report_metric_terms()。
3. _replace_risk_section_with_code_owned()。
4. _ensure_metric_explanation_prefill()。
5. _remove_metric_explanation_sections()。
6. forbidden phrases / code-owned label 清理。
7. 五章结构与术语后处理相关逻辑。

允许 included fixes：
1. 修复“模拟模拟极化指数”重复前缀。
2. 扩展 code-owned / prompt 指令泄漏过滤词表，覆盖中文转述。

禁止：
1. 不新增新的指标解释语义。
2. 不改变风险章节 code-owned 内容含义。
3. 不改变 final_report.md 五章结构。
```

### 5.4 兼容策略

```text
1. report_agent.py 继续作为 Phase 4 主入口。
2. main.py 不应因本次拆分改变调用语义。
3. 旧测试若 import report_agent.py 中函数，原则上应继续兼容。
4. 如必须迁移内部 helper，report_agent.py 可保留 re-export / wrapper。
5. 不删除 report_prompts.py。
6. 不新增 docs/prompts/phase4/。
```

### 5.5 运行产物变化

```text
本版本不改变运行产物契约。
```

不得新增：

```text
single_run_summary.json
parallel_world_synthesis.json
batch_synthesis_context.json
```

### 5.6 Whitebox / Observability 产物变化

```text
本版本不改变 whitebox 产物契约。
```

---

## 6. File Change Scope

### 6.1 允许新增

```text
- src/phase4/report_narrative.py
- src/phase4/report_title.py
- src/phase4/report_normalizer.py
- tests/test_phase4_report_agent_decoupling.py
- tests/test_phase4_report_normalizer.py
```

### 6.2 允许修改

```text
- src/phase4/report_agent.py
  - 拆出 narrative / title / normalizer 逻辑；
  - 保留 façade / orchestrator；
  - 调整 import；
  - 不改变对外主链语义。

- src/phase4/__init__.py
  - 如确有必要，补充新模块 export；
  - 不改变现有对外导出语义。

- docs/iterations/v1.2.9-Phase-4-Report-Agent-Decoupling-R0.md
  - 记录 DS pre-audit、attempt、acceptance 与 closeout。

- docs/iterations/TASK_LOG.md
  - 记录 v1.2.9 执行与验收。

- docs/iterations/CHANGELOG.md
  - 记录 v1.2.9 变更。

- docs/dev_spec.md
  - 如确有必要，同步 Phase 4 模块结构；
  - 不修改业务 contract。
```

### 6.3 禁止修改

```text
- main.py
- src/phase1/
- src/phase2/
- src/phase3/
- src/schemas/
- src/whitebox/
- src/utils/runtime_logger.py
- seeds/
- outputs/
- smoke_logs/
- audit/
- README.md
- docs/workflow_core.md
- docs/skills/
- 产品侧 v0.3 文档原件
```

### 6.4 必须保持不变

```text
- Phase 4 对外调用入口
- final_report.json 字段契约
- final_report.md 五章结构
- risk_level 四档枚举
- risk_level_label 映射
- primary_risk_types 现有逻辑
- METRIC_EXPLANATION_PREFILL 语义
- REPORT_SYSTEM_PROMPT 语义
- RuntimeLogger 职责
- Whitebox artifact contract
```

### 6.5 删除文件

```text
无。
```

---

## 7. Execution Attempts

### 7.1 Execution Mode

- [ ] Single Attempt
- [x] Two-Attempt Workflow
- [ ] Multi-Attempt Workflow

选择理由：

```text
本轮是 L-Level 架构拆分，但不改变业务 contract。
为降低风险，分为两个 attempt：

attempt-01：拆 normalizer + included fixes。
attempt-02：拆 narrative / title + façade 收口。

这样可以先隔离 Markdown 后处理链，再移动 LLM 叙事与标题逻辑，避免一次性搬迁过大。
```

---

### 7.2 attempt-v1.2.9-01：Report Normalizer Extraction

目标：

```text
1. 新增 src/phase4/report_normalizer.py。
2. 从 report_agent.py 中迁移 Markdown normalizer pipeline。
3. 保持 normalize 后的 final_report.md 语义不变。
4. 修复“模拟模拟极化指数”重复前缀。
5. 扩展 code-owned / prompt 指令中文转述泄漏过滤。
6. 新增 targeted tests。
```

允许修改：

```text
- src/phase4/report_agent.py
- src/phase4/report_normalizer.py
- src/phase4/__init__.py（如必要）
- tests/test_phase4_report_normalizer.py
```

禁止修改：

```text
- main.py
- src/phase1/
- src/phase2/
- src/phase3/
- src/schemas/
- src/whitebox/
- src/phase4/report_prompts.py
- audit/
- docs/iterations/TASK_LOG.md
- docs/iterations/CHANGELOG.md
```

执行要求：

```text
1. report_normalizer.py 必须承接原 normalizer 逻辑。
2. report_agent.py 调用 report_normalizer.py，不重复保留大段 normalizer 实现。
3. 不改变 METRIC_EXPLANATION_PREFILL 文本含义。
4. 不改变风险章节 code-owned 注入语义。
5. “模拟极化指数”不得被替换成“模拟模拟极化指数”。
6. code-owned / prompt 指令中文转述不得进入 final_report.md。
```

验收命令：

```bash
.venv/bin/python -m py_compile src/phase4/report_agent.py src/phase4/report_normalizer.py
.venv/bin/python -m pytest tests/test_phase4_report_normalizer.py -v
.venv/bin/python -m pytest tests/test_report_markdown_grounding.py tests/test_phase4_markdown_metric_grounding.py -v
```

通过条件：

```text
1. py_compile 通过。
2. 新增 normalizer tests 通过。
3. 既有 markdown grounding tests 通过。
4. 不触碰 forbidden files。
5. 不改变 report_prompts.py。
```

---

### 7.3 attempt-v1.2.9-02：Narrative / Title Extraction + Facade Preservation

目标：

```text
1. 新增 src/phase4/report_narrative.py。
2. 新增 src/phase4/report_title.py。
3. 从 report_agent.py 中迁移 LLM 叙事生成与标题处理逻辑。
4. report_agent.py 保持 Phase 4 façade / orchestrator 职责。
5. 保持对 main.py 和既有测试的兼容。
6. 新增 decoupling integrity tests。
```

允许修改：

```text
- src/phase4/report_agent.py
- src/phase4/report_narrative.py
- src/phase4/report_title.py
- src/phase4/__init__.py（如必要）
- tests/test_phase4_report_agent_decoupling.py
```

禁止修改：

```text
- main.py
- src/phase1/
- src/phase2/
- src/phase3/
- src/schemas/
- src/whitebox/
- src/phase4/report_prompts.py
- audit/
```

执行要求：

```text
1. report_agent.py 对外入口保持不变。
2. LLM prompt 语义保持不变。
3. LLM 不新增风险等级判断职责。
4. 标题 fallback 逻辑保持。
5. 若旧测试 import report_agent.py 内部 helper，应优先保留 wrapper / re-export。
6. 不新增 Prompt Registry。
```

验收命令：

```bash
.venv/bin/python -m py_compile src/phase4/report_agent.py src/phase4/report_narrative.py src/phase4/report_title.py src/phase4/report_normalizer.py
.venv/bin/python -m pytest tests/test_phase4_report_agent_decoupling.py -v
.venv/bin/python -m pytest tests/test_report_product_contract.py tests/test_report_markdown_grounding.py tests/test_phase4_markdown_metric_grounding.py tests/test_risk_assessment_directionality.py -v
```

通过条件：

```text
1. py_compile 通过。
2. decoupling integrity tests 通过。
3. 既有 Phase 4 targeted tests 全部通过。
4. report_agent.py 保留对外主入口。
5. 不触碰 forbidden files。
```

---

### 7.4 attempt-v1.2.9-closeout：Docs + Smoke + Hygiene Cleanup + Acceptance Evidence

目标：

```text
1. 根据 DS Verify 发现修补低风险问题。
2. 执行 DS Dependency Hygiene Audit 标记为 SAFE_TO_REMOVE 的 A 类 6 项 cleanup。
3. 同步 TASK_LOG / CHANGELOG / dev_spec。
4. 跑最终 smoke test。
5. 生成 closeout evidence。
```

允许修改：

```text
- docs/iterations/v1.2.9-Phase-4-Report-Agent-Decoupling-R0.md
- docs/iterations/TASK_LOG.md
- docs/iterations/CHANGELOG.md
- docs/dev_spec.md（如确有必要）
- src/phase4/report_agent.py（仅限 SAFE_TO_REMOVE A 类 6 项 cleanup）
- src/phase4/__init__.py（仅限 SAFE_TO_REMOVE A6 项 re-export 清理）
```

允许执行的 hygiene cleanup（仅限 DS SAFE_TO_REMOVE A 类 6 项）：

```text
A1. 移除 report_agent.py 中未使用的 _normalize_report_title_line import。
    - 文件：src/phase4/report_agent.py:51
    - 证据：函数体内零引用，无测试通过 report_agent._normalize_report_title_line 访问。

A2. 删除 build_entity_distribution() 死函数。
    - 文件：src/phase4/report_agent.py（约 line 346-364）
    - 证据：全代码库唯一出现处即该定义，零调用者。

A3. 删除 _trajectory_description() 死函数。
    - 文件：src/phase4/report_agent.py（约 line 723）
    - 证据：全代码库唯一出现处即该定义，零调用者。

A4. 删除 _stance_summary_lines() 死函数。
    - 文件：src/phase4/report_agent.py（约 line 865）
    - 证据：全代码库唯一出现处即该定义，零调用者。

A5. 删除 load_tick_logs() 内重复的 TickLog 局部导入。
    - 文件：src/phase4/report_agent.py:1100
    - 证据：TickLog 已在文件顶部导入，函数内重复导入冗余。

A6. 清理 src/phase4/__init__.py 中 4 个无外部消费者的 re-export：
    - build_full_report_context
    - load_tick_logs
    - parse_llm_report_response
    - generate_markdown_report
    - 证据：main.py 仅导入 generate_report_with_llm/save_report/save_markdown_report。
```

允许运行：

```bash
.venv/bin/python -m compileall src
.venv/bin/python -m pytest tests/ -v
.venv/bin/python main.py seeds/test8.txt
```

禁止：

```text
1. closeout patch 不得继续大规模改源码。
2. 不得引入新功能。
3. 不得修 main.py run_dir 并发 bug。
4. 不得处理 audit/ dirty tree。
5. 不得清理 KEEP_REQUIRED（B 类）项目。
6. 不得清理 NEEDS_CODEX_VERIFICATION（C 类）项目。
7. 不得触碰 DO_NOT_TOUCH（D 类）项目。
8. 不得修改 prompt 语义。
9. 不得修改 risk algorithm。
10. 不得修改 schema。
11. 不得修改 main.py。
12. 不得修改 whitebox。
13. 不得修改 report_prompts.py。
14. 不得修改 config.py。
15. 不得修改 src/llm_client.py。
```

通过条件：

```text
1. compileall 通过。
2. tests/ 全量通过。
3. test8 smoke 通过。
4. final_report.json / final_report.md / whitebox 产物齐全。
5. final_report.md 不出现“模拟模拟极化指数”。
6. final_report.md 不出现 code-owned / prompt 指令泄漏。
7. TASK_LOG / CHANGELOG / iteration doc 同步。
```

---

### 7.5 Attempt Dependency

```text
- attempt-02 是否依赖 attempt-01 通过：是。
- 是否允许并行：否。
- 是否允许两个 attempt 同时修改 report_agent.py：不允许并行，只能串行。
- 如果 attempt-01 fail，attempt-02 必须停止。
- closeout patch 依赖 attempt-01 / attempt-02 均通过。
```

---

## 8. Verification Plan

### 8.1 静态检查

```bash
.venv/bin/python -m py_compile src/phase4/report_agent.py src/phase4/report_normalizer.py src/phase4/report_narrative.py src/phase4/report_title.py
.venv/bin/python -m compileall src
```

### 8.2 Import / Unit Test

```bash
.venv/bin/python -m pytest tests/test_phase4_report_normalizer.py -v
.venv/bin/python -m pytest tests/test_phase4_report_agent_decoupling.py -v
```

### 8.3 Existing Phase 4 Regression Tests

```bash
.venv/bin/python -m pytest tests/test_report_product_contract.py -v
.venv/bin/python -m pytest tests/test_report_markdown_grounding.py -v
.venv/bin/python -m pytest tests/test_phase4_markdown_metric_grounding.py -v
.venv/bin/python -m pytest tests/test_risk_assessment_directionality.py -v
```

### 8.4 Hygiene Cleanup Verification（v1.2.9 Closeout）

在 closeout hygiene cleanup 执行后，必须额外运行以下验证：

```bash
# 静态编译
.venv/bin/python -m compileall src

# Decoupling / normalizer targeted tests
.venv/bin/python -m pytest tests/test_phase4_report_normalizer.py tests/test_phase4_report_agent_decoupling.py -v

# Contract / markdown grounding regression
.venv/bin/python -m pytest tests/test_report_markdown_grounding.py tests/test_report_product_contract.py -v
```

Hygiene 通过条件：

```text
1. compileall 通过。
2. test_phase4_report_normalizer.py 全部通过。
3. test_phase4_report_agent_decoupling.py 全部通过。
4. test_report_markdown_grounding.py 全部通过。
5. test_report_product_contract.py 全部通过。
6. 未触碰 KEEP_REQUIRED（B 类）项目。
7. 未触碰 NEEDS_CODEX_VERIFICATION（C 类）项目。
8. 未触碰 DO_NOT_TOUCH（D 类）项目。
9. 未触碰 forbidden files。
```

### 8.5 Full Test Suite

```bash
.venv/bin/python -m pytest tests/ -v
```

### 8.6 Smoke Test

```bash
.venv/bin/python main.py seeds/test8.txt
```

是否作为硬门槛：

- [x] 是
- [ ] 否

原因：

```text
本轮虽不改变业务语义，但涉及 Phase 4 主报告生成链路拆分。
最终 closeout 前必须用 test8 smoke 验证 final_report.json / final_report.md / whitebox 仍能完整生成。
```

### 8.7 Artifact Check

必须检查：

```text
1. run_dir 是否生成。
2. run.log 是否生成。
3. timing_summary.json 是否生成。
4. run_meta.json 是否生成。
5. entities_and_relations.json 是否生成。
6. social_graph.json 是否生成。
7. tick_logs.json 是否生成。
8. final_report.json 是否生成。
9. final_report.md 是否生成。
10. whitebox_summary.json 是否生成。
11. final_report.md 不出现”模拟模拟极化指数”。
12. final_report.md 不出现 code-owned / prompt 指令泄漏。
13. final_report.md 五章结构保持。
14. final_report.json risk_level / risk_level_label / primary_risk_types 保持。
```

---

## 9. Acceptance Target & Criteria

### 9.1 Hard Acceptance Target

不满足任一项即 fail / hold：

```text
1. src/phase4/report_normalizer.py 创建并被 report_agent.py 使用。
2. src/phase4/report_narrative.py 创建并被 report_agent.py 使用。
3. src/phase4/report_title.py 创建并被 report_agent.py 使用。
4. report_agent.py 仍保留 Phase 4 对外主入口。
5. prompt 语义不改变。
6. risk algorithm 语义不改变。
7. final_report.json contract 不改变。
8. final_report.md 五章结构不改变。
9. targeted tests 通过。
10. full tests 通过。
11. test8 smoke 通过。
12. 不触碰 forbidden files。
13. audit/ dirty tree 不纳入本轮 diff。
```

### 9.2 Soft Acceptance Target

不满足可记录为 pass_with_known_issues：

```text
1. report_agent.py 行数仍然较长，但职责边界已经拆出。
2. 风险计算逻辑仍留在 report_agent.py。
3. select_primary_risk_types() 仍依赖文本 keyword matching。
4. run_dir 并发撞名问题已由 v1.2.8.1.1 独立 hotfix 处理，v1.2.9 不再触碰 main.py。
5. 模拟极化指数仍是工程 proxy。
6. 模拟关键变化点仍未升级为多信号 framework。
```

### 9.3 Pass

```text
1. 三个新模块完成拆分。
2. report_agent.py 对外兼容。
3. targeted tests / full tests / smoke 全部通过。
4. 两个 included normalizer bug 已修复。
5. 无 forbidden file diff。
6. 文档同步完成。
```

### 9.4 Pass with Known Issues

允许的 known issues：

```text
1. 风险阈值仍是工程初始阈值，待后续多 seed 标定。
2. 模拟极化指数仍是工程 proxy。
3. 模拟关键变化点仍未完整升级为多信号 framework。
4. external_risk_adjustment 仅作为 future hook，未实现、未接入、未进入报告产物。
5. select_primary_risk_types() 仍依赖 risk_assessment 文本 keyword matching。
6. main.py 并发 run_dir 问题已由 v1.2.8.1.1 独立 hotfix 处理；v1.2.9 不修改 main.py。
7. single_run_summary / parallel_world_synthesis / batch_synthesis_context 未实现。
8. risk calculation 仍留在 report_agent.py，待后续 risk engine 解耦。
```

### 9.5 Fail / Hold

出现以下任一情况即 fail / hold：

```text
1. smoke test 失败且无法证明为外部模型 / 网络环境阻塞。
2. 触碰 forbidden files。
3. 改变 final_report.json contract。
4. 改变 final_report.md 五章结构。
5. 改变 risk_level 判断语义。
6. 改变 prompt 语义。
7. 修改 Phase 1-3。
8. 新增 runtime artifact contract。
9. 把 risk type patch / inflection framework / structured synthesis 混入本轮。
10. Codex 把 audit/ dirty tree 混入本轮 diff。
```

---

## 10. Execution Report Requirement

Codex 每个 attempt 完成后必须回传：

```text
1. 当前 git status --short。
2. 当前 git diff --stat。
3. 实际新增文件清单。
4. 实际修改文件清单。
5. 实际删除文件清单。
6. 是否触碰 forbidden files。
7. 每个测试命令与结果。
8. 若测试失败，说明失败原因和是否为环境阻塞。
9. 对 report_agent.py 外部入口兼容性的说明。
10. 对 final_report.json / final_report.md contract preservation 的说明。
11. 是否出现 audit/ dirty tree 混入。
```

closeout attempt 必须额外回传：

```text
1. 最新 run_dir。
2. final_report.json 路径。
3. final_report.md 路径。
4. whitebox_summary.json 路径。
5. risk_level / risk_level_label。
6. primary_risk_types。
7. final_report.md 五章结构检查结果。
8. “模拟模拟极化指数”检查结果。
9. code-owned / prompt 指令泄漏检查结果。
10. TASK_LOG / CHANGELOG / iteration doc 同步结果。
```

---

## 11. DS Agent Team Pre-Audit Prompt

```text
你现在作为 DS Agent Team，执行 v1.2.9 Phase 4 Report Agent Decoupling R0 的前置审计。

硬性要求：
team_mode_used 必须为 true。
必须启动至少 5 个 reviewer agent：
1. Architecture Boundary Reviewer
2. Import / Compatibility Reviewer
3. Phase 4 Behavior Preservation Reviewer
4. Test Plan Reviewer
5. Scope Drift Reviewer

如果无法开启 DS Agent Team / 多 reviewer 并行审查，必须立即停止并说明：
team_mode_used=false
stop_reason=DS Agent Team not available

输入材料：
1. 当前迭代文档：
   docs/iterations/v1.2.9-Phase-4-Report-Agent-Decoupling-R0.md

2. 参考审计材料：
   audit/phase4大版本改造/DS_Agent_Team_Session_Full_Audit_Export_v1.2.8.1_2026-05-15.md

3. 当前源码事实：
   - src/phase4/report_agent.py
   - src/phase4/report_prompts.py
   - src/phase4/__init__.py
   - tests/test_report_product_contract.py
   - tests/test_report_markdown_grounding.py
   - tests/test_phase4_markdown_metric_grounding.py
   - tests/test_risk_assessment_directionality.py

审计目标：
只审查 v1.2.9 迭代文档是否可执行、边界是否清晰、allowed / forbidden files 是否合理、attempt 拆分是否稳妥、测试计划是否足够。

严禁：
1. 不得重写版本目标。
2. 不得扩大到 Risk Type Signal Source Patch。
3. 不得扩大到 Inflection Point Framework。
4. 不得扩大到 single_run_summary / parallel_world_synthesis / batch_synthesis_context。
5. 不得要求修 main.py run_dir 并发 bug。
6. 不得把 audit/ dirty tree 作为 blocker。
7. 不得替 Control Agent 做最终 gate。

请输出中文 Markdown 审计报告文件，不要把完整报告塞进聊天上下文。

报告保存路径：
audit/phase4大版本改造/DS_Agent_Team_Pre_Audit_Report_v1.2.9_2026-05-15.md

聊天回复只输出：
1. team_mode_used
2. reviewer_agents
3. verdict: GO / CONDITIONAL_GO / HOLD / FAIL
4. hard_blockers
5. must_fix_before_codex
6. nice_to_have
7. report_path
```

---

## 12. Codex Execution Prompt Draft

> 注意：此 Prompt 只有在 DS Pre-Audit 完成、Control Agent 采纳或修正文档后才能使用。

```text
你现在作为 Codex，执行 v1.2.9 Phase 4 Report Agent Decoupling R0。

当前版本：
v1.2.9 - Phase 4 Report Agent Decoupling R0

基于版本：
v1.2.8.1 - Risk Assessment Directionality & Metric Explanation Patch

执行依据：
docs/iterations/v1.2.9-Phase-4-Report-Agent-Decoupling-R0.md

硬性原则：
1. 严格按 iteration doc 执行。
2. 不得自行扩大 scope。
3. 不得修改 forbidden files。
4. 不得把 audit/ dirty tree 混入本轮 diff。
5. 若发现源码事实与 iteration doc 冲突，立即停止并回传。
6. 本轮只做 Phase 4 解耦 R0，不做新功能。

执行前先记录：
git status --short
git log --oneline -5
find src/phase4 -maxdepth 1 -type f | sort

当前允许新增：
- src/phase4/report_narrative.py
- src/phase4/report_title.py
- src/phase4/report_normalizer.py
- tests/test_phase4_report_agent_decoupling.py
- tests/test_phase4_report_normalizer.py

当前允许修改：
- src/phase4/report_agent.py
- src/phase4/__init__.py（如必要）
- docs/iterations/v1.2.9-Phase-4-Report-Agent-Decoupling-R0.md
- docs/iterations/TASK_LOG.md
- docs/iterations/CHANGELOG.md
- docs/dev_spec.md（如必要）

禁止修改：
- main.py
- src/phase1/
- src/phase2/
- src/phase3/
- src/schemas/
- src/whitebox/
- src/utils/runtime_logger.py
- src/phase4/report_prompts.py
- seeds/
- outputs/
- smoke_logs/
- audit/
- README.md
- docs/workflow_core.md
- docs/skills/
- 产品侧 v0.3 文档原件

Attempt 01：Report Normalizer Extraction
目标：
1. 新增 src/phase4/report_normalizer.py。
2. 从 report_agent.py 中迁移 Markdown normalizer pipeline。
3. 修复“模拟模拟极化指数”重复前缀。
4. 扩展 code-owned / prompt 指令中文转述泄漏过滤。
5. 新增 tests/test_phase4_report_normalizer.py。

执行后运行：
.venv/bin/python -m py_compile src/phase4/report_agent.py src/phase4/report_normalizer.py
.venv/bin/python -m pytest tests/test_phase4_report_normalizer.py -v
.venv/bin/python -m pytest tests/test_report_markdown_grounding.py tests/test_phase4_markdown_metric_grounding.py -v

Attempt 02：Narrative / Title Extraction + Facade Preservation
前提：
Attempt 01 必须通过，否则停止。

目标：
1. 新增 src/phase4/report_narrative.py。
2. 新增 src/phase4/report_title.py。
3. 从 report_agent.py 中迁移 LLM 叙事生成与标题处理逻辑。
4. report_agent.py 保持 Phase 4 façade / orchestrator。
5. 新增 tests/test_phase4_report_agent_decoupling.py。

执行后运行：
.venv/bin/python -m py_compile src/phase4/report_agent.py src/phase4/report_narrative.py src/phase4/report_title.py src/phase4/report_normalizer.py
.venv/bin/python -m pytest tests/test_phase4_report_agent_decoupling.py -v
.venv/bin/python -m pytest tests/test_report_product_contract.py tests/test_report_markdown_grounding.py tests/test_phase4_markdown_metric_grounding.py tests/test_risk_assessment_directionality.py -v

Closeout 验证：

Closeout 阶段分为两步：

**Step 1 — Hygiene Cleanup（DS SAFE_TO_REMOVE A 类 6 项）：**

本阶段允许执行以下源码修改（仅此 6 项，不得扩大）：

A1. 移除 report_agent.py 中未使用的 _normalize_report_title_line import。
A2. 删除 build_entity_distribution() 死函数。
A3. 删除 _trajectory_description() 死函数。
A4. 删除 _stance_summary_lines() 死函数。
A5. 删除 load_tick_logs() 内重复的 TickLog 局部导入。
A6. 清理 src/phase4/__init__.py 中 4 个无外部消费者的 re-export。

Hygiene cleanup 执行后必须运行：
.venv/bin/python -m compileall src
.venv/bin/python -m pytest tests/test_phase4_report_normalizer.py tests/test_phase4_report_agent_decoupling.py -v
.venv/bin/python -m pytest tests/test_report_markdown_grounding.py tests/test_report_product_contract.py -v

**Step 2 — Full Verification：**

```bash
.venv/bin/python -m compileall src
.venv/bin/python -m pytest tests/ -v
.venv/bin/python main.py seeds/test8.txt
```

交付要求：
1. 回传 git status --short。
2. 回传 git diff --stat。
3. 回传新增 / 修改 / 删除文件清单。
4. 回传每条测试命令及结果。
5. 回传最新 run_dir。
6. 回传 final_report.json / final_report.md / whitebox_summary.json 路径。
7. 明确说明：
   - 是否触碰 forbidden files；
   - 是否改变 prompt 语义；
   - 是否改变 risk algorithm 语义；
   - 是否改变 final_report.json contract；
   - 是否改变 final_report.md 五章结构；
   - 是否混入 audit/ dirty tree。
8. 若 smoke 因模型网络失败，按环境阻塞处理，必须说明 endpoint / model / network 证据，不得直接判定代码失败。

Closeout hygiene cleanup 额外回传：

9. 实际执行的清理项列表（逐项列出 A1-A6，标注是否执行）。
10. 每个清理项对应的 DS evidence 编号（A1-A6）。
11. git diff --stat（仅 hygiene 部分）。
12. 每条 hygiene 验证命令及测试结果。
13. 是否触碰 B 类（KEEP_REQUIRED）项目。
14. 是否触碰 C 类（NEEDS_CODEX_VERIFICATION）项目。
15. 是否触碰 D 类（DO_NOT_TOUCH）项目。
16. 是否触碰 forbidden files（main.py / schemas/ / whitebox/ / report_prompts.py / config.py / src/llm_client.py）。
```

---

## 13. Closeout Record Template

```text
## v1.2.9 Closeout Record

- task_id: task-v1.2.9-phase4-report-agent-decoupling-r0
- audit_id: audit-v1.2.9-01
- attempt_id:
  - attempt-v1.2.9-01
  - attempt-v1.2.9-02
  - attempt-v1.2.9-closeout
- acceptance_id: accept-v1.2.9-01
- acceptance_result: pass_with_known_issues
- closeout_status: closed
- Gate: PASS_WITH_KNOWN_ISSUES
- blocks_next_version: no
- DS acceptance report path: audit/phase4大版本改造/DS_Agent_Team_Verify_Accept_Report_v1.2.9_2026-05-15.md
- team_mode_used: true
- coupling_verdict: MODERATE_COUPLING_ACCEPTABLE_WITH_KNOWN_ISSUES
- whether_1000_lines_is_blocker: false
- recommended_closeout_decision: closeout_pass_with_known_issues
- hard_blockers: none
- forbidden_files_touched: false
- audit_dirty_tree_mixed: false

Actual added files:
- src/phase4/report_normalizer.py
- src/phase4/report_narrative.py
- src/phase4/report_title.py
- tests/test_phase4_report_normalizer.py
- tests/test_phase4_report_agent_decoupling.py

Actual modified files:
- src/phase4/report_agent.py
- src/phase4/__init__.py
- docs/iterations/v1.2.9-Phase-4-Report-Agent-Decoupling-R0.md
- docs/iterations/TASK_LOG.md
- docs/iterations/CHANGELOG.md

Actual deleted files:
- none

Verification:
- `.venv/bin/python -m py_compile src/phase4/report_agent.py src/phase4/report_normalizer.py` -> pass
- `.venv/bin/python -m pytest tests/test_phase4_report_normalizer.py -v` -> 4 passed
- `.venv/bin/python -m pytest tests/test_inflection_point_output_guard.py -v` -> 5 passed
- `.venv/bin/python -m pytest tests/test_report_markdown_grounding.py tests/test_phase4_markdown_metric_grounding.py -v` -> 20 passed
- `.venv/bin/python -m py_compile src/phase4/report_agent.py src/phase4/report_narrative.py src/phase4/report_title.py src/phase4/report_normalizer.py` -> pass
- `.venv/bin/python -m pytest tests/test_phase4_report_agent_decoupling.py -v` -> 4 passed
- `.venv/bin/python -m pytest tests/test_report_product_contract.py tests/test_report_markdown_grounding.py tests/test_phase4_markdown_metric_grounding.py tests/test_risk_assessment_directionality.py tests/test_inflection_point_output_guard.py -v` -> 46 passed
- `.venv/bin/python -m compileall src` -> pass
- `.venv/bin/python -m pytest tests/ -v` -> 83 passed
- `.venv/bin/python main.py seeds/test8.txt` -> pass
- closeout hygiene verification:
  - `.venv/bin/python -m compileall src` -> pass
  - `.venv/bin/python -m pytest tests/test_phase4_report_normalizer.py tests/test_phase4_report_agent_decoupling.py -v` -> 8 passed
  - `.venv/bin/python -m pytest tests/test_report_markdown_grounding.py tests/test_report_product_contract.py -v` -> 28 passed
  - `.venv/bin/python -m pytest tests/test_phase4_markdown_metric_grounding.py tests/test_risk_assessment_directionality.py tests/test_inflection_point_output_guard.py -v` -> 18 passed
  - `.venv/bin/python -m pytest tests/test_run_dir_concurrency.py tests/test_whitebox_artifact_shell.py -v` -> 8 passed
  - `.venv/bin/python -m pytest tests/ -v` -> 83 passed

Latest run_dir:
- outputs/runs/test8_20260515_184351/run_964791_45220

Smoke artifacts:
- final_report.json: outputs/runs/test8_20260515_184351/run_964791_45220/final_report.json
- final_report.md: outputs/runs/test8_20260515_184351/run_964791_45220/final_report.md
- whitebox_summary.json: outputs/runs/test8_20260515_184351/run_964791_45220/whitebox_summary.json
- risk_level: high
- risk_level_label: 高风险
- primary_risk_types: group_polarization_risk
- whitebox report_completeness: pass
- whitebox artifact_check: pass
- final_report.md 五章结构: pass
- “模拟模拟极化指数”: not found
- “模拟模拟关键变化点”: not found
- code-owned / prompt instruction leakage: not found

Contract preservation:
- prompt semantics changed: no
- risk algorithm semantics changed: no
- final_report.json contract changed: no
- final_report.md five-chapter structure changed: no
- runtime artifact contract changed: no
- forbidden files touched: no
- audit/ dirty tree mixed in: no
- circular imports observed: no
- `_llm_generated_markdown` remains in report_agent.py: yes
- `parse_llm_report_response` remains in report_agent.py: yes
- `generate_fallback_report` remains in report_agent.py: yes

Closeout hygiene cleanup:
- basis: audit/phase4大版本改造/DS_Agent_Team_Dependency_Hygiene_Audit_v1.2.9_2026-05-15.md
- cleanup class: SAFE_TO_REMOVE A class only
- A1: removed unused `_normalize_report_title_line` import from report_agent.py
- A2: removed dead `build_entity_distribution()` from report_agent.py
- A3: removed dead `_trajectory_description()` from report_agent.py
- A4: removed dead `_stance_summary_lines()` from report_agent.py
- A5: removed duplicate local `TickLog` import in `load_tick_logs()`
- A6: removed `build_full_report_context`, `load_tick_logs`, `parse_llm_report_response`, `generate_markdown_report` from `src/phase4/__init__.py` re-export layer
- B class touched: no
- C class touched: no
- D class touched: no
- prompt semantics changed: no
- risk algorithm changed: no
- schema changed: no
- whitebox contract changed: no
- final_report contract changed: no
- main.py changed: no

Known issues:
- 风险阈值仍是工程初始阈值，待后续多 seed 标定。
- 模拟极化指数仍是工程 proxy。
- 模拟关键变化点仍未完整升级为多信号 framework。
- external_risk_adjustment 仅作为 future hook，未实现、未接入、未进入报告产物。
- select_primary_risk_types() 仍依赖 risk_assessment 文本 keyword matching。
- single_run_summary / parallel_world_synthesis / batch_synthesis_context 未实现。
- risk calculation 仍留在 report_agent.py，待后续 risk engine 解耦。

Carry-over:
- Risk Type Signal Source Patch waits for product risk-layer matrix.
- Inflection Point Framework R0.
- Structured Synthesis Artifacts.
- Phase 4 Risk Engine Extraction.

Blocks next version:
no
```

---

## 14. Carry-over Pool

以下内容不得在 v1.2.9 执行，但应保留为后续版本池：

```text
1. Risk Type Signal Source Patch
   - 等产品侧《政府治理视角舆情风险分层与等级映射清单 v0.1》回来后推进。

2. Inflection Point Framework R0
   - stance_mean_delta
   - key_group_stance_shift
   - risk_level_changed
   - risk_type_changed
   - source_metrics
   - confidence
   - whitebox consistency check

3. Structured Synthesis Artifacts
   - single_run_summary.json
   - parallel_world_synthesis.json
   - batch_synthesis_context.json

4. Infrastructure Hotfix
   - main.py run_dir 秒级时间戳 + exist_ok=False 并发撞名已在 v1.2.8.1.1 处理；v1.2.9 无新增 infra carry-over。

5. Phase 4 Risk Engine Extraction
   - 后续可考虑从 report_agent.py 继续拆出 risk_calculator.py / risk_types.py。

6. Dependency / Import Hygiene 长期规则（自 v1.2.9 起生效）
   - 后续所有"解耦"类版本，默认同步包含 dependency/import hygiene 检查。
   - hygiene cleanup 可作为同版本 closeout cleanup 执行，不默认拆成独立版本。
   - 但只允许清理有明确证据的 SAFE_TO_REMOVE 项。
   - KEEP_REQUIRED / NEEDS_CODEX_VERIFICATION / DO_NOT_TOUCH 项目不得直接删除。
   - 每次解耦版本 closeout 前，应至少运行一次静态 import 消费者扫描（grep/rg/AST）。
```
