# 迭代记录：v1.2.7 - Phase 4 Report Product Governance Sprint

## 1. Version Info

- **版本号**：v1.2.7
- **版本名称**：Phase 4 Report Product Governance Sprint
- **基于版本**：v1.2.6 - Schema Split Governance & Contract Library Boundary
- **当前阶段**：frozen / ready for Codex attempt-01
- **状态**：✅ frozen / owner approved
- **task_id**：task-v1.2.7-phase4-report-product-governance
- **audit_id**：audit-v1.2.7-01
- **review_id**：review-v1.2.7-01
- **attempt_id**：
  - attempt-v1.2.7-01
  - attempt-v1.2.7-02
  - attempt-v1.2.7-closeout-patch
- **acceptance_id**：accept-v1.2.7-01
- **base_version_status**：v1.2.6 closed / acceptance_result = pass
- **Git Commit / Tag**：`待填写`

---

## 2. Control Agent Decision

### Gate

```text
CONDITIONAL_GO
```

### 决策理由

v1.2.7 可以进入执行准备，但必须按本迭代文档冻结后的范围执行，不允许继续沿用早期 v1.2.7-v1.2.13 慢拆路线中的完整理想态架构。

本版本主问题定义为：

```text
Phase 4 报告产品化 R0 闭环。
```

该主问题可以包含多个子任务：

```text
1. report_meta / generated_at / report_type
2. audience_mode 最小路由
3. risk expression 结构化
4. Markdown grounding
5. whitebox section / report product checks 对齐
6. targeted tests
```

这些子任务均服务于同一个目标：

```text
让 final_report.md / final_report.json / whitebox 从技术输出，
升级为面向业务阅读、可追溯、可验收的模拟推演型舆情风险研判报告产物。
```

### 执行前条件

```text
1. Owner 确认本迭代文档。
2. Codex 必须先读取本迭代文档。
3. Codex 必须确认当前工作树状态。
4. 如果发现 v1.2.6 closeout 后存在非本轮 dirty files，必须先报告，不得混入本轮 diff。

   尤其不得混入以下 dirty files（Phase 3 是本轮 forbidden scope）：
   - src/__init__.py 的非本轮修改
   - src/phase3/tick_simulation.py 的非本轮修改
   - src/schemas.py deleted 状态

   如发现上述 dirty files，必须先停止并报告，由 Owner / Control Agent 判断是否提交、还原或隔离。

5. attempt-01 完成前不得进入 attempt-02。
6. attempt-02 完成前不得进入 closeout patch。
```

---

## 3. Goal & Boundary

### 3.1 本版本主目标

```text
在一周内完成 Phase 4 报告产品化 R0 闭环：
让 final_report.md / final_report.json / whitebox 从技术输出升级为面向业务阅读、可追溯、可验收的模拟推演型舆情风险研判报告产物。
```

---

### 3.2 本版本要解决的问题

```text
1. final_report.md 缺少稳定报告元信息，尤其是 generated_at。
2. 报告标题、类型、模拟口径不够清晰，容易被误读为现实舆情事实。
3. 风险研判缺少稳定的“风险等级 + 主要风险类型 + 风险解释”结构。
4. Markdown 正文存在技术指标裸露、测试报告口吻、业务解释不足的问题。
5. audience_mode / 阅读主体路由缺少最小字段承载。
6. whitebox report section check 与产品侧五章模板存在不一致风险。
7. report_schema_0507.md 中关于拐点检测归属的旧表述需要与 code-owned grounding 对齐。
8. prompt / Markdown 约束需要禁止未实现能力伪装、现实事实化表达、行政决策越界。
```

---

### 3.3 本版本不解决的问题

```text
1. 不实现双输出模式。
2. 不接入外部检索。
3. 不接入政策知识库。
4. 不做复杂可视化。
5. 不实现完整 risk classifier。
6. 不实现完整 representative comment selector。
7. 不重构完整 inflection detector。
8. 不做 Phase 4 大规模架构拆分。
9. 不创建 audience_router.py / risk_taxonomy.py / prefill_loader.py / quote_grounding.py / inflection_grounding.py / 完整 report_prompt_builder.py / report_markdown_writer.py / report_json_writer.py。允许新增轻量 src/phase4/report_prompts.py。
10. 不修改 Phase 1-3 主链行为。
11. 不改变 RuntimeLogger 职责。
12. 不改变 speaker selector 策略。
13. 不把 whitebox 扩大为 runtime authority。
```

---

### 3.4 禁止变化

本轮明确禁止：

```text
1. 不改 Phase 1 / Phase 2 / Phase 3 主链行为。
2. 不改 Phase 1 prompt。
3. 不改 Phase 3 speaker selector 策略。
4. 不改 RuntimeLogger 职责。
5. 不接 Web Search / RAG / MCP / 外部态势感知。
6. 不接政策知识库。
7. 不新增双输出模式。
8. 不让 LLM 自由发明风险类型。
9. 不让 LLM 自行重算拐点、立场矩阵、全局指标。
10. 不让 LLM 承担核心评分职责。
11. 不输出行政决策、法律判断、责任定性。
12. 不把模拟推演结果写成现实舆情事实。
13. 不把未实现能力包装成已完成能力。
14. 不大规模拆分 Phase 4 架构。
```

---

## 4. DS Pre-Audit Summary & Control Agent Adoption

### 4.1 DS Pre-Audit Summary

- **是否经过 DS 前置审查**：是
- **DS Verdict**：CONDITIONAL_GO
- **审查对象**：Phase4_Report_Product_Governance_Track_Roadmap_v0.1.md
- **审查模式**：DS Agent Team，team_mode_used = true
- **结论摘要**：

```text
路线图方向正确，可以作为 v1.2.7+ 阶段规划输入；
没有 hard blocker；
但存在 9 项必须在写 v1.2.7 迭代文档前修正的问题。
```

DS 关键发现：

```text
1. v1.2.7 一周可落地，约 23 工时、6-7 文件、400-500 行变更。
2. 推荐 2 次 attempt。
3. v1.2.7 不应过早创建 audience_router.py / risk_taxonomy.py / prefill_loader.py / quote_grounding.py / inflection_grounding.py 等模块。
4. v1.2.7 应主要修改 report_agent.py、schemas/phase4.py、新增 tests，并可选同步 whitebox check。
5. primary_risk_types 在 v1.2.7 使用，但风险白名单在原路线图 v1.2.9 才建立，必须修正。
6. 风险等级存在 PRD 6 级 vs 当前代码 4 级冲突。
7. audience_mode 在 v1.2.7 的填充策略必须明确。
8. report_schema_0507.md 与 PRD/Roadmap 在拐点检测归属上存在冲突。
9. whitebox REQUIRED_SECTION_GROUPS 与 PRD 五章模板不匹配。
```

---

### 4.2 Control Agent 采纳决策

#### Must Fix #1：风险类型过渡问题

采纳。

v1.2.7 将风险类型白名单前移为 **轻量内置白名单**，但不创建独立 `risk_taxonomy.py`。

规则：

```text
1. primary_risk_types 必须来自轻量白名单。
2. 白名单可先定义在 schemas/phase4.py 或 report_agent.py 附近的最小常量中。
3. 不实现完整 risk classifier。
4. LLM 只消费 risk_type_labels，不自由发明风险类型。
```

初始轻量白名单：

```text
fact_dispute_risk               事实争议风险
procedure_dispute_risk          程序争议风险
regulatory_accountability_risk  监管责任质疑风险
law_enforcement_trust_risk      执法公信力风险
response_delay_risk             回应滞后风险
information_opacity_risk        信息不透明风险
negative_narrative_risk         负面叙事聚合风险
group_polarization_risk         群体对立风险
secondary_spread_risk           次生传播风险
overseas_amplification_risk     境外放大风险
rumor_spread_risk               谣言扩散风险
institution_image_risk          机构形象风险
local_governance_pressure_risk  属地治理压力风险
```

#### Must Fix #2：风险等级枚举冲突

采纳。

v1.2.7 不扩展当前代码级 risk_level 枚举，沿用当前 4 级：

```text
low
medium
high
critical
```

新增中文 label 映射：

```text
low      → 低风险
medium   → 中风险
high     → 高风险
critical → 重大风险
```

说明：

```text
PRD 中“中低风险 / 中高风险”作为后续产品表达预留；
v1.2.7 不改变当前底层 risk_level enum，避免扩大 schema 语义。
```

#### Must Fix #3：audience_mode 填充策略

采纳。

v1.2.7 使用最小 deterministic rule：

```text
默认：generic_government

公安 / 交警 / 派出所 / 执法 / 警方：
  law_enforcement_facing

市场监督管理局 / 市监局 / 监管部门 / 食药监：
  regulator_facing

教育局 / 卫健委 / 住建局 / 属地政府 / 街道办：
  public_management_facing
```

数据来源：

```text
1. event_entities
2. relations
3. seed_input.txt
4. final_report.json / existing risk_assessment text if available
```

限制：

```text
1. 不创建 audience_router.py。
2. 不引入复杂 NER。
3. 不让 LLM 自由改写 audience_mode。
4. v1.2.8 可再做独立 audience routing 模块。
```

#### Must Fix #4：禁止伪装表达纳入 Markdown grounding

采纳。

v1.2.7 必须禁止：

```text
1. “综合全网信息显示”
2. “依据相关法规建议”
3. “系统已识别最具代表性观点”
4. “全网舆情已经”
5. “公众普遍认为”
6. “现实中已经形成”
```

除非未来接入外部检索、政策知识库或真实监测数据，否则不得出现上述现实事实化或能力伪装表达。

#### Must Fix #5：对策建议边界纳入 prompt 约束

采纳。

v1.2.7 的 Markdown 生成约束必须明确：

```text
系统输出的是舆情风险防范建议，不是法律意见、行政处罚建议或正式决策指令。
```

禁止：

```text
1. 建议立即处罚某具体人员。
2. 建议认定某机构违法。
3. 建议启动具体法律程序。
4. 替政府部门作行政决策。
5. 输出未经依据支持的责任定性。
```

#### Must Fix #6：五章模板策略

采纳。

v1.2.7 选择：

```text
迁移 / 对齐到 PRD 已审核通过的五章式模板。
```

默认五章：

```text
一、舆情概要
二、演化分析
三、风险研判
四、对策建议
五、附录
```

要求：

```text
1. 不新增“核心结论”作为第一章。
2. 不实现双输出模式。
3. whitebox REQUIRED_SECTION_GROUPS 必须同步更新为五章模板。
4. whitebox FINAL_SECTION_HEADINGS 必须同步检查并对齐五章模板（当前使用"综合建议/应对建议"，需更新为"对策建议"）。
```

同时 whitebox report_completeness.py 的 truncation detection 依赖 FINAL_SECTION_HEADINGS，该常量也必须同步更新。

#### Must Fix #7：Doc Patch 缓冲日

采纳为流程规则。

v1.2.7 执行前必须完成：

```text
1. Owner 审阅本迭代文档。
2. 如 DS / Owner 对本迭代文档提出边界修正，先 patch iteration doc。
3. Codex 不得在 iteration doc 未冻结前开始 attempt-01。
```

#### Must Fix #8：report_schema_0507.md 拐点检测归属冲突

采纳。

v1.2.7 明确：

```text
拐点由代码侧 / deterministic logic / whitebox 提供；
LLM 只负责解释 code-owned inflection_points；
若没有显著拐点，Markdown 必须写“本轮模拟未发现显著拐点”。
```

本轮不重构拐点检测器。

#### Must Fix #9：whitebox section headings 与 PRD 五章模板对齐

采纳。

v1.2.7 必须同步更新 whitebox report completeness / section matcher：

```text
旧 section group 如：
- 事件概述
- 舆情态势
- 风险评估
- 处置建议

应对齐为：
- 舆情概要
- 演化分析
- 风险研判
- 对策建议
- 附录
```

---


#### Additional Adoption：Prompt 外置化

采纳。

Owner 要求 Phase 4 prompt 后续单独文件承载，便于 Prompt Governance / Prompt Registry / prompt 版本治理。

v1.2.7 修正：

```text
允许新增 src/phase4/report_prompts.py，
用于承接静态 prompt 文本、Markdown grounding rules、risk expression rules、simulation wording rules。
```

边界：

```text
1. 不创建完整 report_prompt_builder.py。
2. 不引入 Prompt Registry。
3. 不创建 docs/prompts/phase4/ 作为本轮硬目标。
4. 不新增双模板 / 双输出模式。
5. 不改变 Phase 4 报告生成主链语义。
```

**report_prompts.py 内容边界（显式约束）：**

report_prompts.py 仅允许包含：
```text
1. 字符串常量
2. list / tuple
3. dict
4. 静态 prompt 文本
5. 静态 grounding rules
6. 静态 forbidden phrase rules
```

report_prompts.py 禁止包含：
```text
1. 函数
2. 方法
3. 类
4. 文件 IO
5. LLM 调用
6. 指标计算
7. audience routing 逻辑
8. risk classification 逻辑
9. whitebox 检查逻辑
10. runtime side effects
```

### 4.3 Control Agent 不采纳 / 延后项

以下 DS 建议延后，不进入 v1.2.7：

```text
1. v1.2.10 / v1.2.12 合并决策。
2. v1.2.13 架构拆分策略。
3. report_json_writer / report_markdown_writer 提前拆分。
4. prefill_loader 正式实现。
5. audience_router.py 独立模块。
6. risk_taxonomy.py 独立模块。
```

---

## 5. Target Structure / Artifact Contract

### 5.1 当前结构

v1.2.6 后 schema authority 为：

```text
src/schemas/
  __init__.py
  common.py
  phase1.py
  phase2.py
  phase3.py
  phase4.py
  _legacy.py
```

Phase 4 当前主要入口：

```text
src/phase4/report_agent.py
```

Whitebox 当前存在：

```text
src/whitebox/
  report_completeness.py
  report_observer.py
  artifact_check.py
```

---

### 5.2 本版本目标结构

本版本不做 Phase 4 大拆分。

允许最小新增 / 修改后结构：

```text
src/
  schemas/
    phase4.py                 # 可扩展 ReportMeta / AudienceMode / risk expression 字段

  phase4/
    report_agent.py           # 继续作为 Phase 4 报告生成入口
    report_prompts.py         # 允许新增：承接静态 prompt 文本、Markdown grounding rules、
                              #   risk expression rules、simulation wording rules、forbidden phrase rules
                              #   不包含业务逻辑（注意：这不是 report_prompt_builder.py，不引入完整 Prompt Registry）

  whitebox/
    report_completeness.py    # 对齐五章模板 section headings
    report_observer.py        # 如需记录 report product check，可最小扩展
```

测试：

```text
tests/
  test_report_product_contract.py
  test_report_markdown_grounding.py
```

可选：

```text
tests/fixtures/
  report_product_basic.json
```

---

### 5.3 明确不创建的结构

本版本不得创建：

```text
src/phase4/audience_router.py
src/phase4/risk_taxonomy.py
src/phase4/risk_expression.py
src/phase4/prefill_loader.py
src/phase4/quote_grounding.py
src/phase4/inflection_grounding.py
src/phase4/report_prompt_builder.py
src/phase4/report_markdown_writer.py
src/phase4/report_json_writer.py
```

如 Codex 认为必须创建新模块，必须先停止并报告，不得自行扩展。

---

### 5.4 运行产物变化

本版本允许扩展 `final_report.json` 与 `final_report.md` 内容，但不改变 run_dir 基本 artifact contract。

必须保持：

```text
outputs/runs/<run_id>/
  seed_input.txt
  entities_and_relations.json
  social_graph.json
  tick_logs.json
  final_report.json
  final_report.md
  run.log
  timing_summary.json
  run_meta.json
  whitebox_summary.json
  whitebox/
    report_completeness.json
    artifact_check.json
```

允许新增 / 扩展：

```text
final_report.json:
  report_meta
  audience_profile / audience_mode
  risk_expression fields

final_report.md:
  generated_at
  report_type
  simulation wording
  risk level + risk types + risk explanation

whitebox/report_completeness.json:
  updated section group checks

whitebox_summary.json:
  may include report product check status if existing structure supports minimal extension
```

---

### 5.5 Whitebox 边界

whitebox 只能做：

```text
1. 观察
2. 检查
3. 汇总
4. 验证
```

不得：

```text
1. 生成报告正文。
2. 参与 LLM 决策。
3. 修改模拟行为。
4. 替代 RuntimeLogger。
5. 成为 runtime authority。
6. 扩展为完整 observability aggregator。
```

---

## 6. File Change Scope

### 6.1 允许新增

```text
tests/test_report_product_contract.py
tests/test_report_markdown_grounding.py
src/phase4/report_prompts.py  # 允许新增：承接静态 prompt 文本
```

可选新增：

```text
tests/fixtures/report_product_basic.json
```

如需文档落盘：

```text
docs/iterations/v1.2.7 - Phase 4 Report Product Governance Sprint.md
```

---

### 6.2 允许修改

```text
src/schemas/phase4.py
  - 增加 ReportMeta / AudienceMode / risk expression 相关字段或类型
  - 保持既有 Phase4Output 兼容

src/schemas/__init__.py
  - 仅允许在新增 ReportMeta / AudienceMode / risk expression 相关类型时做 re-export 更新
  - 不得改变既有 schema export 语义
  - 如未新增需要 re-export 的类型，可不修改

src/phase4/report_agent.py
  - 增加 generated_at / report_type / audience_mode / risk expression wire
  - 修改 Markdown prompt / template 约束
  - 保持 Phase 4 调用入口兼容
  - 不做大规模架构拆分

src/whitebox/report_completeness.py
  - 更新 REQUIRED_SECTION_GROUPS 至五章模板
  - 可加入 generated_at / report_type / section heading 最小检查

src/whitebox/report_observer.py
  - 如现有结构需要，可最小记录 report product check 结果

docs/dev_spec.md
  - 同步 Phase 4 report product contract 摘要

docs/iterations/TASK_LOG.md
  - closeout 时同步

docs/iterations/CHANGELOG.md
  - closeout 时同步
```

---

### 6.3 禁止修改

```text
src/phase1/
src/phase2/
src/phase3/
src/llm_client.py
src/utils/runtime_logger.py
src/whitebox/artifact_check.py  # 除非仅为路径兼容的最小修正
profiling/
scripts/probes/
seeds/
outputs/
```

说明：

```text
src/whitebox/report_completeness.py / report_observer.py 可改；
whitebox 其他模块默认禁止。
```

---

### 6.4 必须保持不变

```text
1. Phase 1-3 主链行为。
2. EntityExtractionOutput canonical object 地位。
3. Phase2Output / TickLog / Phase4Output 对外兼容。
4. RuntimeLogger 职责。
5. speaker selector 策略。
6. outputs/runs/<run_id>/ 基本 artifact contract。
7. report generation 入口调用方式。
```

---

### 6.5 删除文件

```text
无。
```

本版本不得删除文件。

---

## 7. Execution Attempts

### 7.1 Execution Mode

```text
Two-Attempt Workflow + Closeout Patch
```

选择理由：

```text
1. DS 原始审查建议 v1.2.7 采用 2 次 attempt。
2. attempt-01 聚焦结构字段与 JSON/metadata wiring。
3. attempt-02 聚焦 Markdown grounding 与 whitebox section 对齐。
4. closeout patch 只处理文档同步、DS verify 小修和最终 smoke。
5. 不采用 4 attempt，避免一周冲刺管理成本过高。
```

---

### 7.2 attempt-v1.2.7-01：Report Contract Fields & Metadata Wiring

#### 目标

```text
建立 Phase 4 报告产品化 R0 的结构化字段基础。
```

#### 允许修改

```text
src/schemas/phase4.py
src/phase4/report_agent.py
tests/test_report_product_contract.py
```

#### 允许新增

```text
tests/test_report_product_contract.py
```

#### 禁止修改

```text
src/phase1/
src/phase2/
src/phase3/
src/utils/runtime_logger.py
src/phase3/speaker_selector.py
src/whitebox/  # attempt-01 不改 whitebox
```

#### 执行要求

```text
1. 增加或复用 report_meta 结构，包含：
   - generated_at
   - timezone
   - report_type
   - event_name
   - total_ticks
   - simulation_run_id

2. generated_at 必须由代码侧生成，不得由 LLM 生成。

3. final_report.json 必须包含 generated_at。

4. final_report.md 顶部必须展示 generated_at。

5. report_type 固定为：
   模拟推演型舆情风险研判报告

6. audience_mode 必须有最小 deterministic 填充：
   - 默认 generic_government
   - 基于关键词匹配 regulator / law enforcement / public management

7. 风险等级继续沿用当前 4 级代码枚举：
   - low
   - medium
   - high
   - critical

8. 增加中文 label 映射：
   - low → 低风险
   - medium → 中风险
   - high → 高风险
   - critical → 重大风险

9. 增加 primary_risk_types / risk_type_labels 字段或输出块。

10. primary_risk_types 必须来自轻量白名单。

11. 不实现完整 risk classifier。
```

#### 验收命令

```bash
.venv/bin/python -m py_compile src/schemas/phase4.py src/phase4/report_agent.py
.venv/bin/python -m pytest tests/test_report_product_contract.py -v
```

如 Windows：

```bash
py -m py_compile src/schemas/phase4.py src/phase4/report_agent.py
py -m pytest tests/test_report_product_contract.py -v
```

#### 通过条件

```text
1. py_compile 通过。
2. test_report_product_contract.py 通过。
3. final_report.json 结构测试可验证 report_meta。
4. generated_at 非 LLM 生成。
5. Markdown 顶部包含 generated_at。
6. risk_level_label 映射稳定。
7. primary_risk_types 来自白名单。
8. 不修改 Phase 1-3。
```

---

### 7.3 attempt-v1.2.7-02：Markdown Grounding & Whitebox Alignment

#### 目标

```text
将 Phase 4 Markdown 输出对齐产品侧报告 contract：
指标后台化、判断前台化、模拟推演口径、风险表达结构、禁止越界表达。
```

#### 允许修改

```text
src/phase4/report_agent.py
src/phase4/report_prompts.py
src/whitebox/report_completeness.py
src/whitebox/report_observer.py  # 如确有必要
tests/test_report_markdown_grounding.py
src/phase4/report_prompts.py  # 允许新增：承接静态 prompt 文本
```

#### 允许新增

```text
tests/test_report_markdown_grounding.py
src/phase4/report_prompts.py  # 允许新增：承接静态 prompt 文本
```

#### 禁止修改

```text
src/phase1/
src/phase2/
src/phase3/
src/utils/runtime_logger.py
src/phase3/speaker_selector.py
src/whitebox/artifact_check.py
```

#### 执行要求

```text
1. Markdown 风险研判章节必须采用：
   风险等级 + 主要风险类型 + 风险解释

2. Markdown 必须明确模拟推演口径。

3. Markdown 不得把模拟结果写成现实舆情事实。

4. 禁止出现：
   - 全网已经
   - 公众普遍认为
   - 现实中已经形成
   - 综合全网信息显示
   - 依据相关法规建议
   - 系统已识别最具代表性观点

5. 对策建议不得输出：
   - 行政决策
   - 法律判断
   - 责任定性
   - 具体处罚建议

6. 正文不得大面积裸露：
   - event_scale
   - event_controversy
   - polarization_index
   - stance_delta

7. 如果需要引用指标，只能转译为业务风险解释。

8. whitebox REQUIRED_SECTION_GROUPS 必须对齐五章模板：
   - 舆情概要
   - 演化分析
   - 风险研判
   - 对策建议
   - 附录

9. whitebox FINAL_SECTION_HEADINGS 必须同步对齐五章模板（"综合建议/应对建议" → "对策建议"）。

10. 拐点表达必须对齐 code-owned 规则：
   - 有 code-owned inflection_points 才解释
   - 无则写“本轮模拟未发现显著拐点”
   - LLM 不得自行重算拐点
```

#### 验收命令

```bash
.venv/bin/python -m py_compile src/phase4/report_agent.py src/whitebox/report_completeness.py
.venv/bin/python -m pytest tests/test_report_markdown_grounding.py -v
.venv/bin/python -m pytest tests/test_report_product_contract.py tests/test_report_markdown_grounding.py -v
```

如 Windows：

```bash
py -m py_compile src/phase4/report_agent.py src/whitebox/report_completeness.py
py -m pytest tests/test_report_markdown_grounding.py -v
py -m pytest tests/test_report_product_contract.py tests/test_report_markdown_grounding.py -v
```

#### 通过条件

```text
1. targeted tests 通过。
2. Markdown 不出现 forbidden phrases。
3. Markdown 包含模拟推演口径。
4. Markdown 风险章节结构稳定。
5. whitebox section headings 与五章模板一致（含 REQUIRED_SECTION_GROUPS 与 FINAL_SECTION_HEADINGS）。
6. 不触碰 forbidden files。
7. 不改变 RuntimeLogger / whitebox 职责。
```

---

### 7.4 attempt-v1.2.7-closeout-patch：Docs + Smoke + Acceptance Evidence

#### 目标

```text
完成小修补、文档同步、最终 smoke 和 closeout evidence。
```

#### 允许修改

```text
docs/iterations/TASK_LOG.md
docs/iterations/CHANGELOG.md
docs/dev_spec.md
docs/iterations/v1.2.7 - Phase 4 Report Product Governance Sprint.md
```

如 DS verify 发现小问题，可最小修改：

```text
src/phase4/report_agent.py
src/phase4/report_prompts.py
src/whitebox/report_completeness.py
tests/test_report_product_contract.py
tests/test_report_markdown_grounding.py
src/phase4/report_prompts.py  # 允许新增：承接静态 prompt 文本
```

#### 禁止修改

```text
除 DS verify 明确要求的最小修正外，不得新增功能。
```

#### 验收命令

```bash
.venv/bin/python -m py_compile src/schemas/phase4.py src/phase4/report_agent.py src/whitebox/report_completeness.py
.venv/bin/python -m pytest tests/test_report_product_contract.py tests/test_report_markdown_grounding.py -v
.venv/bin/python main.py seeds/test1.txt
```

如 Windows：

```bash
py -m py_compile src/schemas/phase4.py src/phase4/report_agent.py src/whitebox/report_completeness.py
py -m pytest tests/test_report_product_contract.py tests/test_report_markdown_grounding.py -v
py main.py seeds/test1.txt
```

#### 通过条件

```text
1. py_compile 通过。
2. targeted tests 通过。
3. test1 smoke 通过。
4. 最新 run_dir 生成。
5. final_report.md / final_report.json / whitebox_summary.json / whitebox/report_completeness.json 齐全。
6. artifact check 通过。
7. TASK_LOG / CHANGELOG / dev_spec 同步。
```

---

### 7.5 Attempt Dependency

```text
attempt-02 依赖 attempt-01 通过：是
closeout-patch 依赖 attempt-02 通过：是
是否允许并行：否
是否允许 attempt-01 / attempt-02 同时修改 report_agent.py：否，必须串行
如果 attempt-01 fail，attempt-02 必须停止：是
如果 attempt-02 fail，closeout-patch 必须停止：是
```

---

## 8. Verification Plan

### 8.1 静态检查

```bash
.venv/bin/python -m py_compile src/schemas/phase4.py
.venv/bin/python -m py_compile src/phase4/report_agent.py
.venv/bin/python -m py_compile src/whitebox/report_completeness.py
```

---

### 8.2 Targeted Tests

```bash
.venv/bin/python -m pytest tests/test_report_product_contract.py -v
.venv/bin/python -m pytest tests/test_report_markdown_grounding.py -v
```

---

### 8.3 Import Check

```bash
.venv/bin/python - <<'PY'
from src.schemas.phase4 import Phase4Output
print("phase4 schema import ok")
PY
```

如新增 ReportMeta / AudienceMode 等类型，也必须验证。

---

### 8.4 Smoke Test

```bash
.venv/bin/python main.py seeds/test1.txt
```

是否作为硬门槛：

```text
是。只在 closeout patch / 最终收口阶段跑一次。
```

原因：

```text
本轮主要修改 Phase 4 报告输出和 whitebox 检查，不应在每个 attempt 都跑完整烟雾测试；
但最终 closeout 必须验证端到端报告产物。
```

---

### 8.5 Regression Test

```bash
.venv/bin/python main.py seeds/test7.txt
```

是否作为硬门槛：

```text
否。
```

原因：

```text
test7 耗时较长且依赖模型环境；
本轮可使用 targeted tests + test1 smoke 完成主验收。
```

---

### 8.6 Artifact Check

最终 smoke 后必须检查最新 run_dir：

```text
1. run_dir 是否生成。
2. run.log 是否生成。
3. timing_summary.json 是否生成。
4. run_meta.json 是否生成。
5. entities_and_relations.json 是否生成。
6. social_graph.json 是否生成。
7. tick_logs.json 是否生成。
8. final_report.json 是否生成。
9. final_report.md 是否生成。
10. whitebox_summary.json 是否生成。
11. whitebox/report_completeness.json 是否生成。
12. whitebox/artifact_check.json 是否生成。
```

新增检查：

```text
1. final_report.md 顶部是否包含 generated_at。
2. final_report.json 是否包含 generated_at。
3. Markdown 与 JSON generated_at 是否一致。
4. final_report.md 是否包含“模拟推演型舆情风险研判报告”。
5. 风险研判章节是否包含风险等级。
6. 风险研判章节是否包含主要风险类型。
7. Markdown 是否不出现 forbidden phrases。
8. whitebox section headings 是否对齐五章模板。
```

---

## 9. Acceptance Target & Criteria

### 9.1 Hard Acceptance Target

不满足任一项即 fail / hold：

```text
1. final_report.md 顶部包含 generated_at。
2. final_report.json 包含 generated_at。
3. generated_at 由代码侧生成，不由 LLM 生成。
4. Markdown 与 JSON generated_at 一致。
5. 报告明确为“模拟推演型舆情风险研判报告”。
6. 风险研判采用“风险等级 + 主要风险类型 + 风险解释”结构。
7. risk_level 沿用当前 4 级枚举，不擅自扩展底层 enum。
8. risk_level_label 映射正确。
9. primary_risk_types 来自轻量白名单。
10. audience_mode 存在且有默认值。
11. Markdown 不出现现实事实化表达。
12. Markdown 不出现未实现能力伪装表达。
13. 对策建议不越界。
14. 正文不大面积裸露技术指标。
15. whitebox section headings 与五章模板一致。
16. py_compile 通过。
17. targeted tests 通过。
18. closeout smoke test1 通过。
19. 未修改 Phase 1-3 主链。
20. 未创建禁止的新架构模块。
21. report_prompts.py 如被新增，必须仅包含静态 prompt 常量和静态规则数据；不得包含函数、方法、类、文件 IO、LLM 调用、指标计算、whitebox 检查或 runtime side effects。
```

---

### 9.2 Soft Acceptance Target

可记录为 pass_with_known_issues：

```text
1. audience_mode 仅为最小关键词规则，存在误判可能。
2. risk_type 仅为轻量白名单，不是完整分类器。
3. representative comment selector 未实现。
4. inflection detector 未重构。
5. prefill loader 未实现，仅保留后续接口。
6. test7 未执行。
7. Markdown 表达仍有轻微风格问题，但不违反硬边界。
8. report_agent.py 存在 _llm_generated_markdown 模块级全局状态，会导致 LLM Markdown 路径与 fallback Markdown 路径之间存在隐性耦合风险。v1.2.7 不要求大规模重构该机制，但必须将其登记为 known issue，并在 targeted tests 中验证 generated_at 在两条 Markdown 路径中一致出现。
```

---

### 9.3 Pass

```text
1. attempt-01 / attempt-02 / closeout patch 全部完成。
2. targeted tests 全部通过。
3. smoke test1 通过。
4. final_report.md / final_report.json / whitebox 产物满足硬验收。
5. TASK_LOG / CHANGELOG / dev_spec 同步。
6. 无 forbidden file 修改。
7. 无 Phase 1-3 行为变化。
```

---

### 9.4 Pass with Known Issues

允许的 known issues：

```text
1. audience routing 为最小规则。
2. risk taxonomy 未独立模块化。
3. quote / inflection grounding 未完整治理。
4. prefill markdown loader 未进入本轮。
5. report_agent.py 仍然偏重，架构拆分留待后续。
```

---

### 9.5 Fail / Hold

出现以下任一情况即 fail / hold：

```text
1. smoke test 失败且无法证明为环境阻塞。
2. final_report.md 缺少 generated_at。
3. final_report.json 缺少 generated_at。
4. generated_at 由 LLM 生成或不可追溯。
5. Markdown 出现“全网已经”“公众普遍认为”等现实事实化表达。
6. Markdown 声称使用外部检索或政策依据，但本轮未接入。
7. LLM 自由发明 risk_type。
8. LLM 自行重算拐点、立场矩阵或全局指标。
9. 对策建议输出行政决策、法律判断或责任定性。
10. Phase 1-3 被修改。
11. RuntimeLogger 被修改。
12. speaker selector 被修改。
13. 创建禁止的新架构模块。
14. report_agent.py 被大规模重构且无法验证行为保持。
15. whitebox 被扩大为 runtime authority。
16. Codex 将非本轮 dirty files（尤其 src/__init__.py 非本轮修改、src/phase3/tick_simulation.py 修改、src/schemas.py deleted 状态）混入 v1.2.7 diff，且触碰 Phase 3 forbidden scope。
```

---

## 10. DS Review Scope

DS Verify / Accept 只审查以下内容：

```text
1. Scope Compliance
   - Codex 是否只做 Phase 4 report product R0。
   - 是否越界进入外部检索、政策知识库、双模式、完整分类器。

2. File Diff Compliance
   - 是否只修改允许文件。
   - 是否创建禁止的新模块。
   - 是否触碰 Phase 1-3、RuntimeLogger、speaker selector。

3. Report Contract Compliance
   - generated_at 是否存在且一致。
   - report_type 是否存在。
   - audience_mode 是否存在。
   - risk level + risk types + risk explanation 是否存在。

4. Markdown Grounding
   - 是否明确模拟推演口径。
   - 是否避免现实事实化表达。
   - 是否避免未实现能力伪装。
   - 是否避免技术指标大面积裸露。
   - 对策建议是否越界。

5. Whitebox Alignment
   - REQUIRED_SECTION_GROUPS 是否对齐五章模板。
   - FINAL_SECTION_HEADINGS 是否同步对齐五章模板。
   - whitebox 是否只做检查，不做生成或决策。
   - artifact check 是否仍通过。

6. Prompt Asset Boundary
   - report_prompts.py 如存在，是否为纯 prompt asset（仅静态常量与规则数据）。
   - 是否演变为 prompt builder 或包含业务逻辑。

7. Verification Evidence
   - py_compile 是否通过。
   - targeted tests 是否通过。
   - smoke test1 是否通过。
   - 最新 run_dir artifact 是否完整。

8. Documentation Sync
   - TASK_LOG / CHANGELOG / dev_spec / iteration doc 是否同步。
```

DS 不负责：

```text
1. 不重新设计版本范围。
2. 不把建议项自动升级为 blocker。
3. 不替 Control Agent 做最终 gate。
4. 不要求进入下一版本。
5. 不扩展到完整 Phase 4 架构拆分。
```

---

## 11. Execution Report Requirement

Codex 每个 attempt 完成后必须回传：

```text
1. attempt_id。
2. 实际新增文件清单。
3. 实际修改文件清单。
4. 实际删除文件清单。
5. 测试命令与结果。
6. 是否触碰 forbidden files。
7. 是否修改 Phase 1-3。
8. 是否修改 RuntimeLogger / speaker selector。
9. 是否创建禁止的新模块。
10. final_report.md / final_report.json 字段变化摘要。
11. whitebox 变化摘要。
12. git diff 摘要。
13. known_issues。
14. carry_over。
15. risk level：LOW / MEDIUM / HIGH。
```

最终 closeout patch 还必须回传：

```text
1. 最新 run_dir。
2. artifact check 结果。
3. final_report.md generated_at 检查结果。
4. final_report.json generated_at 检查结果。
5. generated_at 一致性结果。
6. risk_type 白名单检查结果。
7. forbidden phrase 检查结果。
8. TASK_LOG / CHANGELOG / dev_spec 同步结果。
```

---

## 12. Codex Execution Prompt

以下 prompt 可在 Owner 批准本迭代文档后发给 Codex。

```text
你是 Codex，负责执行 v1.2.7 - Phase 4 Report Product Governance Sprint。

必须先阅读并遵守：
docs/iterations/v1.2.7 - Phase 4 Report Product Governance Sprint.md

当前任务：
执行 attempt-v1.2.7-01：Report Contract Fields & Metadata Wiring。

硬约束：
1. 不修改 Phase 1 / Phase 2 / Phase 3。
2. 不修改 RuntimeLogger。
3. 不修改 speaker selector。
4. 不接外部检索 / 政策知识库。
5. 不实现双输出模式。
6. 不创建 audience_router.py / risk_taxonomy.py / prefill_loader.py / quote_grounding.py / inflection_grounding.py / 完整 report_prompt_builder.py / report_markdown_writer.py / report_json_writer.py。允许新增轻量 src/phase4/report_prompts.py。
7. 不让 LLM 自由发明风险类型。
8. 不让 LLM 自行重算指标。
9. generated_at 必须由代码侧生成，不得由 LLM 生成。

执行范围：
允许修改：
- src/schemas/phase4.py
- src/phase4/report_agent.py
- tests/test_report_product_contract.py

允许新增：
- tests/test_report_product_contract.py

目标：
1. 增加 report_meta：
   - generated_at
   - timezone
   - report_type
   - event_name
   - total_ticks
   - simulation_run_id

2. final_report.json 必须包含 generated_at。
3. final_report.md 顶部必须展示 generated_at。
4. report_type 固定为：模拟推演型舆情风险研判报告。
5. audience_mode 必须存在，默认 generic_government。
6. audience_mode 支持最小关键词路由：
   - 公安 / 交警 / 派出所 / 执法 / 警方 → law_enforcement_facing
   - 市监局 / 市场监督管理局 / 监管部门 / 食药监 → regulator_facing
   - 教育局 / 卫健委 / 住建局 / 属地政府 / 街道办 → public_management_facing
7. risk_level 沿用当前 4 级代码枚举：low / medium / high / critical。
8. 增加 risk_level_label 映射：
   - low → 低风险
   - medium → 中风险
   - high → 高风险
   - critical → 重大风险
9. 增加 primary_risk_types / risk_type_labels 字段或输出块。
10. primary_risk_types 必须来自轻量白名单。
11. 不实现完整 risk classifier。

验收命令：
.venv/bin/python -m py_compile src/schemas/phase4.py src/phase4/report_agent.py
.venv/bin/python -m pytest tests/test_report_product_contract.py -v

完成后回传：
1. 实际新增文件
2. 实际修改文件
3. 测试命令与结果
4. final_report.json / markdown 字段变化摘要
5. 是否触碰 forbidden files
6. git diff 摘要
7. risk level
8. 是否允许进入 attempt-02 的建议
```

---

## 13. DS Verify Prompt

以下 prompt 可在 Codex attempt-01 或 attempt-02 后发给 DS Team。

```text
请开启 DS Agent Team / 多 agent 并行验收模式。

硬约束：
team_mode_used 必须为 true。
至少启动以下 reviewer：
1. Scope Compliance Reviewer
2. Report Contract Reviewer
3. Markdown Grounding Reviewer
4. Whitebox / Test Evidence Reviewer

如果无法开启多个 reviewer agent，请停止并说明，不要用单 agent 替代。

任务：
验收 v1.2.7 - Phase 4 Report Product Governance Sprint 的 Codex attempt 结果。

输入：
1. v1.2.7 iteration document
2. Codex attempt report
3. 当前 git diff
4. 当前测试结果
5. 最新 run_dir（如已运行 smoke）

重点检查：
1. 是否只修改允许文件。
2. 是否触碰 Phase 1-3 / RuntimeLogger / speaker selector。
3. 是否创建禁止的新模块。
4. generated_at 是否由代码侧生成。
5. final_report.md / final_report.json 是否都有 generated_at。
6. Markdown 与 JSON generated_at 是否一致。
7. report_type 是否为“模拟推演型舆情风险研判报告”。
8. audience_mode 是否存在。
9. risk_level_label 是否正确。
10. primary_risk_types 是否来自白名单。
11. Markdown 是否避免现实事实化表达。
12. Markdown 是否避免未实现能力伪装。
13. 对策建议是否越界。
14. whitebox section headings 是否对齐五章模板。
15. py_compile / targeted tests / smoke test 是否通过。

输出格式：
# DS Verify Report

## 1. Metadata
task_id:
attempt_id:
team_mode_used:
verdict: PASS / PASS_WITH_ISSUES / HOLD / FAIL

## 2. Scope Compliance
## 3. Report Contract Check
## 4. Markdown Grounding Check
## 5. Whitebox / Test Evidence
## 6. Blockers
## 7. Known Issues
## 8. Recommendation

注意：
DS 不做最终 gate。
DS 不得扩大版本范围。
DS 不得要求进入下一版本。
```

---

## 14. Closeout Record

```text
iteration: v1.2.7
task_id: task-v1.2.7-phase4-report-product-governance
audit_id: audit-v1.2.7-01
review_id: review-v1.2.7-01
attempt_id:
- attempt-v1.2.7-01
- attempt-v1.2.7-02
- attempt-v1.2.7-closeout-patch
acceptance_id: accept-v1.2.7-01
acceptance_result: 待填写
git_commit: 待填写
git_tag: 待填写
latest_run_dir: 待填写
risk_level: 待填写
carry_over:
- audience routing 独立模块化延后
- risk taxonomy 独立模块化延后
- markdown prefill loader 延后
- representative quote grounding 延后
- inflection grounding 延后
- Phase 4 architecture hardening 延后
```

---

## 15. Control Agent Final Note

v1.2.7 不追求理想态 Phase 4 架构完成，而是追求：

```text
Phase 4 报告产品化 R0 闭环完成。
```

本轮成功的标准不是“模块拆得漂亮”，而是：

```text
1. 业务读者能读。
2. 工程侧能验。
3. 指标能追溯。
4. 模拟口径不越界。
5. 报告生成链路不漂移。
```
