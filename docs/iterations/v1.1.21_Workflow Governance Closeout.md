# 迭代记录：v1.1.21 - Workflow Governance Closeout

## 📍 版本信息
- **版本号**：v1.1.21
- **任务 ID**：task-v1.1.21-workflow-governance-closeout
- **基于版本**：v1.1.19
- **迭代日期**：2026-04-14
- **Closeout 时间**：2026-04-15 17:35 +08:00
- **Git Commit**：`0a846d7`
- **状态**：✅ 已完成（pass_with_known_issues）

---

## 🎯 本次迭代目标

### 问题描述
- 当前存在的问题：
  - workflow 规则权威源不单一
  - control plane 与主流程并存，存在状态冲突
  - workflow 缺少最小 event ids 与 freeze / rollback 治理落点
- 影响范围：
  - workflow governance
  - closeout / rollback 可审查性
- 是否为结构性问题：**是**

---

### 解决目标
- 本次希望达成的效果：
  - 收口 workflow authority model
  - 退役 control plane 运行时路径
  - 为 current workflow 落下最小 eventization 与 freeze 模板
- 是否允许 breaking change：**否**
- 本次是否涉及架构调整：**是**

---

## 🪪 Workflow Event IDs

- **Review ID**：`review-v1.1.21-01`
- **Attempt ID（最新）**：`attempt-v1.1.21-01`
- **Acceptance ID（最新）**：`accept-v1.1.21-01`

---

## 🔚 实际收口边界

本轮定义即为 `workflow governance closeout iteration`，不是 `Phase1 + Phase3` 双解耦实现 iteration。

本轮真实收口范围是：

- workflow governance refactor landing
- single authority model 落盘
- control plane 退役与归档
- minimal eventization 落盘
- freeze / closeout 模板落盘

未在本轮完成、并明确 carry-over 的内容：

- Phase1 解耦实现
- Phase3 解耦实现
- 与双解耦目标直接相关的代码级验证

---

# 🧠 治理变更说明

### 当前治理问题
- workflow 规则、状态、审计材料散落在多个入口
- control plane 与主流程并存，形成竞争性 authority
- 失败反馈无法稳定追到具体 attempt

---

### 目标结构（本次之后）

- `workflow_core.md` 作为唯一规则 authority
- iteration 文档状态 + `TASK_LOG.md` 作为 runtime authority
- `control/` 退役为 archive artifact
- `task_id / review_id / attempt_id / acceptance_id` 进入 workflow artifacts

---

### 本次迭代涉及层级（必须勾选）

- [x] Workflow Core
- [x] Agent Delivery Rules
- [x] Iteration / Acceptance Records
- [x] Probe Runtime Dependency
- [ ] Phase 1 Implementation
- [ ] Phase 3 Implementation

👉 本次只改：**workflow governance closeout**

---

# 🔄 流程变化

### 旧流程

多文档并行定义 workflow → 用户手工中转 → control plane 补充状态 → 审计难以追踪

---

### 新流程（本次迭代后）

`workflow_core.md` 定义规则
→ iteration 文档定义任务状态
→ `TASK_LOG.md` 定义 acceptance facts
→ closeout / freeze 通过 tag 与 record 固定版本边界

---

### 兼容性说明

- [ ] 完全兼容
- [x] 部分变更（需说明）
- [ ] 不兼容（需迁移）

说明：

- 业务实现范围保持不变
- workflow 治理边界被重新收口

---

# 📦 字段职责迁移（新增关键模块）

### 从 LLM 移除（改为代码处理）

- 字段：
  - importance（I 值）
  - 分布约束（排序 / 唯一性）
- 原因：
  - 非语义任务，不适合 LLM
  - 增加 JSON 复杂度

---

### 保留在 LLM

- group_name
- persona 语义内容
- 发言内容（Phase3）

---

### 延后生成（未来阶段处理）

- 高阶统计字段
- 多 agent 交互关系

---

# 📋 文件变更清单

### 新增文件
- `src/phase3/structure_parser.py`

---

### 修改文件
- `src/phase1_entity_extraction.py`
  - 拆分 P1-P 语义与数值生成
- `src/phase3_opinion_spread.py`
  - 拆分 content / structure

---

### 删除文件（如有）
- 无

---

### 未修改文件（明确列出）
- `src/phase2_*`
- `src/phase4_*`
- `profiling/*`

---

# 🔧 详细修改指令（给 Codex）

Pre-Implementation Review 输出必须带 `task_id` 与 `review_id`。

每轮 Codex 交付必须带 `attempt_id`。

每轮 MiniMax 验收必须带 `acceptance_id`。

## 任务 1：Phase1 解耦

**目标**：
将结构约束从 LLM 移至代码

**输入**：
P1-P / P1-W 当前实现

**输出**：
解耦后的 group + persona 生成

**实现要求**：
- I 值由代码生成
- LLM 仅输出语义字段
- persona 拆为两步生成（essentials / details）

---

## 任务 2：Phase3 解耦

**目标**：
拆分发言生成与结构输出

**输入**：
当前 generate_opinion_spreader_post

**输出**：
两段式执行流程

**实现要求**：
- Step1：生成 text
- Step2：structure parser 生成 JSON
- 保持原 tick / state 流程不变

---

## 任务 3：日志增强

**目标**：
增加可观测性

**实现要求**：
记录：

- generation_latency
- structuring_latency
- total_latency

---

# 🔒 实现约束（非常重要）

- 不允许跳过 Pre-Implementation Review
- 不允许用 Prompt 修复结构问题
- 不允许扩展未声明范围
- 优先保证结构正确，其次优化生成质量

---

# ✅ 验收标准（MiniMax 使用）

## 模块级验收

- [ ] Phase1 是否解耦成功
- [ ] Phase3 是否解耦成功
- [ ] 是否减少 JSON 复杂度
- [ ] 是否未引入新耦合

---

## 行为验收

运行：

```bash
py main.py seeds/test1.txt
```

---

# 🧊 Closeout Record

```text
iteration: v1.1.21
task_id: task-v1.1.21-workflow-governance-closeout
current_tag: iter-v1.1.21-closeout
previous_tag: v1.1.19-profiling-closeout
acceptance: pass_with_known_issues
carry_over:
- Phase1 双解耦实现未在本轮落地
- Phase3 双解耦实现未在本轮落地
- 业务代码级 smoke / acceptance 仍需在后续实现轮完成
- `profiling/output/raw_logs/_worker_tmp_test/chain_worker_vawgkfsj` 为无关权限残留，不影响当前 closeout commit 与 tag 建立
```
