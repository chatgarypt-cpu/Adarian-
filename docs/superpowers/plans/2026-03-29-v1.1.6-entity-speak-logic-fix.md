# v1.1.6 事件实体发言逻辑修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复"已故/匿名实体在 Tick 0 发言"和"未提取种子材料原始发言"两个问题

**Architecture:** 在 Entity 模型增加 `can_speak` 和 `original_statement` 字段，LLM2 负责标注和提取，LLM3 负责校验，Phase3 按字段控制发言逻辑

**Tech Stack:** Python 3.10+, Pydantic v2, LLM API (Qwen/DeepSeek)

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `src/schemas.py` | Entity 模型新增字段 |
| `src/phase1_entity_extraction.py` | LLM2 输出格式 + LLM3 校验规则 |
| `src/phase3_tick_simulation.py` | Tick 0 发言逻辑 + EVENT_ENTITY_POST_SYSTEM_PROMPT |
| `src/phase4_report_agent.py` | 报告区分"发言实体"和"被讨论实体" |

---

## Task 1: 修改 schemas.py - Entity 新增字段

**Files:**
- Modify: `src/schemas.py:35-52`

- [ ] **Step 1: 查看当前 Entity 类定义**

```python
# 当前 Entity 类（位于 schemas.py:35-52）
class Entity(BaseModel):
    name: str = Field(..., description="实体名称")
    type: Literal["individual", "organization", "group"] = Field(..., description="实体类型")
    role: str = Field(..., description="在事件中的角色")
    entity_category: Literal["event_entity"] = Field(default="event_entity")
```

- [ ] **Step 2: 修改 Entity 类，新增两个字段**

在 `entity_category` 字段后新增：

```python
class Entity(BaseModel):
    name: str = Field(..., description="实体名称")
    type: Literal["individual", "organization", "group"] = Field(
        ..., description="实体类型：individual=个人, organization=组织机构, group=群体"
    )
    role: str = Field(..., description="在事件中的角色")
    entity_category: Literal["event_entity"] = Field(
        default="event_entity", description="固定为 event_entity"
    )

    # v1.1.6 新增
    can_speak: bool = Field(..., description="是否可以发言：true=可以，false=不可（如已故/匿名）")
    original_statement: Optional[str] = Field(
        default=None,
        description="从种子材料提取的原始发言（带引号的直接引语），无原始发言则为 None"
    )
```

- [ ] **Step 3: 运行验证**

```bash
cd "d:/项目开发/研一/adarian/adarian mvp" && py -c "from src.schemas import Entity; print('Entity fields:', [f for f in Entity.model_fields.keys()])"
```

Expected output: `['name', 'type', 'role', 'entity_category', 'can_speak', 'original_statement']`

- [ ] **Step 4: Commit**

```bash
git add src/schemas.py
git commit -m "feat(v1.1.6): Entity 新增 can_speak 和 original_statement 字段"
```

---

## Task 2: 修改 phase1_entity_extraction.py - LLM2 输出格式

**Files:**
- Modify: `src/phase1_entity_extraction.py:79-180` (LLM2_SYSTEM_PROMPT)

- [ ] **Step 1: 查看当前 LLM2_SYSTEM_PROMPT 结构**

找到 `LLM2_SYSTEM_PROMPT` 的定义位置（大约在 line 79），确认当前的 event_entities 输出格式要求。

- [ ] **Step 2: 修改 LLM2_SYSTEM_PROMPT 中的 event_entities 格式要求**

在 event_entities 格式说明中新增 `can_speak` 和 `original_statement` 字段说明：

```json
"event_entities": [
  {
    "name": "实体名称",
    "type": "individual | organization | group",
    "role": "在事件中的角色",
    "entity_category": "event_entity",
    "can_speak": true | false,
    "original_statement": "原始发言或null"
  }
]
```

- [ ] **Step 3: 新增 can_speak 判断规则说明**

在 LLM2_SYSTEM_PROMPT 的"输出格式"说明中，在 event_entities 列表说明后追加：

```
【can_speak 判断规则】
- 机构/组织（organization）：默认 can_speak = true
- 个人（individual）：
  * 已故 → can_speak = false
  * 匿名（如当事人、受害者、佚名）→ can_speak = false
  * 具名在世 → can_speak = true

【original_statement 提取规则】
- 优先提取带引号的"直接引语"（如："哪位少爺吸了"）
- 如果有多条，提取"引发舆情的那一条"
- 如果没有直接引语但有转述，提取转述内容
- 如果完全没有，设为 null
```

- [ ] **Step 4: Commit**

```bash
git add src/phase1_entity_extraction.py
git commit -m "feat(v1.1.6): LLM2 输出格式新增 can_speak 和 original_statement 字段"
```

---

## Task 3: 修改 phase1_entity_extraction.py - LLM3 校验规则

**Files:**
- Modify: `src/phase1_entity_extraction.py` (LLM3_SYSTEM_PROMPT)

- [ ] **Step 1: 查找 LLM3_SYSTEM_PROMPT 位置**

使用 Grep 搜索 `LLM3_SYSTEM_PROMPT`，确认其位置。

- [ ] **Step 2: 在 LLM3_SYSTEM_PROMPT 中新增校验规则**

在现有的校验规则后追加：

```
【can_speak 合理性校验】
- 检查种子材料中是否有"已故"、"去世"、"死亡"、"离世"、"身亡"等关键词
  * 如果有，检查对应实体的 can_speak 是否为 false
  * 如果 can_speak 为 true 而实体已故，报错："XXX 已故，can_speak 应为 false"
- 检查是否有"匿名"、"佚名"、"当事人"、"受害者"、"网友"等匿名表述
  * 如果有，检查对应实体的 can_speak 是否为 false

【original_statement 合理性校验】
- 如果 original_statement 不为 null，检查种子材料中是否确实有该发言
- 如果种子材料中没有，提示："original_statement 与种子材料不符，请确认或设为 null"
```

- [ ] **Step 3: Commit**

```bash
git add src/phase1_entity_extraction.py
git commit -m "feat(v1.1.6): LLM3 新增 can_speak 合理性校验规则"
```

---

## Task 4: 修改 phase3_tick_simulation.py - Tick 0 发言逻辑

**Files:**
- Modify: `src/phase3_tick_simulation.py:212-292` (run_tick_0 方法)

- [ ] **Step 1: 查看当前 run_tick_0 方法结构**

读取 `run_tick_0` 方法完整代码（line 212-292），理解当前流程：
1. 遍历 event_nodes
2. 调用 LLM 生成发言
3. 创建 AgentEntry

- [ ] **Step 2: 修改 run_tick_0 方法，在 LLM 调用前增加 can_speak 检查**

修改位置约在 line 237（构建 Prompt 之前）：

```python
def run_tick_0(self) -> List[AgentEntry]:
    """执行 Tick 0：事件实体发言

    Returns:
        事件实体发言条目列表
    """
    console.print("[bold cyan]Tick 0:[/bold cyan] 事件实体发言")

    entries = []
    event_nodes = [
        n for n in self.phase2_output.nodes
        if n.entity_category == "event_entity"
    ]

    for node in event_nodes:
        # 获取对应的 Entity 信息
        entity = None
        for e in self.extraction_output.event_entities:
            if e.name == node.related_entity:
                entity = e
                break

        if not entity:
            continue

        # v1.1.6: 检查 can_speak
        if not entity.can_speak:
            # can_speak=false：不生成发言，标记为被讨论
            reason = entity.original_statement or "（该实体不可发言）"
            self.event_entity_posts[node.id] = reason
            self.agent_comments[node.id].append(reason)

            entry = AgentEntry(
                agent_id=node.id,
                group_name=node.group_name,
                saw_posts_from=[],
                previous_stance=5.0,
                current_stance=5.0,
                stance_delta=0.0,
                comment=reason,
                reasoning="实体不可发言",
            )
            entries.append(entry)
            console.print(f"  [dim]○[/dim] {node.group_name}: {reason}")

            continue

        # v1.1.6: 优先使用 original_statement
        if entity.original_statement:
            # 有原始发言，直接使用
            comment = entity.original_statement
            reasoning = "原始发言（从种子材料提取）"
            self.event_entity_posts[node.id] = comment
            self.agent_comments[node.id].append(comment)

            entry = AgentEntry(
                agent_id=node.id,
                group_name=node.group_name,
                saw_posts_from=[],
                previous_stance=5.0,
                current_stance=5.0,
                stance_delta=0.0,
                comment=comment,
                reasoning=reasoning,
            )
            entries.append(entry)
            console.print(f"  [green]✓[/green] {node.group_name}: {comment[:30]}... [dim](原始发言)[/dim]")

            continue

        # 无原始发言，调用 LLM 生成
        # 构建 Prompt（保持原有逻辑）
        system_prompt = EVENT_ENTITY_POST_SYSTEM_PROMPT.format(
            entity_name=entity.name,
            entity_type=entity.type,
            entity_role=entity.role,
            event_summary=self.event_summary,
            seed_text=self.seed_text[:2000] if len(self.seed_text) > 2000 else self.seed_text,
        )

        try:
            response = self.llm_event_entity.generate(
                system=system_prompt,
                user="请发布你的声明。",
                response_model=None,
            )

            comment, reasoning = self._parse_event_entity_response(response)

            self.event_entity_posts[node.id] = comment
            self.agent_comments[node.id].append(comment)

            entry = AgentEntry(
                agent_id=node.id,
                group_name=node.group_name,
                saw_posts_from=[],
                previous_stance=5.0,
                current_stance=5.0,
                stance_delta=0.0,
                comment=comment,
                reasoning=reasoning,
            )
            entries.append(entry)

            console.print(f"  [green]✓[/green] {node.group_name}: {comment[:30]}...")

        except Exception as e:
            console.print(f"  [yellow]⚠[/yellow] {node.group_name} 生成发言失败: {e}")
            comment = "（无评论）"
            self.event_entity_posts[node.id] = comment
            self.agent_comments[node.id].append(comment)

            entries.append(AgentEntry(
                agent_id=node.id,
                group_name=node.group_name,
                saw_posts_from=[],
                previous_stance=5.0,
                current_stance=5.0,
                stance_delta=0.0,
                comment=comment,
                reasoning="生成失败",
            ))

    return entries
```

- [ ] **Step 3: 运行验证**

```bash
cd "d:/项目开发/研一/adarian/adarian mvp" && py -c "from src.phase3_tick_simulation import SimulationEngine; print('Import OK')"
```

- [ ] **Step 4: Commit**

```bash
git add src/phase3_tick_simulation.py
git commit -m "feat(v1.1.6): Tick 0 按 can_speak 控制发言，优先使用 original_statement"
```

---

## Task 5: 修改 phase3_tick_simulation.py - EVENT_ENTITY_POST_SYSTEM_PROMPT

**Files:**
- Modify: `src/phase3_tick_simulation.py` (EVENT_ENTITY_POST_SYSTEM_PROMPT，约 line 68)

- [ ] **Step 1: 查找 EVENT_ENTITY_POST_SYSTEM_PROMPT 位置**

使用 Grep 搜索 `EVENT_ENTITY_POST_SYSTEM_PROMPT`，确认位置。

- [ ] **Step 2: 修改 Prompt，增加"禁止事后声明"指令**

在现有 Prompt 的"输出格式"说明后追加：

```
【重要约束】
- 这条声明应该是"事件起点"，而不是"事后回应"
- 禁止生成"道歉声明"、"澄清说明"、"事后回应"、"已处理"等
- 错误示例："该评论为不当言论，已严肃处理"
- 正确示例：基于你的角色和立场的初始表态
```

- [ ] **Step 3: Commit**

```bash
git add src/phase3_tick_simulation.py
git commit -m "feat(v1.1.6): EVENT_ENTITY_POST_SYSTEM_PROMPT 禁止事后声明"
```

---

## Task 6: 修改 phase4_report_agent.py - 区分发言/被讨论实体

**Files:**
- Modify: `src/phase4_report_agent.py`

- [ ] **Step 1: 查找报告生成中实体列表的位置**

使用 Grep 搜索"事件实体"或"发言实体"，确认报告中生成实体列表的位置。

- [ ] **Step 2: 修改报告生成逻辑**

将原有的"事件实体"列表拆分为两个：
- **发言实体**：can_speak=true 的实体，包含 Tick 0 发言
- **被讨论实体**：can_speak=false 的实体，说明原因

示例输出格式：

```markdown
## 事件实体

### 发言实体（2个）
| 实体 | 角色 | Tick 0 发言 |
|------|------|------------|
| 南通文旅 | 评论引发争议的官方账号 | "哪位少爺吸了" |
| 兵團禁毒 | 发布普法视频的官方机构 | "我们始终坚决打击毒品犯罪..." |

### 被讨论实体（1个）
| 实体 | 角色 | 说明 |
|------|------|------|
| 胖貓 | 事件当事人 | 已故，不可发言 |
```

- [ ] **Step 3: Commit**

```bash
git add src/phase4_report_agent.py
git commit -m "feat(v1.1.6): 报告区分发言实体和被讨论实体"
```

---

## Task 7: 端到端验证

**Files:**
- Test: 使用 test2, test3, test5 种子素材运行完整流程

- [ ] **Step 1: 运行 test2 验证**

```bash
cd "d:/项目开发/研一/adarian/adarian mvp" && py main.py seeds/test2.txt
```

**验收标准**：
- 胖猫 comment 为"（该实体不可发言）"或"已故"
- 其他实体正常发言

- [ ] **Step 2: 运行 test3 验证**

```bash
cd "d:/项目开发/研一/adarian/adarian mvp" && py main.py seeds/test3.txt
```

**验收标准**：
- 当事学生 comment 为"（该实体不可发言）"或"匿名"
- 其他实体正常发言

- [ ] **Step 3: 运行 test5 验证**

```bash
cd "d:/项目开发/研一/adarian/adarian mvp" && py main.py seeds/test5.txt
```

**验收标准**：
- 南通文旅的 original_statement 应为"哪位少爺吸了"
- Tick 0 直接使用原始发言，不调用 LLM

- [ ] **Step 4: 对比新旧版本输出差异**

```bash
# 对比 test5 的 entities_and_relations.json
# 检查 can_speak 和 original_statement 字段是否存在
```

- [ ] **Step 5: 更新文档**

- 更新 `docs/iterations/TASK_LOG.md` 记录完成状态
- 更新 `docs/iterations/CHANGELOG.md` 追加 v1.1.6 变更记录
- 更新 `v1.1.6_entity_speak_logic_fix.md` 状态为 ✅ 已完成

---

## 验收标准总结

| 验收项 | 预期结果 |
|--------|---------|
| schemas.py 包含 can_speak 和 original_statement | ✅ |
| test2：胖猫在 Tick 0 不发言 | ✅ |
| test3：当事学生在 Tick 0 不发言 | ✅ |
| test5：南通文旅原始评论"哪位少爺吸了"被提取使用 | ✅ |
| 报告正确区分"发言实体"和"被讨论实体" | ✅ |
