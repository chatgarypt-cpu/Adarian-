# v1.1.9 数据修复与 susceptibility 简单接入 - 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复最终报告数据源错误，让 susceptibility 字段真实接入 stance 变化约束逻辑

**Architecture:** 在现有 phase3/phase4 代码上做最小化改动：1) schema 扩展 tick_log entry 字段，2) phase3 的 apply_stance_constraint 接入 susceptibility 调制，3) phase4 的报告上下文构建从 tick_log[1] 和 tick_log[-1] 读取真实 stance 变化数据

**Tech Stack:** Python 3, Pydantic schemas, 现有 LLM client

---

## 文件变更概览

| 文件 | 改动类型 | 改动说明 |
|------|---------|---------|
| `adarian mvp/src/schemas.py` | 修改 | `AgentEntry` 新增 `susceptibility` 和 `change_reason` 字段 |
| `adarian mvp/src/phase3_tick_simulation.py` | 修改 | `apply_stance_constraint` 接入 susceptibility 调制并返回 change_reason，`run_tick` 记录新字段 |
| `adarian mvp/src/phase4_report_agent.py` | 修改 | `build_full_report_context` 从 tick_log[1] 和 tick_log[-1] 读取 stance 变化数据 |
| `adarian mvp/config.py` | 修改 | 新增 `SUSCEPTIBILITY_MODULATION_FACTOR` 参数 |

---

## Task 1: schemas.py AgentEntry 扩展

**Files:**
- Modify: `adarian mvp/src/schemas.py:380-389`

- [ ] **Step 1: 在 AgentEntry 类中添加 susceptibility 和 change_reason 字段**

当前 `AgentEntry` 类定义在 `schemas.py` 第 380-389 行。在 `stance_delta` 字段后新增两个字段：

```python
class AgentEntry(BaseModel):
    """单个 Agent 的发言条目"""
    agent_id: int = Field(..., description="Agent 唯一ID")
    group_name: str = Field(..., description="所属群体名称")
    saw_posts_from: List[int] = Field(..., description="本轮读取的发言者 ID 列表")
    previous_stance: float = Field(..., ge=1.0, le=10.0, description="上一轮立场分")
    current_stance: float = Field(..., ge=1.0, le=10.0, description="本轮立场分")
    stance_delta: float = Field(..., description="立场变化量，绝对值表示变化程度")
    susceptibility: float = Field(..., ge=0.0, le=1.0, description="该 agent 的易感性（新增 v1.1.9）")
    change_reason: str = Field(..., description="立场变化原因：within_effective_delta | bounded_by_susceptibility（新增 v1.1.9）")
    comment: str = Field(..., max_length=200, description="发表的评论内容")
    reasoning: str = Field(..., max_length=100, description="立场理由")
```

- [ ] **Step 2: 验证**

```bash
cd "D:\项目开发\研一\adarian\adarian mvp"
python3 -c "from src.schemas import AgentEntry; print('AgentEntry fields:', list(AgentEntry.model_fields.keys()))"
```

预期输出包含 `susceptibility` 和 `change_reason`

- [ ] **Step 3: 提交**

```bash
git add src/schemas.py
git commit -m "feat(v1.1.9): add susceptibility and change_reason to AgentEntry schema"
```

---

## Task 2: config.py 新增调制参数

**Files:**
- Modify: `adarian mvp/config.py`

- [ ] **Step 1: 在 Phase 3 参数区域添加 SUSCEPTIBILITY_MODULATION_FACTOR**

在 `config.py` 第 131 行（`MAX_POSTS_PER_TICK = 3`）之后添加：

```python
# susceptibility 调制系数（v1.1.9 新增）
# 高 susceptibility（>0.7）的 agent 变化幅度可增加 50%
SUSCEPTIBILITY_MODULATION_FACTOR = 0.5
```

- [ ] **Step 2: 验证**

```bash
python3 -c "import config; print('SUSCEPTIBILITY_MODULATION_FACTOR:', config.SUSCEPTIBILITY_MODULATION_FACTOR)"
```

预期输出：`SUSCEPTIBILITY_MODULATION_FACTOR: 0.5`

- [ ] **Step 3: 提交**

```bash
git add config.py
git commit -m "feat(v1.1.9): add SUSCEPTIBILITY_MODULATION_FACTOR config param"
```

---

## Task 3: phase3 apply_stance_constraint 接入 susceptibility

**Files:**
- Modify: `adarian mvp/src/phase3_tick_simulation.py`

### 3.1 重写 apply_stance_constraint 函数

**位置：** `phase3_tick_simulation.py` 第 489-526 行

- [ ] **Step 1: 替换 apply_stance_constraint 函数**

将现有函数替换为：

```python
def apply_stance_constraint(
    previous_stance: float,
    proposed_stance: float,
    confirmation_bias_level: str,
    susceptibility: float,
    group_distribution_strategy: str = "normal",
    mean_stance: float = 5.0,
) -> Tuple[float, str]:
    """在代码层面强制约束 stance 变化幅度，接入 susceptibility 调制

    Args:
        previous_stance: 上一轮立场分
        proposed_stance: LLM 提出的新立场分
        confirmation_bias_level: 确认偏差级别
        susceptibility: 该 agent 的易感性（0.0-1.0）
        group_distribution_strategy: 群体分布策略
        mean_stance: 全局平均立场

    Returns:
        (final_stance, change_reason) tuple
        change_reason: "within_effective_delta" | "bounded_by_susceptibility"
    """
    # 1. 基础变化上限
    base_delta_map = {
        "strong": 0.3,
        "weak": 1.0,
        "none": 2.0
    }
    base_delta = base_delta_map.get(confirmation_bias_level, 1.0)

    # 2. susceptibility 调制：高 susceptibility → 更大变化幅度
    # modulation = 1 + 0.5 × (susceptibility - 0.5)
    # susceptibility=1.0 时，modulation = 1.25（变化幅度+25%）
    # susceptibility=0.0 时，modulation = 0.75（变化幅度-25%）
    susceptibility_modulation = 1 + config.SUSCEPTIBILITY_MODULATION_FACTOR * (susceptibility - 0.5)
    effective_delta = base_delta * susceptibility_modulation

    # 3. v1.1.7 新增：舆论压力机制（minimal_supporters 策略下）
    if group_distribution_strategy == "minimal_supporters":
        if mean_stance < 4.0 and previous_stance >= 7.0:
            effective_delta = effective_delta * 1.2  # 增加 20% 的变化幅度

    # 4. 限制变化幅度
    delta = proposed_stance - previous_stance

    if abs(delta) <= effective_delta:
        final_stance = proposed_stance
        change_reason = "within_effective_delta"
    else:
        clipped_delta = effective_delta if delta > 0 else -effective_delta
        final_stance = previous_stance + clipped_delta
        change_reason = "bounded_by_susceptibility"

    final_stance = round(max(1.0, min(10.0, final_stance)), 2)
    return final_stance, change_reason
```

注意：原函数只返回 `float`，新函数返回 `Tuple[float, str]`。

### 3.2 修改 generate_opinion_spreader_post 返回值

**位置：** `phase3_tick_simulation.py` 第 375 行附近

- [ ] **Step 2: 修改函数签名和返回值**

函数签名从：
```python
def generate_opinion_spreader_post(self, agent: GraphNode) -> Tuple[str, float, str]:
```

改为：
```python
def generate_opinion_spreader_post(self, agent: GraphNode) -> Tuple[str, float, str, str]:
```

第 475-481 行调用处，修改为接收 change_reason：
```python
final_stance, change_reason = self.apply_stance_constraint(
    previous_stance=self.agent_stances[agent.id],
    proposed_stance=proposed_stance,
    confirmation_bias_level=confirmation_bias,
    susceptibility=agent.susceptibility,
    group_distribution_strategy=self.group_distribution_strategy,
    mean_stance=sum(self.agent_stances.values()) / len(self.agent_stances),
)
```

第 483 行返回语句从：
```python
return comment, final_stance, reasoning
```

改为：
```python
return comment, final_stance, reasoning, change_reason
```

### 3.3 修改 run_tick 以传递和记录 change_reason

**位置：** `phase3_tick_simulation.py` 第 617 行附近

- [ ] **Step 3: 修改 run_tick 中调用 generate_opinion_spreader_post**

约第 638 行，从：
```python
comment, new_stance, reasoning = self.generate_opinion_spreader_post(node)
```

改为：
```python
comment, new_stance, reasoning, change_reason = self.generate_opinion_spreader_post(node)
```

约第 647-656 行，修改 `AgentEntry` 创建，添加 `susceptibility` 和 `change_reason`：

```python
entry = AgentEntry(
    agent_id=node.id,
    group_name=node.group_name,
    saw_posts_from=saw_posts,
    previous_stance=previous_stance,
    current_stance=new_stance,
    stance_delta=round(new_stance - previous_stance, 2),
    susceptibility=node.susceptibility,  # 新增 v1.1.9
    change_reason=change_reason,          # 新增 v1.1.9
    comment=comment,
    reasoning=reasoning,
)
```

注意：`node` 是 `GraphNode` 类型，已有 `susceptibility` 字段。

- [ ] **Step 4: 验证语法**

```bash
python3 -c "import src.phase3_tick_simulation; print('phase3 import OK')"
```

- [ ] **Step 5: 提交**

```bash
git add src/phase3_tick_simulation.py
git commit -m "feat(v1.1.9): integrate susceptibility modulation into apply_stance_constraint"
```

---

## Task 4: phase4 报告数据源切换

**Files:**
- Modify: `adarian mvp/src/phase4_report_agent.py`

**问题根因：** 当前代码用 `tick_logs[0]`（事件实体发言）和 `tick_logs[-1]`（意见传播者最终）比较，但由于 tick_logs[0] 的 entries 是事件实体（ID=0,1,2...），tick_logs[-1] 的 entries 是意见传播实体（ID=3,4,...），两者 ID 不重叠，导致计算出的 stance delta 全部为 0 或无数据。

**修复方案：** 改为从 `tick_logs[1]`（意见传播者第一次发言）读取初始立场，从 `tick_logs[-1]` 读取最终立场。

- [ ] **Step 1: 修改 build_full_report_context 中的立场变化计算**

找到 `build_full_report_context` 函数中约第 155-166 行的立场变化数据部分。

原代码：
```python
# 5. 立场变化数据（用于洞察生成）
lines.append("\n【立场变化详情】")
if tick_logs:
    tick_0_entries = {e.agent_id: e for e in tick_logs[0].entries}
    tick_n_entries = {e.agent_id: e for e in tick_logs[-1].entries}
    for agent_id in tick_n_entries:
        if agent_id in tick_0_entries:
            initial = tick_0_entries[agent_id].current_stance
            final = tick_n_entries[agent_id].current_stance
            delta = final - initial
            group = tick_n_entries[agent_id].group_name
            lines.append(f"  {group}: {initial:.1f} → {final:.1f} ({delta:+.1f})")
```

替换为：
```python
# 5. 立场变化数据（用于洞察生成）
# v1.1.9 修复：从 tick_log[1]（意见传播者初始）而非 tick_log[0]（事件实体）读取初始立场
lines.append("\n【立场变化详情】")
if tick_logs and len(tick_logs) >= 2:
    # tick_log[0] 是事件实体发言，用 tick_log[1] 作为意见传播者初始立场
    tick_1_entries = {e.agent_id: e for e in tick_logs[1].entries}
    tick_n_entries = {e.agent_id: e for e in tick_logs[-1].entries}
    for agent_id in tick_n_entries:
        if agent_id in tick_1_entries:
            initial = tick_1_entries[agent_id].current_stance
            final = tick_n_entries[agent_id].current_stance
            delta = final - initial
            group = tick_n_entries[agent_id].group_name
            lines.append(f"  {group}: {initial:.1f} → {final:.1f} ({delta:+.1f})")
elif tick_logs and len(tick_logs) == 1:
    # 只有一个 tick 的边缘情况，用 tick_log[0] 的数据
    tick_0_entries = {e.agent_id: e for e in tick_logs[0].entries}
    for entry in tick_0_entries:
        lines.append(f"  {entry.group_name}: {entry.current_stance:.1f}（无变化，单 tick 数据）")
```

- [ ] **Step 2: 验证语法**

```bash
python3 -c "import src.phase4_report_agent; print('phase4 import OK')"
```

- [ ] **Step 3: 提交**

```bash
git add src/phase4_report_agent.py
git commit -m "fix(v1.1.9): use tick_log[1] as initial stance for opinion spreaders"
```

---

## Task 5: 集成测试验证

**Files:**
- Run: `python main.py seeds/test2.txt`

- [ ] **Step 1: 运行 test2 模拟**

```bash
cd "D:\项目开发\研一\adarian\adarian mvp"
python3 main.py seeds/test2.txt
```

- [ ] **Step 2: 验证 tick_log 数据**

检查 `outputs/tick_logs/tick_1.json` 和 `outputs/tick_logs/tick_5.json`（或最后一个 tick），确认：
1. AgentEntry 包含 `susceptibility` 和 `change_reason` 字段
2. `change_reason` 值为 `"within_effective_delta"` 或 `"bounded_by_susceptibility"`

```bash
python3 -c "
import json
from pathlib import Path
tick_1 = json.loads(Path('outputs/tick_logs/tick_1.json').read_text())
entry = tick_1['entries'][0]
print('susceptibility:', entry.get('susceptibility'))
print('change_reason:', entry.get('change_reason'))
"
```

- [ ] **Step 3: 验证 final_report.md stance 数字**

检查 `outputs/final_report.md` 中的"立场变化详情"部分，数字应从 tick_log 真实计算。

- [ ] **Step 4: 验证 susceptibility 调制效果**

对比不同 susceptibility 值的 agent：
- 高 susceptibility（如 >0.7）的 agent 应有更大的 stance 变化
- 低 susceptibility（如 <0.3）的 agent 应有更小的 stance 变化

---

## Task 6: 更新 CHANGELOG

**Files:**
- Modify: `adarian mvp/docs/iterations/CHANGELOG.md`

- [ ] **Step 1: 追加 v1.1.9 记录**

在 CHANGELOG 顶部添加：

```markdown
## v1.1.9 (2026-03-30)

### Bug Fix
- **数据源修复**：最终报告立场变化数据从 tick_log[1] 和 tick_log[-1] 读取，而非 tick_log[0]（事件实体）和 tick_log[-1]。修复了意见传播实体立场变化始终为 0 的问题。

### Feature
- **susceptibility 简单接入**：susceptibility 字段接入 stance 变化约束逻辑，高 susceptibility agent 可获得更大的变化幅度（通过 SUSCEPTIBILITY_MODULATION_FACTOR 调制）
- **tick_log 扩展**：AgentEntry 新增 `susceptibility` 和 `change_reason` 字段，便于后续分析

### 配置
- 新增 `SUSCEPTIBILITY_MODULATION_FACTOR = 0.5` 参数
```

- [ ] **Step 2: 提交**

```bash
git add docs/iterations/CHANGELOG.md
git commit -m "docs(v1.1.9): update CHANGELOG"
```

---

## Spec 自检

1. **Spec 覆盖检查**：
   - ✅ 最终报告数据源修复 → Task 4
   - ✅ susceptibility 简单接入 → Task 3
   - ✅ tick_log schema 扩展 → Task 1
   - ✅ 配置参数 → Task 2
   - ✅ 测试验证 → Task 5

2. **Placeholder 扫描**：无 TODOs / TBD

3. **类型一致性检查**：
   - `apply_stance_constraint` 返回 `Tuple[float, str]` ✅
   - `generate_opinion_spreader_post` 返回 `Tuple[str, float, str, str]` ✅
   - `run_tick` 中解包 `comment, new_stance, reasoning, change_reason` ✅
   - `AgentEntry` 新增 `susceptibility: float` 和 `change_reason: str` ✅

4. **Tick log 索引修复**：`tick_logs[0]`（事件实体）→ `tick_logs[1]`（意见传播者初始）✅

---

*计划创建时间：2026-03-30*
