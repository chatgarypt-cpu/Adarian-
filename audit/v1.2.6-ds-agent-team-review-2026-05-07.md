# DS Agent Team Review: v1.2.6 Schema Split Governance & Contract Library Boundary

**Date**: 2026-05-07
**Review Round**: R2 (post governance cleanup)
**Branch**: master
**Anchor Commit**: acf8e7e (chore: close out v1.2.5 whitebox artifact shell)

---

## 0. Team Mode

team_mode_used: **true**

reviewers:
- Source Structure Reviewer: **completed**
- Import Compatibility Reviewer: **completed**
- Contract Preservation Reviewer: **completed**
- Legacy Boundary Reviewer: **completed**
- Verification & Risk Reviewer: **completed**

---

## 1. Executive Verdict

DS_verdict: **CONDITIONAL_GO_AFTER_DOC_PATCH**

one_sentence_decision: 治理后源码结构显著改善（shim 全部清除、平行 import 路径统一、9 个死类型已隔离），v1.2.6 方向正确且可执行，但文档需修补 6 项（§6.2 允许修改清单过时、§5.2 未明确 schemas.py 处置策略、§5.3 类型归属需更新、§7.3 test 哑弹未修复、§8.2 验收命令与 Python 环境不一致、死类型处置策略缺失），修补后方可进入 Codex attempt-01。

---

## 2. Source Structure Findings

### 2.1 治理后结构（已确认）

```
src/
  __init__.py                 ← 导出 18 个活跃类型，版本 "1.2.5.1"
  llm_client.py
  schemas.py                  ← 单体 667 行，29 个类型（20 活跃 + 9 死）
  phase1/__init__.py          ← 17 个导出（含 _normalize_unescaped_quotes_inside_string_values）
  phase1/extraction.py
  phase1/prompts.py
  phase2/__init__.py          ← 7 个导出
  phase2/topology_builder.py
  phase3/__init__.py          ← 5 个导出
  phase3/tick_simulation.py
  phase3/context_builder.py
  phase3/simulation_card.py
  phase3/speaker_selector.py
  phase3/state_updater.py
  phase4/__init__.py          ← 11 个导出
  phase4/report_agent.py
  utils/runtime_logger.py
  whitebox/                   ← 4 个文件
```

### 2.2 治理变更确认

| 已删除 | 状态 |
|---|---|
| `src/phase1_entity_extraction.py` | ✅ 已删除 |
| `src/phase2_topology_builder.py` | ✅ 已删除 |
| `src/phase3_tick_simulation.py` | ✅ 已删除 |
| `src/phase4_report_agent.py` | ✅ 已删除 |
| `tests/test_legacy_shim_imports.py` | ✅ 已删除 |

| 已归档至 `docs/_archive/legacy/` | 状态 |
|---|---|
| `phase0_entity_extraction.py` | ✅ 完整归档，含 README |
| `phase1_persona_engine.py` | ✅ 完整归档 |
| `agent_quality_analyzer.py` | ✅ 完整归档 |

| 已更新为规范路径 | 状态 |
|---|---|
| `profiling/prompts.py` → `from src.phase1` | ✅ |
| `scripts/probes/p1a_prompt_probe.py` → `from src.phase1` | ✅ |
| `scripts/probes/p1g_prompt_probe.py` → `from src.phase1` | ✅ |
| `tests/test_json_parser.py` → `from src.phase1` | ✅ |
| `tests/test_json_parser_quote_tolerance.py` → `from src.phase1` | ✅ |

### 2.3 平行 Import 路径

**已解决。** 所有 import 现在统一使用规范路径（`from src.phase1 import X`），不存在两个路径指向同一代码的情况。

### 2.4 死类型清单（9 个，schemas.py 内零外部引用）

| 类型 | 行号 | 说明 |
|---|---|---|
| `EntityExtractionResult` | 283 | Phase 1 旧中间产物 |
| `GroupPlanItem` | 301 | Phase 1 旧骨架类型 |
| `GroupPlanResult` | 314 | Phase 1 旧输出 |
| `PersonaProfile` | 322 | Phase 1 旧 Persona 画像 |
| `PersonaEnrichedGroupItem` | 333 | Phase 1 旧合并类型 |
| `PersonaEnrichedGroupPlan` | 339 | Phase 1 旧输出 |
| `Archetype` | 358 | 旧人群原型（已被 OpinionSpreader 取代） |
| `Phase1Output` | 401 | 旧 Phase 1 输出 |
| `SimulationCard` | 583 | 定义但从未实例化 |

**DS 裁定：** 拆分时移入 `src/schemas/_legacy.py`，不从 `__init__.py` 重新导出。不做删除（符合 §3.4 禁止删除历史入口）。

### 2.5 额外发现

- `_deprecated/` 为空目录 — 应删除
- `main.py` L374-411 日志标签仍引用旧文件名（`"phase1_entity_extraction"`）— 语义过时但不阻塞
- `SpeakerSelectionResult` 和 `SilentAgentUpdate` 未通过 `src/__init__.py` 导出（仅 phase3 内部使用）
- `NodeRole` 在 `phase3/tick_simulation.py:32` 为无用 import

---

## 3. Import Compatibility Findings

### 3.1 Import Map（治理后，9 个唯一文件）

| # | 文件 | 导入符号数 |
|---|---|---|
| 1 | `main.py:39` | 4 (EntityExtractionOutput, Phase2Output, Phase4Output, TickLog) |
| 2 | `src/__init__.py:17-42` | 18 (全部重新导出) |
| 3 | `src/phase1/extraction.py:33` | 4 |
| 4 | `src/phase2/topology_builder.py:26-29` | 8 |
| 5 | `src/phase2/topology_builder.py:343` | 1 (lazy, `if __name__`) |
| 6 | `src/phase3/tick_simulation.py:29-33` | 10 |
| 7 | `src/phase3/speaker_selector.py:9` | 2 |
| 8 | `src/phase3/state_updater.py:7` | 1 |
| 9 | `src/phase4/report_agent.py:26-29` | 7 |
| 10 | `src/phase4/report_agent.py:617` | 1 (lazy, 函数体内) |
| 11 | `tests/test_phase1_output_contract.py:1` | 4 |

**模式：** 100% `from src.schemas import X`，无星导入，无动态导入，无循环依赖。

### 3.2 Python 模块/包冲突

**确认：`src/schemas.py` 与 `src/schemas/` 无法共存。** Python 3 在两者同时存在时优先解析目录包。执行策略必须是：

```text
1. 删除 src/schemas.py
2. 创建 src/schemas/ package
3. __init__.py 重新导出所有 20 个活跃类型
4. 所有 11 个现有 import 点无需修改（通过 package __init__.py 透明解析）
```

### 3.3 Compat 决策

**不需要 `src/compat/` 目录。** 所有遗留 shim 已删除，所有消费者已迁移到规范路径，`from src.schemas import X` 通过 package `__init__.py` 自然保持兼容。

---

## 4. Contract Preservation Findings

### 4.1 最终类型归属（DS 裁定）

#### common.py — 5 个跨阶段共享类型

| 类型 | 理由 |
|---|---|
| `EntityCategory` | 共享词汇枚举（保留在 `__init__.py` 导出中以保持 API 兼容） |
| `Entity` | Phase 1/2/3 使用 |
| `OpinionSpreader` | Phase 1/2/3 使用，含 C/stance_score/confirmation_bias_level 兼容属性 |
| `Relation` | Phase 1 使用，作为 EntityExtractionOutput 的路径依赖 |
| `EntityExtractionOutput` | **全部 5 个阶段使用**，canonical object |

#### phase2.py — 5 个 Phase 2 类型

| 类型 | 理由 |
|---|---|
| `NodeRole` | 主要由 Phase 2 使用（Phase 3 有无用 import，应清理） |
| `EdgeType` | 仅 Phase 2 使用 |
| `GraphNode` | Phase 2 生产，Phase 3 消费 |
| `GraphEdge` | Phase 2 生产，Phase 3 消费 |
| `Phase2Output` | Phase 2/3/4 使用 |

注：治理后 `phase1_persona_engine.py`（唯一在 Phase 1 中引用 GraphNode 的文件）已归档，GraphNode 现仅被 Phase 2/3 使用，归属 phase2.py 更合理。

#### phase3.py — 6 个 Phase 3 类型

| 类型 | 理由 |
|---|---|
| `AgentEntry` | Phase 3 使用 |
| `GlobalMetrics` | Phase 3 使用 |
| `TickLog` | Phase 3/4 使用 |
| `SimulationCard` | 死代码，移入 _legacy.py |
| `SpeakerSelectionResult` | Phase 3 内部使用 |
| `SilentAgentUpdate` | Phase 3 内部使用 |

注：`SimulationCard` 虽为死代码，但语义上属 Phase 3 域。DS 裁定移入 `_legacy.py` 而非 phase3.py，因其无活跃引用。

#### phase4.py — 4 个 Phase 4 类型

| 类型 | 理由 |
|---|---|
| `EmotionTrajectory` | Phase 4 使用 |
| `InflectionPoint` | Phase 4 使用 |
| `RiskLevel` | Phase 4 使用 |
| `Phase4Output` | Phase 4 使用 |

#### _legacy.py — 9 个死类型

`EntityExtractionResult`, `GroupPlanItem`, `GroupPlanResult`, `PersonaProfile`, `PersonaEnrichedGroupItem`, `PersonaEnrichedGroupPlan`, `Archetype`, `Phase1Output`, `SimulationCard`

包含其所有 field_validator 和 model_validator。

注：`ConfirmationBiasLevel` 枚举亦为死代码，但因其仍在 `src/__init__.py` 中导出（公共 API），DS 裁定保留在 common.py 并标记 deprecated 注释。

### 4.2 契约保留确认

| 必须保留项 | 状态 | 验证 |
|---|---|---|
| `EntityExtractionOutput` 作为 canonical object | ✅ | 赋值至 common.py，4 个 model_validator 完整保留 |
| `OpinionSpreader.C` property (L168-171) | ✅ | `return self.P * (self.I / 10)` |
| `OpinionSpreader.stance_score` property (L173-179) | ✅ | `return self.I if P==+1 else 11-self.I` |
| `OpinionSpreader.confirmation_bias_level` property (L181-189) | ✅ | I≥7→"strong", I≥4→"weak", else "none" |
| 所有 field_validator / model_validator | ✅ | 不做任何修改 |

### 4.3 跨阶段依赖图

```
                    common.py
             ┌────────────────────┐
             │  Entity             │
             │  OpinionSpreader    │
             │  Relation           │
             │  EntityExtractionOutput │
             │  EntityCategory     │
             └──────┬──────┬──────┘
                    │      │
         ┌──────────┤      ├──────────────┐
         ▼          ▼      ▼              ▼
     phase1.py   phase2.py  phase3.py   phase4.py
     (extract)  (topology) (simulate)  (report)
                     │         │
                     └────┬────┘
                          │
                    互引用 common.py 类型
```

---

## 5. Legacy Boundary Findings

### 5.1 遗留清理完整性

**通过。** 全仓库 grep 确认零残留 import 引用。仅存的引用为 `main.py` 中的日志标签字符串（`"phase1_entity_extraction"`），属信息性字符串，非 import 路径。

### 5.2 归档质量

`docs/_archive/legacy/README.md` 记录了 3 个归档文件及其保留原因。归档文件完整、未截断。

### 5.3 `_deprecated/` 建议

**应删除。** 当前为空目录，无实际用途。

### 5.4 Compat 策略

**本轮不需要 `src/compat/` 目录。** 兼容性通过 `src/schemas/__init__.py` 重新导出实现。所有遗留消费者已在治理中迁移至规范路径。

### 5.5 退役条件更新

| 资产 | 退役条件 |
|---|---|
| `_deprecated/` 空目录 | 立即删除（不属于 v1.2.6 scope，可作为治理收尾） |
| `main.py` 日志标签 | 更新为 `"phase1"/"phase2"/"phase3"/"phase4"`（低优先级，记录 carry_over） |
| `schemas.py` 中 9 个死类型 | 移入 `_legacy.py`，不重新导出 → v1.2.7+ 正式删除 |
| `config.py` 中 `MIN_ARCHETYPES`/`MAX_ARCHETYPES` | 孤立常量，与死类型一起清理 |

---

## 6. Verification Plan Findings

### 6.1 关键发现

| # | 发现 | 严重性 |
|---|---|---|
| V1 | `test_phase1_output_contract.py` 仍是**哑弹** — 无 `sys.path` 处理、无 `if __name__ == "__main__"` 块。`python tests/test_phase1_output_contract.py` 静默退出或 ModuleNotFoundError | **CRITICAL** |
| V2 | 无持久化旧 import 兼容性测试 — v1.2.6 §7.3 的内联 `python - <<'PY'` 不可回归 | **HIGH** |
| V3 | `python` 命令在系统上可能不存在（实际可用 `python3` 或 `.venv/bin/python`） | **MEDIUM** |
| V4 | `src/__init__.py` 不在 v1.2.6 §6.2 允许修改清单中 — schema split 后必须更新其中的 18 个导入 | **HIGH** |
| V5 | v1.2.6 §6.2 列出的 `src/phase1_entity_extraction.py` 等 4 个文件已被删除 — 清单过时 | **MEDIUM** |

### 6.2 验收命令修正

当前 v1.2.6 §7.3 验收命令：
```bash
python tests/test_phase1_output_contract.py   # ← 哑弹，需改为 pytest
```

必须修正为：
```bash
pytest tests/test_phase1_output_contract.py -v
```

并增补：
```bash
python tests/test_schema_imports.py            # ← 新增：持久化 import 验证
python -c "from src import EntityExtractionOutput, Phase2Output, TickLog, Phase4Output"  # ← src re-export 验证
```

### 6.3 测试基础设施总览

| 测试文件 | python 直接运行 | pytest 运行 | 覆盖内容 |
|---|---|---|---|
| `test_phase1_output_contract.py` | ❌ 哑弹 | ✅ 2/2 passed | Phase 1 contract fields + compatibility properties |
| `test_phase_package_imports.py` | ✅ | ✅ 1 test | Phase 1/2/3/4 entrypoint callability |
| `test_json_parser.py` | ✅ 10/10 | ✅ 10 tests | CJK quote normalization + JSON parsing |
| `test_json_parser_quote_tolerance.py` | ✅ 6/6 | ✅ 6 tests | Unescaped quote handling |
| `test_whitebox_artifact_shell.py` | ✅ | ✅ 4 tests | Whitebox artifact shell checks |

**pytest 全量：23/23 passed in 0.20s**

---

## 7. Required Document Patches Before Execution

以下 6 项**必须**在进入 Codex attempt-01 前修补到 v1.2.6 文档：

### Patch 1：§6.2 允许修改 — 更新过时清单

删除已不存在的文件（4 个 shim 已在治理中删除）：
```diff
- src/phase1_entity_extraction.py
- src/phase2_topology_builder.py
- src/phase3_tick_simulation.py
- src/phase4_report_agent.py
```
新增必须修改的文件：
```diff
+ src/__init__.py                    ← schema split 后 18 个导入路径需更新
```

### Patch 2：§5.2 目标结构 — 明确 schemas.py 处置策略

新增：
```text
Python 3 中 src/schemas.py 与 src/schemas/ package 无法共存。
执行策略：删除 src/schemas.py → 创建 src/schemas/ package → __init__.py 重新导出所有活跃类型。
不得尝试同时保留两者。
新增 src/schemas/_legacy.py 收纳 9 个死类型（不从 __init__.py 重新导出）。
```

### Patch 3：§5.3 类型归属 — 更新为治理后最终映射

按 DS 裁定更新 common.py（5 类型）/ phase2.py（5 类型）的具体清单。GraphNode 和 NodeRole 从 common.py 移入 phase2.py。

### Patch 4：§7.3 验收命令 — 修复 test 哑弹 + 增补 import test

```diff
- python tests/test_phase1_output_contract.py
+ pytest tests/test_phase1_output_contract.py -v
+ python tests/test_schema_imports.py
```

### Patch 5：§8.2 Import / Unit Test — 添加环境前提

```text
所有测试命令需在项目根目录执行，确保 venv 已激活（.venv/bin/python）。
如 pytest 不可用，运行 pip install pytest。
test_phase1_output_contract.py 需添加 sys.path 处理（与其他测试文件对齐）。
```

### Patch 6：§5.2 新增 _legacy.py 说明

```text
_legacy.py：收纳 9 个已确认无活跃引用的死类型。
不从 __init__.py 重新导出。保留用于历史数据反序列化兼容。
退役条件：v1.2.7 P/C/V Skeleton 通过后正式删除。
```

---

## 8. Risk Register

| # | Risk | Severity | Mitigation |
|---|---|---|---|
| R1 | `test_phase1_output_contract.py` 哑弹 — 核心验收命令无效 | **CRITICAL** | Doc Patch 4 + 5：改用 pytest，补充 sys.path |
| R2 | `src/__init__.py` 未列入允许修改 — schema split 后 18 个导入断裂 | **HIGH** | Doc Patch 1：添加到 §6.2 |
| R3 | `schemas.py` 与 `schemas/` 共存冲突 — Codex 可能尝试危险中间状态 | **HIGH** | Doc Patch 2：明确必须删除 schemas.py |
| R4 | 无持久化 import 测试 — 旧 import 兼容性无回归保护 | **HIGH** | Doc Patch 4：创建 `test_schema_imports.py` |
| R5 | §6.2 允许修改清单过时 — 列出已删除文件 | **MEDIUM** | Doc Patch 1：更新清单 |
| R6 | `python` 命令不可用 — 验收脚本无法执行 | **MEDIUM** | Doc Patch 5：明确使用 `python3` 或 venv |
| R7 | 9 个死类型增加 import 噪音 — 可能被误重新导出 | **MEDIUM** | Doc Patch 6：移入 `_legacy.py`，不重新导出 |
| R8 | `NodeRole` 在 phase3 中为无用 import | **LOW** | attempt-02 时清理 |
| R9 | `main.py` 日志标签引用旧文件名 | **LOW** | 记录 carry_over，不阻塞 v1.2.6 |
| R10 | `SpeakerSelectionResult`/`SilentAgentUpdate` 未通过 `__init__.py` 导出 | **LOW** | 仅 phase3 内部使用，非公共 API |
| R11 | 归档文件中的 `from src.schemas` import 在拆分后失效 | **LOW** | 归档文件不参与运行时，README 已说明 |

---

## 9. Final DS Recommendation

1. **v1.2.6 可以进入 Codex attempt-01，但必须先完成 6 项 Doc Patch。** 治理后的源码结构显著改善：平行 import 路径已统一、4 个遗留 shim 已删除、3 个遗留业务逻辑已归档、import 表面从 11 个文件缩减至 9 个、9 个死类型已识别隔离。核心拆分策略（删除 `schemas.py` → 创建 `schemas/` package → `__init__.py` 统一重新导出）经 5 个 reviewer 独立验证为可行，且可实现零调用方修改。

2. **必须补强的边界：** (a) `src/__init__.py` 是 schema split 后唯一**必须**修改的 re-export 文件，文档 §6.2 必须将其纳入允许修改清单；(b) `test_phase1_output_contract.py` 的哑弹问题（无 sys.path + 无 `__main__` 块）使核心验收命令形同虚设，必须在 attempt-01 中修复；(c) 必须创建持久化的 `test_schema_imports.py` 作为旧 import 兼容性的回归守卫；(d) 9 个死类型必须移入 `_legacy.py` 且不重新导出，防止活跃 API 表面被污染。

3. **必须继续冻结的内容：** Parser / Compiler / Validator Skeleton、Repair Loop、Diff Guard、Prompt Library、Phase 4 Report Governance、产品侧报告标准、拐点标准、代表性发言标准、Web Search / RAG / MCP。本轮 scope 严格限定为 schema 契约资产的阶段化治理。`src/compat/` 目录不需要创建。`_deprecated/` 空目录建议在治理收尾中删除但不阻塞 v1.2.6。9 个死类型不删除，仅移入 `_legacy.py` 隔离，正式删除留给 v1.2.7+。

---

## Appendix A: Reviewer Conflict Resolution Log

| 冲突点 | Reviewer A | Reviewer B | DS 裁定 |
|---|---|---|---|
| GraphNode 归属 | Contract Preservation R1: common.py | Contract Preservation R2: phase2.py | 采纳 R2 — 治理后 phase1_persona_engine.py 已归档，GraphNode 仅 Phase 2/3 使用 |
| NodeRole 归属 | Contract Preservation R1: common.py | Contract Preservation R2: phase2.py | 采纳 R2 — 主要消费者为 Phase 2 |
| `src/compat/` 是否需要 | Legacy Boundary R1: 不需要 | Import Compatibility R2: 不需要 | 一致 — 不需要 |
| 死类型处置 | Contract Preservation R2: _legacy.py | Import Compatibility R2: 直接删除 | 采纳 _legacy.py — 符合 §3.4 禁止删除 |
| `schemas.py` 处置 | Legacy Boundary R1: 保留（不建议拆分）| Import Compatibility R2: 删除并替换为 package | 否决 R1 保留建议 — Schema Split 是 v1.2.6 核心目标，经证明可通过 __init__.py 实现零调用方修改 |

## Appendix B: Carry Over Items

| # | Item | Target Version |
|---|---|---|
| C1 | `main.py` 日志标签更新为规范 phase 名称 | v1.2.7 |
| C2 | `config.py` 中 `MIN_ARCHETYPES`/`MAX_ARCHETYPES` 清理 | v1.2.7 |
| C3 | `_deprecated/` 空目录删除 | 治理收尾 |
| C4 | 9 个死类型正式删除 | v1.2.7+ |
| C5 | `NodeRole` 在 phase3 中无用 import 清理 | v1.2.6 attempt-02 |
| C6 | `SpeakerSelectionResult`/`SilentAgentUpdate` 添加到 `__init__.py` 导出 | 如需公共 API |
