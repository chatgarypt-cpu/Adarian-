# DS Agent Team Report: v1.2.5.1 Legacy Source Cleanup Direction Audit

**Audit date**: 2026-05-07
**Project**: Adarian MVP
**Phase**: Phase 1 Generation Governance — v1.2.5 closeout 后删前方向审计
**DS Master**: 汇总与冲突消解

---

## 0. Team Mode

**team_mode_used**: `true`

**reviewers**:

| Reviewer | Status |
|---|---|
| Import Graph Reviewer | **completed** |
| Runtime Path Reviewer | **completed** |
| Test & Script Reference Reviewer | **completed** |
| Documentation Contract Reviewer | **completed** |
| Deletion Risk Reviewer | **completed** (partial — risk assessment synthesized from other 4 reviews) |

---

## 1. Repository State

```
current_branch:         master
latest_commit:          acf8e7e "chore: close out v1.2.5 whitebox artifact shell"
latest_tags:            iter-v1.2.5-closeout, baseline-test1-e2e-runnable, shitmountain, iter-v1.1.21-closeout
git_status_summary:     clean tree, no pending changes
rollback_anchor:        iter-v1.2.5-closeout tag (matching HEAD)
```

```
clean_tree:             true
recent_commit_exists:   true
closeout_tag_exists:    true  (iter-v1.2.5-closeout)
rollback_risk:          LOW  (tagged, clean tree, full git history intact)
```

---

## 2. Executive Verdict

**DS_verdict**: `GO_DELETE_SOME`

**one_sentence_decision**: 3 个孤立旧文件可以立即归档；4 个 shim 文件当前不能删（有活跃消费者），但消费者迁移路径清晰且成本极低——完成迁移后四个 shim 即可全部删除，完全不需要永久保留。

**详细说明**：

- `src/phase0_entity_extraction.py`、`src/phase1_persona_engine.py`、`src/agent_quality_analyzer.py` 三个文件在全项目范围内**零 import 引用、零 runtime 触达、零 test 依赖**。可以归档到 `docs/_archive/legacy/`。
- 四个旧入口 shim 文件（`phase1_entity_extraction.py` ~ `phase4_report_agent.py`）均有活跃下游消费者。其中 Phase1 shim 有 6 个消费者，Phase2/3/4 shim 各 1 个消费者。**但这些 shim 不是永久性资产**——它们是 v1.2.5 的冷备用过渡层。一旦把消费者的 import 目标从旧路径改成新 package 路径，shim 就完成了历史使命，可以直接删除。
- Phase2/3/4 shim 的消费者迁移成本极低（各 1 行 import 改动），Phase1 shim 稍多但全为机械操作（6 个文件的 import 行改动 + 1 行 `__init__.py` 导出补全）。
- 本轮审计确认**消费者迁移完全可以做、路径明确、无技术阻塞**。是否在本轮执行迁移由 owner 决定。

---

## 3. DELETE Candidates

> 以下文件**技术上可以删除**（零活跃依赖），但 v1.2.5 合同将其列入 S6.3 保护清单。owner 可在 v1.2.5 正式关闭后自行决定是删除还是归档。

| file | reason | referenced_by | risk | recommended_validation |
|---|---|---|---|---|
| `src/phase0_entity_extraction.py` | 完全孤立：零 import、零 runtime、零 test、零 probe 引用。v1.1.1 时代的旧 Phase 0 实现，已被 `src/phase1/` 完全替代。唯一引用是 `phase1_persona_engine.py:445` 中的字符串打印（该文件本身也是孤立文件） | 无（仅 `phase1_persona_engine.py` 有一行 CLI help 字符串） | LOW | `python -m py_compile main.py && python main.py seeds/test1.txt` |
| `src/phase1_persona_engine.py` | 完全孤立：零 import、零 runtime、零 test 引用。v1.1.0 时代的旧 persona engine，含收缩算法和原始 prompt，已被 `src/phase1/extraction.py` 替代 | 无 | LOW | 同上 |
| `src/agent_quality_analyzer.py` | 完全孤立：零 import、零 runtime、零 test 引用。v1.1.5 时代的独立质量分析工具，从未接入主 pipeline | 无 | LOW | 同上 |

**重要提醒**：这三个文件在 v1.2.5 closeout 文档 `v1.2.5-source-tree-governance-whitebox-artifact-shell.md` S6.3 中被列入"禁止修改"清单。删除它们技术上安全，但会与 v1.2.5 的 closeout 合同声明"无文件删除"产生文档不一致。**CHANGELOG 中 2026-04-08 已声称这三个文件被删除（与磁盘现实冲突）**，如果 owner 决定删除，需要同时修正 CHANGELOG 中的虚假声明，改为"v1.2.5.1 正式删除"。

---

## 4. ARCHIVE Candidates

| file | reason | referenced_by | risk | target_archive_path |
|---|---|---|---|---|
| `src/phase0_entity_extraction.py` | 含 6 个独特 prompt 常量、`extract_entities()` 函数、v1.1.1 架构设计意图的完整独立实现。建议归档保留历史参考 | 无 | LOW | `docs/_archive/legacy/phase0_entity_extraction.py` |
| `src/phase1_persona_engine.py` | 含 `shrink_to_agents()` 收缩算法、原始 PHASE1 prompt 模板、v1.1.0→v1.1.1 架构变迁记录。算法有独特历史价值 | 无 | LOW | `docs/_archive/legacy/phase1_persona_engine.py` |
| `src/agent_quality_analyzer.py` | 独立分析工具，含立场分布统计、diff 相似度检测、逻辑一致性验证。虽然从未接入主 pipeline，但代码完整可独立运行 | 无 | LOW | `docs/_archive/legacy/agent_quality_analyzer.py` |

**归档建议**：如果选择 ARCHIVE 而非 DELETE，建议统一移动到 `docs/_archive/legacy/` 并在 README 或 CHANGELOG 中注明归档位置。

---

## 5. KEEP Files

| file | reason | referenced_by | risk_if_deleted |
|---|---|---|---|
| `src/phase1_entity_extraction.py` | **Phase 1 旧入口 shim。** 6 个活跃消费者：tests (3) + probes (2) + profiling (1)。v1.2.5 合同 S5.2 明确要求保留 | `tests/test_legacy_shim_imports.py`, `tests/test_json_parser.py`, `tests/test_json_parser_quote_tolerance.py`, `scripts/probes/p1a_prompt_probe.py`, `scripts/probes/p1g_prompt_probe.py`, `profiling/prompts.py` | **HIGH** — 6 个消费者立即 import 失败，test suite 全崩，profiling pipeline 全崩 |
| `src/phase2_topology_builder.py` | **Phase 2 旧入口 shim。** 1 个活跃消费者。v1.2.5 合同 S5.2 明确要求保留 | `tests/test_legacy_shim_imports.py` | **MEDIUM** — 一个测试文件 import 失败。迁移路径极简单（改 import 目标） |
| `src/phase3_tick_simulation.py` | **Phase 3 旧入口 shim。** 1 个活跃消费者。v1.2.5 合同 S5.2 明确要求保留 | `tests/test_legacy_shim_imports.py` | **MEDIUM** — 迁移路径极简单 |
| `src/phase4_report_agent.py` | **Phase 4 旧入口 shim。** 1 个活跃消费者。v1.2.5 合同 S5.2 明确要求保留 | `tests/test_legacy_shim_imports.py` | **MEDIUM** — 迁移路径极简单 |
| `profiling/prompts.py` | 活跃 profiling 工具模块，提供 prompt builder 函数 | 由 profiling chain 内部使用 | **HIGH** — profiling pipeline 中断 |
| `scripts/probes/p1a_prompt_probe.py` | 活跃生产 profiling 探针（Analyzer prompt 系列，L1/L2/L3 三级） | profiling baseline manifest 引用 | **HIGH** — profiling 能力损失 |
| `scripts/probes/p1g_prompt_probe.py` | 活跃生产 profiling 探针（Generator prompt 系列，G1/G2/G3 三级，含重复采样） | profiling baseline manifest 引用 | **HIGH** — profiling 能力损失 |
| `scripts/probes/reduced_schema_chain_probe.py` | 活跃生产 profiling 探针（简化 schema 测试，已完全解耦于 shim） | profiling baseline manifest 引用 | **MEDIUM** — profiling 能力损失；但此探针不 import shim，迁移风险最低 |

---

## 6. HOLD Files

| file | reason | missing_evidence | next_check |
|---|---|---|---|
| `src/agent_quality_analyzer.py` | 运行时路径 Reviewer 给出 HOLD（独立诊断工具价值），而 Test Reviewer 给出 DELETE（零引用）。存在判断分歧 | 是否有外部脚本或手动运维流程依赖此文件？是否需要作为 profiling 补充诊断工具保留？ | 由 owner 确认是否有手动使用场景；如无，按 DELETE 或 ARCHIVE 处理 |
| `scripts/generate_snapshot.py` | `docs/structure_audit_cleanup_task.md` 声明此文件"应保留"在 `scripts/` 目录，但实际文件系统中**该文件不存在**。文档和磁盘存在事实冲突 | 文件是否曾被删除？是否有 git 历史可恢复？是否已迁移到别处？ | 用 `git log -- scripts/generate_snapshot.py` 确认历史；如已永久删除，更新 `structure_audit_cleanup_task.md` 移除该条目 |
| `docs/_archive/control_plane/` | 控制面概念已退役（见 `docs/audit/workflow/control_plane_retirement.md`），但 `_archive/control_plane/` 中的 `inbox.md`、`snapshot.md`、`README.md` 仍保留。不清楚是否有复盘价值 | control plane 退役决策的恢复路径是什么？这些文件是退役产物还是历史参考？ | owner 确认是否全部保留在 `_archive/` 或可删除 |

---

## 7. Special Check: v1.2.5 Shim Files

逐一判断四个旧入口文件的保留状态：

### `src/phase1_entity_extraction.py`

```
recommendation:      KEEP
reason:              6 个活跃消费者（3 tests + 2 probes + 1 profiling 工具）。
                     profiling/prompts.py 和两个 probe 脚本依赖此 shim 获取
                     ANALYZER/GENERATOR/VALIDATOR prompt 常量。
                     此外，两个 JSON parser 测试依赖此 shim 获取内部函数
                     _normalize_inner_cjk_quotes 和 _parse_json_candidate。
                     删除前必须完成 6 个消费者的迁移。
referenced_by:       tests/test_legacy_shim_imports.py, tests/test_json_parser.py,
                     tests/test_json_parser_quote_tolerance.py,
                     scripts/probes/p1a_prompt_probe.py,
                     scripts/probes/p1g_prompt_probe.py,
                     profiling/prompts.py
safe_to_delete:      false
```

### `src/phase2_topology_builder.py`

```
recommendation:      KEEP (可迁后删除)
reason:              仅 1 个消费者：tests/test_legacy_shim_imports.py。
                     src.phase2/__init__.py 已导出所有同名符号。
                     迁移成本极低：改 test 的 import 目标即可。
                     在迁移完成前必须保留。
referenced_by:       tests/test_legacy_shim_imports.py
safe_to_delete:      false (迁移后为 true)
```

### `src/phase3_tick_simulation.py`

```
recommendation:      KEEP (可迁后删除)
reason:              仅 1 个消费者：tests/test_legacy_shim_imports.py。
                     src.phase3/__init__.py 已导出所有同名符号。
                     迁移成本极低。
referenced_by:       tests/test_legacy_shim_imports.py
safe_to_delete:      false (迁移后为 true)
```

### `src/phase4_report_agent.py`

```
recommendation:      KEEP (可迁后删除)
reason:              仅 1 个消费者：tests/test_legacy_shim_imports.py。
                     src.phase4/__init__.py 已导出所有同名符号。
                     迁移成本极低。
referenced_by:       tests/test_legacy_shim_imports.py
safe_to_delete:      false (迁移后为 true)
```

---

## 7B. Shim Consumer Migration Plan（shim 消费者迁移方案）

四个 shim 文件的"不能删"只是暂时的——它们的存在意义是让旧 import 路径继续工作。一旦所有消费者的 import 目标从旧路径迁移到新 package 路径，四个 shim 就可以全部删除。

### 关键前提：补全 `src/phase1/__init__.py` 导出

`_normalize_unescaped_quotes_inside_string_values` 这个函数当前只存在于 `src/phase1/extraction.py` 中，通过 `phase1_entity_extraction.py` 的 `from src.phase1.extraction import *` 间接暴露给 `tests/test_json_parser_quote_tolerance.py`。`src/phase1/__init__.py` 没有显式导出它。

**操作**：在 `src/phase1/__init__.py` 的 import 块中追加此函数，在 `__all__` 列表中追加其名称。

```python
# src/phase1/__init__.py 追加

from .extraction import (
    ...
    _normalize_unescaped_quotes_inside_string_values,  # 追加
    ...
)

__all__ = [
    ...
    "_normalize_unescaped_quotes_inside_string_values",  # 追加
    ...
]
```

### 消费者迁移清单

#### Phase 1 shim — 6 个消费者

| # | 消费者文件 | 当前 import 行 | 迁移后 import 行 |
|---|---|---|---|
| 1 | `profiling/prompts.py:5-10` | `from src.phase1_entity_extraction import (GENERATOR_SYSTEM_PROMPT, GENERATOR_USER_PROMPT, VALIDATOR_SYSTEM_PROMPT, VALIDATOR_USER_PROMPT)` | `from src.phase1 import GENERATOR_SYSTEM_PROMPT, GENERATOR_USER_PROMPT, VALIDATOR_SYSTEM_PROMPT, VALIDATOR_USER_PROMPT` |
| 2 | `scripts/probes/p1a_prompt_probe.py:23` | `from src.phase1_entity_extraction import ANALYZER_SYSTEM_PROMPT, ANALYZER_USER_PROMPT` | `from src.phase1 import ANALYZER_SYSTEM_PROMPT, ANALYZER_USER_PROMPT` |
| 3 | `scripts/probes/p1g_prompt_probe.py:23` | `from src.phase1_entity_extraction import GENERATOR_SYSTEM_PROMPT, GENERATOR_USER_PROMPT` | `from src.phase1 import GENERATOR_SYSTEM_PROMPT, GENERATOR_USER_PROMPT` |
| 4 | `tests/test_legacy_shim_imports.py:8-9` | `from src.phase1_entity_extraction import ANALYZER_SYSTEM_PROMPT, extract_entities_from_file` | `from src.phase1 import ANALYZER_SYSTEM_PROMPT, extract_entities_from_file` |
| 5 | `tests/test_json_parser.py:16` | `from src.phase1_entity_extraction import _normalize_inner_cjk_quotes, _parse_json_candidate` | `from src.phase1 import _normalize_inner_cjk_quotes, _parse_json_candidate` |
| 6 | `tests/test_json_parser_quote_tolerance.py:16-20` | `from src.phase1_entity_extraction import (_normalize_unescaped_quotes_inside_string_values, _normalize_inner_cjk_quotes, _parse_json_candidate)` | `from src.phase1 import _normalize_unescaped_quotes_inside_string_values, _normalize_inner_cjk_quotes, _parse_json_candidate` |

#### Phase 2 shim — 1 个消费者

| # | 消费者文件 | 当前 import 行 | 迁移后 import 行 |
|---|---|---|---|
| 7 | `tests/test_legacy_shim_imports.py:10` | `from src.phase2_topology_builder import build_topology_from_extraction` | `from src.phase2 import build_topology_from_extraction` |

#### Phase 3 shim — 1 个消费者

| # | 消费者文件 | 当前 import 行 | 迁移后 import 行 |
|---|---|---|---|
| 8 | `tests/test_legacy_shim_imports.py:11` | `from src.phase3_tick_simulation import SimulationEngine` | `from src.phase3 import SimulationEngine` |

#### Phase 4 shim — 1 个消费者

| # | 消费者文件 | 当前 import 行 | 迁移后 import 行 |
|---|---|---|---|
| 9 | `tests/test_legacy_shim_imports.py:12` | `from src.phase4_report_agent import generate_report_with_llm` | `from src.phase4 import generate_report_with_llm` |

### `test_legacy_shim_imports.py` 的处置

迁移完成后，这个测试文件的 4 条 import 全部改成了新 package 路径。此时它不再是"legacy shim 回归测试"，因为 shim 已经不存在了。有两个选择：

- **选项 A**：删掉 `test_legacy_shim_imports.py`，因为 `test_phase_package_imports.py` 已经用新路径做了完全相同的 import 验证。
- **选项 B**：保留但重命名为 `test_phase_package_entry_imports.py`，继续作为新 package 入口的回归测试。内容不变（import 已经是新路径）。

推荐**选项 A**——`test_phase_package_imports.py` 已经覆盖了新 package import，维护两个做同样事的测试文件没有意义。

### 迁移后：删除 4 个 shim

```bash
# 消费者全部迁移完成后，安全删除
rm src/phase1_entity_extraction.py
rm src/phase2_topology_builder.py
rm src/phase3_tick_simulation.py
rm src/phase4_report_agent.py

# 同时清理 __pycache__
rm -f src/__pycache__/phase1_entity_extraction.cpython-314.pyc
rm -f src/__pycache__/phase2_topology_builder.cpython-314.pyc
rm -f src/__pycache__/phase3_tick_simulation.cpython-314.pyc
rm -f src/__pycache__/phase4_report_agent.cpython-314.pyc
```

### 同步清理的文档引用

删除 shim 后以下文件的过时引用需要更新（建议归入 v1.2.6）：

| 文件 | 当前内容 | 改为 |
|---|---|---|
| `src/__init__.py:8-11` | docstring 列出 `phase1_entity_extraction.py` 等旧 flat 文件名 | 改为引用 `src/phase1/`、`src/phase2/` 等 package 路径 |
| `README.md:76-80` | 树形结构列出旧 flat 文件名 | 改为新的 package 结构 |
| `docs/structure_audit_cleanup_task.md:86` | 描述 `src/phase1_entity_extraction.py` 包含 legacy prompt | 更新为当前状态 |

---

## 8. 最终状态：迁移完成后的 `src/` 目录

当 3 个孤立文件归档 + 4 个 shim 删除后，`src/` 目录将变为：

```
src/
├── __init__.py              # 更新 docstring
├── schemas.py               # 公共数据模型
├── llm_client.py            # LLM 调用封装
├── phase1/                  # Phase 1: 实体提取（新 package）
│   ├── __init__.py
│   ├── extraction.py
│   └── prompts.py
├── phase2/                  # Phase 2: 拓扑构建（新 package）
│   ├── __init__.py
│   └── topology_builder.py
├── phase3/                  # Phase 3: 模拟推演（新 package）
│   ├── __init__.py
│   ├── context_builder.py
│   ├── simulation_card.py
│   ├── speaker_selector.py
│   ├── state_updater.py
│   └── tick_simulation.py
├── phase4/                  # Phase 4: 报告生成（新 package）
│   ├── __init__.py
│   └── report_agent.py
├── utils/
│   └── runtime_logger.py
└── whitebox/
    ├── __init__.py
    ├── artifact_check.py
    ├── report_completeness.py
    └── report_observer.py
```

对比当前 `src/` 下散落的 4 个旧 flat 文件 + 4 个 shim 的混乱结构，迁移完成后每个 Phase 一个 package、职责清晰、无重复、无兼容层。

---

## 9. Minimum Validation

### 第一阶段：归档 3 个孤立文件

```bash
# 1. 移动到归档目录
mkdir -p docs/_archive/legacy
mv src/phase0_entity_extraction.py docs/_archive/legacy/
mv src/phase1_persona_engine.py docs/_archive/legacy/
mv src/agent_quality_analyzer.py docs/_archive/legacy/

# 2. 验证：语法 + 全量 test + E2E
python -m py_compile main.py
python -m py_compile src/phase1_entity_extraction.py
python -m py_compile src/phase2_topology_builder.py
python -m py_compile src/phase3_tick_simulation.py
python -m py_compile src/phase4_report_agent.py
python -m pytest tests/ -v
python main.py seeds/test1.txt

# 3. profiling import 验证
python -c "from profiling.prompts import build_generator_prompts, build_validator_prompts; print('profiling OK')"
python -c "from scripts.probes.p1a_prompt_probe import main; print('p1a probe OK')"
python -c "from scripts.probes.p1g_prompt_probe import main; print('p1g probe OK')"
```

### 第二阶段：迁移消费者 + 删除 4 个 shim

```bash
# 步骤 1：补全 src/phase1/__init__.py 导出
#   → 追加 _normalize_unescaped_quotes_inside_string_values 到 import 块和 __all__ 列表

# 步骤 2：迁移 6 个消费者的 import 目标（9 行改动，详见 7B 节表格）
#   → profiling/prompts.py:5
#   → scripts/probes/p1a_prompt_probe.py:23
#   → scripts/probes/p1g_prompt_probe.py:23
#   → tests/test_json_parser.py:16
#   → tests/test_json_parser_quote_tolerance.py:16
#   → tests/test_legacy_shim_imports.py:8-12

# 步骤 3：验证新路径可解析
python -c "from src.phase1 import ANALYZER_SYSTEM_PROMPT, extract_entities_from_file, _normalize_inner_cjk_quotes, _parse_json_candidate, _normalize_unescaped_quotes_inside_string_values; print('phase1 new path OK')"
python -c "from src.phase2 import build_topology_from_extraction; print('phase2 new path OK')"
python -c "from src.phase3 import SimulationEngine; print('phase3 new path OK')"
python -c "from src.phase4 import generate_report_with_llm; print('phase4 new path OK')"

# 步骤 4：全量验证
python -m py_compile main.py
python -m pytest tests/ -v
python main.py seeds/test1.txt

# 步骤 5：删除 4 个 shim + 清理 pycache
rm src/phase1_entity_extraction.py src/phase2_topology_builder.py \
   src/phase3_tick_simulation.py src/phase4_report_agent.py
rm -f src/__pycache__/phase1_entity_extraction.cpython-314.pyc \
      src/__pycache__/phase2_topology_builder.cpython-314.pyc \
      src/__pycache__/phase3_tick_simulation.cpython-314.pyc \
      src/__pycache__/phase4_report_agent.cpython-314.pyc

# 步骤 6：再次全量验证（确认 shim 删除后一切正常）
python -m py_compile main.py
python -m pytest tests/ -v
python main.py seeds/test1.txt
```

---

## 10. Risk Register

| risk | severity | mitigation |
|---|---|---|
| Phase1 shim 删除导致 6 个消费者 break | **HIGH** | 不得删除 Phase1 shim。如需删除，必须先完成 6 个消费者的 import 迁移，并将 `_normalize_unescaped_quotes_inside_string_values` 加入 `src/phase1/__init__.py` 的 `__all__` |
| profiling pipeline 因 shim 变更中断 | **HIGH** | `profiling/prompts.py` 依赖 Phase1 shim 获取 prompt 常量。任何 shim 变更必须先确认 profiling 可用 |
| 删除孤立文件与 v1.2.5 closeout 合同不一致 | **LOW** | 3 个孤立文件在 v1.2.5 S6.3 中列为"禁止修改"。owner 已关闭 v1.2.5，可自行决定。需同步更新 CHANGELOG（当前有 2026-04-08 声称删除的虚假记录） |
| `docs/_archive/` 中文件无明确处置规则 | **LOW** | `_archive/` 目录按定义已是归档区，本轮不需要操作。但其中 `control_plane/` 可能可以考虑清理 |
| `scripts/generate_snapshot.py` 文档声称存在但磁盘未找到 | **MEDIUM** | 文档与磁盘矛盾。需用 `git log -- scripts/generate_snapshot.py` 查历史，确认是否需要恢复或更新文档 |
| 新 package `__init__.py` 导出不完整 | **MEDIUM** | `src/phase1/__init__.py` 未导出 `_normalize_unescaped_quotes_inside_string_values`（该函数通过 shim 的 `*` import 间接暴露）。迁移消费者前必须补全导出 |
| `src/__init__.py` 和 `README.md` 中的文档引用已过时 | **LOW** | `src/__init__.py` 第 8-11 行 docstring 仍引用旧 flat 文件名；README.md 第 76-80 行的结构图过时。建议在 v1.2.6 中更新 |

---

## 11. Final Recommendation to Owner

**可以直接归档的**：`src/phase0_entity_extraction.py`、`src/phase1_persona_engine.py`、`src/agent_quality_analyzer.py` 三个文件在全项目内零依赖、零引用、零 runtime 触达。移到 `docs/_archive/legacy/` 下面即可，删不删都对运行无影响。如果舍不得丢就归档留着当考古材料。

**做完迁移就能删的**：四个 shim 文件（`phase1_entity_extraction.py` ~ `phase4_report_agent.py`）不是永久资产，只是 v1.2.5 的冷备用过渡层。Phase2/3/4 三个 shim 的迁移成本极低——各只有 1 个消费者、各改 1 行 import。Phase1 shim 有 6 个消费者但全是机械操作（改 import 路径 + 补 1 个导出函数）。全部迁移完成后这四个 shim 就可以直接删掉，`src/` 目录将变成每个 Phase 一个干净 package 的理想结构。

**现在不要碰的**：`scripts/probes/` 下三个探针和 `profiling/` 下所有文件是活跃生产资产，不在本轮清理范围内。`src/__init__.py` 和 `README.md` 里的过时文档引用归入 v1.2.6 统一批量处理。
