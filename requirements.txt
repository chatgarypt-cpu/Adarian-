# Adarian 平行世界舆情推演系统 — 依赖
# Python >= 3.10
# 推荐方式：python -m venv .venv && .venv/bin/pip install -r requirements.txt
# 也可使用 pip install -e . 通过 pyproject.toml 安装

# Web 后端
flask>=3.0.0
flask-cors>=4.0.0

# LLM HTTP 调用
httpx>=0.27.0

# 数据模型
pydantic>=2.0.0

# CLI 可视化
rich>=13.0.0

# 环境配置
python-dotenv>=1.0.0

# YAML 规格文档
pyyaml>=6.0
